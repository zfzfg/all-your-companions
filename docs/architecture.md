# Architecture

How the Grok Build VS Code and desktop clients are put together, and the places
they deliberately stop being "thin." For day-to-day usage see the
[README](../README.md); for the test layers see [TESTS.md](../TESTS.md).

## The thin-client boundary

The host is a UI shell over either `grok agent stdio` or the pinned
`@agentclientprotocol/codex-acp` adapter. It speaks JSON-RPC over the
[Agent Client Protocol (ACP)](https://agentclientprotocol.com) on the selected
backend's stdin/stdout and renders one internal event vocabulary. Conversation
state remains in the selected CLI; provider identity and presentation state live
in the host.

| Lives in the CLI | Lives in the extension |
|---|---|
| Provider-owned conversation history and memory | Chips list (active editor + drag-added files) |
| MCP servers, subagents, plugins | Provider connection/model caches and immutable per-session provider metadata |
| Tool execution and model state | Grok auto-approval and client Plan gate; Codex permission presentation |
| Grok plan text on disk (`~/.grok/sessions/<…>/plan.md`) | Webview UI state, popovers, slash filter, pending diff per `toolCallId` |

Kill the host and its backend child dies with it; kill the backend and the host
surfaces an error and offers a fresh session. Restarting the session (the **+**
button) kills that child and spawns the same provider again; persisted provider
history survives.

## Message flow

```
webview / browser
       │ additive HostMsg/WebviewMsg (session.provider, providerState, models)
       ▼
sidebar host ──► AcpClient ──► GrokBackend   ──► grok agent stdio
                         ├────► CodexBackend  ──► node codex-acp (CODEX_PATH=codex)
                         ├────► ClaudeBackend ──► node claude-agent-acp
                         │                        (CLAUDE_CODE_EXECUTABLE=claude)
                         └────► GeminiBackend ──► gemini --acp  (native)
                                            └───► node agy-acp-adapter.js (Antigravity/agy)
       ▲                         │
       └── established internal events ◄── Codex wire normalization
```

`GeminiBackend.spawn` starts the native `gemini --acp` CLI unless the located
binary is Antigravity (`isAntigravityCli`), in which case it instead spawns
`src/agy-acp-adapter.ts` under Node (`AGY_PATH`, `ELECTRON_RUN_AS_NODE=1`) —
a bespoke ACP-over-stdio bridge in front of `agy`'s own `--input-format
stream-json` protocol, not a real ACP agent. The UI label stays "Gemini"
either way; provider identity is `gemini` in both cases. Antigravity has no
`session/request_permission` (writes run through, `--dangerously-skip-permissions`
under Auto accept) and its tool steps carry only raw parameters, so
[src/diff-synthesize.ts](../src/diff-synthesize.ts) builds the same
`{type:"diff"}` block Grok/Codex send natively — `agy-acp-adapter.ts` does
this for its own live tool_call/tool_call_update pairs and (best-effort,
docs/UNIVERSAL_DIFF_SUPPORT_PLAN.md § 4.6) its `session/load` transcript
replay; `claude-backend.ts` / `gemini-backend.ts` normalizers do it for
Claude and native `gemini --acp` tool calls that arrive without one.

Grok supplies the mandatory `fs/*` and `terminal/*` callbacks, native
`x.ai/exit_plan_mode` / `x.ai/ask_user_question`, and its private notification
rail. Codex executes commands and edits server-side; its adapter sends ordinary
tool/diff updates and `session/request_permission`, including plan review.

`initialize` uses `acpClientCapabilities(provider, grokVersion, versionVerified)`:
only a live-verified grok >= 1.0.4 withholds `readTextFile` so the CLI's
image-aware `read_file` runs (#79). Measured 1.0.4+ builds treat client fs as
all-or-nothing, so that also stops write delegation. 0.2.x, 1.0.0–1.0.3,
Codex, an unreadable version, and a cache/unverified banner keep
`readTextFile: true`. The handlers still exist (`fs/read_text_file`,
`fs/write_text_file`, `terminal/{create,output,wait_for_exit,kill,release}`);
`terminal` is a separate capability.

`AcpClient` has a provider seam at the wire's divergence points. The default
`grokBackend` is an identity adapter. `CodexBackend` and `ClaudeBackend` spawn
the pinned `@agentclientprotocol/*-acp` entry points under Node with
`ELECTRON_RUN_AS_NODE=1` and the user's own CLI path (`CODEX_PATH` /
`CLAUDE_CODE_EXECUTABLE`). Claude's adapter is unbundled ESM, so the vsix also
packs its JS deps and never the optional native SDK binaries. Both adapters
normalize models, config options, usage, titles, and `session/list` into the
existing host shapes. Plan enforcement stays in the adapter/CLI, so the Grok
terminal/fs Plan gate is off for those providers. Claude authentication stays
in Anthropic's own `claude` CLI — this host never implements, proxies, holds,
or forwards Claude credentials.

Connection is explicit and binary-aware. `grok.providerConnections` records the
user choice; a located binary alone never connects Codex. `grok.providerModelCache`
holds each provider's last advertised model list, and `grok.projectProviderDefaults`
holds the last provider/model for each normalized project path. These exact keys
are used by VS Code globalState and the desktop `globalState.json` memento. An
empty cached `modelId` means “use that provider's default,” not “no selection.”
On Codex connect, a short-lived adapter creates a session in a temporary scratch
cwd, stores the advertised models, deletes that throwaway session through ACP,
and disposes; a failure is logged and does not fail the connection.

If no Codex binary is found, onboarding can install the pinned official
`rust-v0.147.0` standalone package into versioned global storage. The download is
streamed to a sibling staging file, SHA-256 verified before decompression, unpacked
with the dependency-free tar reader (preserving regular-file mode bits on POSIX),
then renamed into place atomically. Discovery
checks this managed package last, so a configured path, PATH command, or ChatGPT
extension bundle always wins. Every sidebar discovery, including session start,
uses the same class-owned locator inputs. Progress and cancellation stay host-local.

The new-session picker groups cached models from all connected providers, Grok
first. Choosing a model chooses the backend and persists it in the session's
`grok.sessionMeta` override; absent metadata means Grok for older installations.
An empty conversation may switch providers through the same discard-and-restart
path on the desk and remotely. After the first user turn the provider is immutable,
and the picker is provider-local; the host also infers the owner of a cached model
from older provider-blind clients and rejects a cross-provider live pick with a
targeted notice before it reaches an adapter. The additive `provider` field travels
in session/history frames through `HostMsg` to the shared webview and browser client.
Every remote snapshot also carries the relay-safe `providerState` frame
(`id` + `connected`, plus host-probed CLI/adapter version facts when known). The inline, `currentColor` provider marks appear only
when more than one provider is connected. In mixed-provider history and rail rows
the status dot overlays the mark; single-provider and old-host rows retain the
standalone dot structure.

The shared `media/chat.js` gear opens with **Use this app for**, then Remote
Control (desk only; Continue remotely + Your account, or Sign in / How it works —
unlink is Settings → Account only), then a single **Settings** entry. Versions,
CLI update, bug/feature tracker links, contact, and the non-affiliation
disclaimer live in Settings → About. Provider account rows appear on the desk gear only when no
provider is connected or one needs login; healthy connected accounts live in
Settings → Providers. The browser receives view-only connection state and
renders no account-management controls in the gear. Desk account actions
reuse the sidebar's provider login/logout messages. The exhaustive inbound policy
classifies `logout` and the durable `recheckConnection` as
host-local, so a modified remote cannot clear credentials or
open a login terminal on the desk. `runGrokLogin` and `setupGithubCli` are
`full`: a remote sign-in is the headless device-code flow, not a terminal. A remote `retryProviderSession` may only restart
an already-connected provider; signed-out remote onboarding shows desk guidance
without dead action buttons. Login remains visible in a terminal. Its action starts
a bounded credential re-probe, and Re-check bypasses the history freshness clock;
Codex probes use `isCodexCredentialError`, so an uncoded `Sign in required` result
sets `needsLogin` and a later success clears it. Grok uses the same observable
re-probe lifecycle. Codex logout runs as an observed one-shot process and clears connection state only after exit success
(an unspawnable process is opened in a terminal while state remains connected).
After a successful sign-out, the provider is disconnected in memory and every
matching focused, background, active-remote, or detached-tab session is
synchronously detached before the memento write or replacement startup can
stall. Every queued draft with a conversation id is written to that conversation's
`grok.sessionMeta` at capture time. Focused and active-remote drafts return to
replacement composers only after replacement startup succeeds; failed starts leave
both META and the in-memory stranded copy intact. Background drafts get a named
transient desk notice, including start races that do not yet have an id. Detached
tabs keep an inert replacement and the META-backed draft, but deliberately miss
both transient notices and composer restore frames while disconnected. Every
same-provider, other-provider, and needs-provider reattachment restores and only
then clears META once the composer is live. Draft-bearing and `needsProvider`
sessions are protected from parking, release, empty sweeps, and TTL/LRU reaping;
provider retargeting scans detached logical tabs as well as attached sessions.
The local replacement uses the focused session's still-authorized cwd rather
than the project currently browsed in history. A rejected memento write is
reported but cannot make the old clients reachable again. A crashed or
clientless focused session on the other provider is untouched.
The desktop has no Codex-path settings row. Its JSON config store still reads
`grok.codexCliPath`, and VS Code keeps the contributed setting, so file/settings
overrides continue to participate in discovery without renderer plumbing.

Settings live in one shared surface ([media/settings.js](../media/settings.js)): a view over existing prefs and actions, plus the voice send-phrase / dictionary setters (`setVoiceSendPhrase` / `setVoiceKeyterms`, classified `propose` so a phone can edit them). Desktop and the remote browser open it as a full-window overlay from gear → **Settings**. The overlay traps Tab, marks covered siblings `inert`, and restores the opener on Escape or the top-left **← Back to app** link (above search; inside the trap). VS Code opens the same component in an editor-area webview tab (`grok.settings`, also a view/title gear on `grok.chat`) and has no Back link — the tab closes natively. Categories are General (purpose + chat display + telemetry), Voice, Notifications, Providers, Connectors, Account, Advanced, About — each nav row is icon + label. Connectors has three sections: On this computer is the host-owned Tier-1 list passed through ACP `session/new` `mcpServers` (connect/disconnect are desk-only: OAuth writes `~/.mcp-auth` on the machine; GitHub pastes a PAT into `HostSecrets` and a remote cannot set, read, or clear it); Grok.com connectors is the account-level grok inventory (`scopeName` is the team name; Open grok.com in the section header); Local Grok connectors are user-level config-file servers (header Open on the desk even when empty; a sentence on remote). Host-injected echoes are omitted from Local. Project-file servers are omitted at read time against the workspace that list was read from; the stored global-only view is rendered anywhere. Origin tags are gone. Live `_x.ai/mcp/*` notifications update health without polling. About is last and is the only place the non-affiliation disclaimer appears. Restore defaults only resets toggles/selects/sliders (`restoreChanges` — never free-text or list inputs such as the send phrase or dictionary), hides the button when nothing on the page would change, and expands an in-surface confirm that lists the rows and target values before acting. The tab snapshots on open and posts the same `set*` / `open*` messages, so a change cannot be lost or desync the sidebar. Host-local Advanced rows stay hidden on remote; Providers, Connectors, and Account render as read-only desk connection state plus a device-manager link. Connectors on a remote keeps the desk-owned catalog read-only, the live Grok inventory (page fields only — `transformHostMsgForRemote` strips launch recipes), a grok.com Open in the grok.com header, and a sentence that local config files are managed on the desk. Telemetry is a desktop toggle, a VS Code settings-opener, and a remote read-only row using the privacy.md claims.

The `postMessage` half (host↔webview) is a **typed contract**: [src/protocol.ts](../src/protocol.ts)
is the single source of truth — `HostMsg` (host→webview) and `WebviewMsg` (webview→host)
discriminated unions. The host types `post`/`emit` against `HostMsg`, and a test asserts the
webview's mirror of the type list ([media/webview-helpers.js](../media/webview-helpers.js)) stays
in sync and that `chat.js` handles every host type — closing the "post one shape, handle another"
gap the untyped `any` direction used to leave open around restore, pagination, and media.

## How a session starts

When the panel opens (or you click **+** for a new session):

1. Resolve the selected provider. Grok uses `grok.cliPath` → `~/.grok/bin/grok`
   → PATH. Codex uses `grok.codexCliPath` → PATH → the newest matching OpenAI
   ChatGPT extension bundle → the versioned extension-managed package.
2. Spawn `grok agent stdio`, or Node with the packaged Codex ACP entry point and
   `CODEX_PATH` set to the located Codex binary.
3. Run `initialize` → `session/new` (or `session/load` to resume). Grok keeps its
   existing `session/set_model` lifecycle; Codex configures model, effort, and
   mode through `session/set_config_option`.
4. Normalize backend updates at the host boundary and stream the established
   message, thought, tool, permission, model, and usage events into the chat.

The composer unlocks as soon as the session is live. Its placeholder follows the
session provider (**Ask Grok…** / **Ask GPT…**). While a user turn is waiting, the
same animated activity row says **Grokking…** for Grok or **Opening AI…** for
Codex, then is replaced in place by the first thought / message / tool card.

### One stdout chunk is dispatched synchronously — state raised after an `await` is too late

`readline` emits a `line` event for **every** line in a chunk before the write
returns, so two ACP messages that arrive together are handled in the same
synchronous turn. Anything a handler sets *after* awaiting a response is
therefore **not** in force when the next line is processed.

This is not theoretical. It is how a `terminal/create` arriving in the same chunk
as a `session/set_mode` success ran `rm -rf` with Plan mode's gate still down:
the gate was raised after the await, one event-loop turn too late. The fix, and
the pattern to copy, is the `onResolve` hook on `AcpClient.request(...)` — it
runs **inside** `onLine`, after a successful response and before the promise
resolves, so state committed there is visible to the very next line. See
[Plan Mode](#plan-mode--provider-owned-review-grok-client-safety-gate).

Whenever a decision depends on state a response is *about* to establish, commit
it in that hook, not after the `await`. Tests must drive real line dispatch — a
single `stdout.write` carrying both JSON lines — because a test that
hand-sequences the two events cannot fail the way production does. Three review
rounds missed this precisely because the tests sequenced by hand.

### Session starts are serialized, and an abandoned send says so

`startSession` bumps `session.gen`, and `handleSend` checks that generation
after it has already emitted `userMessage`. A concurrent start therefore used to
strand a send: the echo painted a bubble, any client reasonably read it as
acceptance, and the guard then returned with no `agentEnd` and no `agentError`,
so no reply ever came and nothing retried. Remote clients drop their retry record
on that echo, so this was silent work loss.

Two rules now hold:

- **A post-echo bail reports itself.** `emitAbandonedSend` emits a generic
  `error` (`INTERRUPTED_SEND_TEXT` + additive `code: "interrupted-send"`),
  not `agentError` — after the generation bump this `Session` *is* the
  replacement, and `agentError` would clear its startup lock or a flushed
  follow-on turn. Cancel recovery emits its own `agentError` first and sets
  `staleSendReported`, so an abandoned send is never double-reported.
- **Starts do not interleave with a live turn.** `runExclusiveSessionStart`
  serializes starts per `Session`, and `handleSend` waits for that tail before
  committing. `decideSessionStart` takes an intent: opportunistic callers —
  desktop boot, `client-ready`, non-live `resumeSession` — pass `"ensure"`,
  which refuses while a turn is in flight, reuses a matching ready client, and
  never bumps `gen` over a live send. Deliberate restarts (cancel recovery, auth
  recovery, model/effort changes) pass `"replace"`.

The generation guard itself is correct and stays; the bug was the silent return.
Note that an already-echoed send **cannot** be transparently replayed — the echo
is in the session buffer, so a client-side retry duplicates the prompt. Visible
and recoverable is the correct terminal outcome for that interleaving; it is not
a substitute for delivering work queued while the host was down, which still
arrives.

## The session pool (Agent Dashboard)

The sidebar shows one conversation at a time, but it keeps a **pool of live
sessions** behind it — one spawned backend process each, with exactly
one *focused* (the one you see). All the per-session state lives in a
[`Session`](../src/session.ts) object; the sidebar holds `focused` plus a `Set` of
every live `Session` (`pool`). The point is **lossless re-focus**: a backgrounded
session keeps streaming into its own *view buffer* (every webview post that built
its chat, in order), so re-focusing it is a `clearMessages` + replay of that
buffer — no backend reload, no process kill, even mid-turn or mid-approval.
The webview defers destroying those nodes until the first replacement append
(`appendTranscriptChild`) or the next animation frame (`flushPendingTranscriptClear`).
`body.identity-restoring` suspends that next-frame flush until the class lifts,
and only then reveals the welcome if nothing replaced the nodes. The welcome
stays hidden (`welcomeHoldActive`: a painted conversation, not a `clearMessages`
mark; also `body.identity-restoring` on a cold restore of a remembered
conversation, which must hide the welcome because it starts visible;
`pendingWelcomeReveal`) and the title / composer
focus / reader's pin (`pendingSessionChromeReset`) until that same moment. Desktop launch still
takes composer focus once (`takeDesktopLaunchComposerFocus`) so a restored
conversation is ready to type into; a later resync of an already-open surface
does not, and VS Code never takes this shot.

Switching focus (`focusSession`) never touches the backend: it swaps `this.focused`,
replays the target's buffer to the webview, and re-pushes the mode/sessions UI.
Clicking a session that *isn't* live (cold — it was reaped, or predates this
window) loads it from grok's on-disk history into a fresh pool member instead
(`openSession`).

Two details make the pool safe:

- **Per-session generation guard.** Each `Session` owns a `gen` counter, bumped
  only when *its* client is torn down. Handlers capture their session's `gen` when
  wired, so a backgrounded session's in-flight events are never judged "stale" just
  because focus moved elsewhere (the old global counter would have done exactly
  that).
- **Session-scoped emit.** `emit(session, …)` buffers to that session and only
  forwards to the webview when it's the focused one; `post(…)` is for UI-wide
  messages (status dots, the sessions list) that aren't tied to one chat.

**Status dots.** Every row in the history dropdown shows a dot. It's **gray** at
rest — and "at rest" is deliberately one bucket: idle, already-read, cold, or
loaded-from-disk all look the same, because the warm-process-vs-cold distinction is
an implementation detail no user should have to reason about. It lights up only
when there's something to know: **blue** working, **yellow** needs-you (a pending
permission / question / plan review), **green** *finished while no view was
watching*, **red** *errored while no view was watching*.

The green/red dot is a **globally unseen-completion badge**, not a per-tab read
receipt or a live state. The persisted schema has one `unread` flag per conversation,
so it cannot represent “read in tab B but unread in tab A.” The deliberate rule is:
set the flag only when a turn ends while neither the local VS Code view nor any
remote tab owns that session. Focusing it in either surface clears the flag. Because
the flag lives in metadata rather than the live process, the badge **survives both
the idle reaping below and a full VS Code restart** — so unattended results remain
visible when you return, without marking a result unread in the tab that watched it.
There's no timer. The actual color is a pure
function ([`computeDot`](../src/session-pool.ts)) of `(live status, unread,
unreadError)`, so the policy is unit-tested without a process pool. The host pushes
one changed dot at a time (cheap, no disk read) and the full map on each list
refresh.

**Reaping** ([src/session-pool.ts](../src/session-pool.ts)). A live process per
session isn't free, so the pool is bounded — silently. The pure `selectReapable`
picks victims under two rules: an **idle TTL** (a session untouched for an hour is
torn down, swept every 5 min) and an **LRU cap** (at most ~8 live; the
least-recently-used eligible sessions are evicted past it). It **never** reaps the
focused session or a `working`/`needs-you` one — so the cap can be exceeded when
everything spare is busy, by design. Reaping just kills the process and recomputes
the dot — a reaped session that's still unread **stays green**, a read one goes
gray — and re-clicking the row reloads the session from disk.

One safety valve sits next to this: the explicit **Update Grok Build CLI** action
tears down every live session to swap the binary, so it now confirms first if any
session is `working` or `needs-you` (the silent startup auto-update runs before
anything is in flight, so it doesn't ask). The teardown is **awaited** before
`grok update` runs — `kill()` only signals, and on Windows the `grok.exe` lock
clears a beat after the process actually exits, so an un-awaited update would race
it and fail with *"cannot rename locked executable"*. On Windows the kill is a
`taskkill /T /F` of the process **tree** (grok backgrounds subagent/command
children that a parent-only kill would orphan, and they keep the binary locked),
and the update retries once if a lingering lock still slips through.

`maybeUpdateCliOnUpgrade` retains the normal session-start trigger: once per
activation it compares `CLI_UPDATE_VERSION_KEY`, updating only after an extension
version change; a fresh install records its baseline without updating. The update
is bounded at **20 seconds** and attempted **once per extension version whether or
not it succeeds** — it is awaited before the spawn with the composer locked, so a
network that cannot reach `x.ai` (where a no-op `grok update` costs ~68s rather
than ~0.8s) would otherwise re-charge that wait on every new window forever. A
skipped update is self-correcting: the version floor and Plan gating still run
against whatever is installed, and the CLI's own `autoUpdate` catches it up.
After that,
every session start reads `grok --version` through `resolvePlanModeAvailability`
(one short retry when the first read is empty/unparseable, then the last verified
banner in `grok.cliVersionCache` when that binary's mtime/size still match). A live
parseable answer always wins over the cache. On Windows, `maybePinBrokenCli` uses the
bounded `isStdioBrokenGrokVersion` check to move 0.2.61–0.2.70 to the current
`GROK_STDIO_DOWNGRADE_TARGET` before ACP spawn. `GROK_REQUIRED_VERSION` is the
cross-platform ACP behavior floor and the current recovery target.
A live parseable CLI below the floor latches Plan off (`planModeVersionVerified:true`);
an unreadable probe with no matching cache also sets `planModeAvailable:false` but
stays re-checkable — the picker keeps Plan clickable
(`planModeAvailability.recheckable`) and `setMode` re-probes via
`recheckPlanModeAvailability` instead of forcing a restart. A cache substitute
keeps that availability but is never verified, so a stale below-floor banner
stays re-checkable and a later live probe replaces the stand-in. A live
verified-old CLI still hard-disables the Plan row and rejects forged Plan requests. Agent-initiated and restored Plan transitions raise the client safety gate.
A live untrusted planning turn is cancelled, and the gate stays raised until both
that `session/prompt` settles and `session/set_mode(default)` confirms Agent; a
failure or stalled recovery stays gated and is surfaced explicitly.
Any stray `exit_plan_mode` request is answered with an error rather than entering the
native-verdict flow that this version floor exists to protect.
Availability is session-scoped so a later successful update re-enables Plan only for
newly started compatible processes, never for an older process that is still alive.
Reactive Windows stdio recovery remains a separate single-retry backstop after an
observed startup failure.

## Plan Mode — provider-owned review, Grok client safety gate

Grok owns plan-review continuation. The extension responds to
`_x.ai/exit_plan_mode` with its native success result: `approved`, `cancelled`
(Keep planning), or `abandoned` (Cancel). Approval continues into implementation
inside the original turn; cancellation stays in Plan and lets grok revise and
re-ask inside that same turn; abandon switches the CLI to its default mode and
ends the turn without a continuation. There is no synthetic verdict prompt, turn
cancel, or synthetic lifecycle.

- **The gate** ([src/plan-gate.ts](../src/plan-gate.ts)). While Plan Mode is
  active, `terminal/create` that isn't on a read-only allowlist is blocked —
  that hook is load-bearing because the CLI still hands mutating shells to the
  client. On grok 1.x, Plan-mode file safety rests on grok's native edit
  refusal (writes are not delegated). The `fs/write_text_file` workspace block
  stays in the handler for 0.2.x (still delegated) and for a later CLI that
  honours `writeTextFile` independently. Plan review is fed by `req.plan`
  (`exit_plan_mode.planContent` arrives populated on 1.x; 0.2.117 still sends
  `null`); the plan.md snoop is a fallback. Entering plan mode *any* way —
  including the agent self-initiating it — raises the gate; only an explicit
  user action lowers it. A grok user Plan pick commits `planActive` in the
  `session/set_mode` response hook — after a successful reply, before the next
  ACP line — so a same-chunk `terminal/create` or `session/request_permission`
  still sees the gate. A rejected transition keeps the previous badge and gate.

- **Verdict state and comments.** `handleExitPlan` settles all implementation-
  relevant state *before* releasing the blocked response. Approval restores the
  remembered pre-plan Auto accept choice and lowers the gate; Keep planning keeps
  the gate raised; Cancel lowers it but deliberately lands on Agent. An
  Approve/Keep-planning comment is sent through `_x.ai/interject` before the
  verdict response, without awaiting it inline. Clicking a verdict collapses the
  card immediately. A successful response write emits buffered `planResolved`;
  a failed write leaves the pending request unconsumed and the verdict
  unpersisted, so re-focus rebuilds an actionable card without a separate
  pending/failure message. While the interject response is outstanding, a
  memory-only `Session.inFlightPlanComments` entry owns the text. Acceptance
  removes it synchronously; a controlled restart moves only unresolved entries
  into the ordinary queue for the replacement process. A dead process drops its
  queue. Session restart clears the map, so it cannot resurrect text
  later. A write accepted by Node is never retried merely because its downstream
  effect is unknown. If the
  unadvertised RPC is unsupported or fails, the text is placed in the ordinary
  queued-send path rather than being lost. An abandon comment always uses that
  queue: the native abandoned turn has no continuation step that could drain an
  interjection, so the comment becomes a real prompt after the turn settles. A
  successful interjection emits the same `userMessage {steer:true}` shape as the
  ordinary Steer path and does not increment `Session.userMessageCount`; it has
  no prompt/rewind point, so counting it would inflate every later
  `afterUserMessage` position and make rewind discard surviving extension records.
  Plan, permission, and usage persistence share `afterHistoryEvent`, a replay-stable
  assistant/tool update boundary. It places native approvals before same-turn
  implementation output. `afterInterjection` remains the secondary compatibility
  boundary for comment/revision cycles, with array-order inference for older entries. The webview
  keeps the raw CLI envelope for classification but strips it and `<user_query>`
  from the displayed/copied comment.
  Each `Session` owns a `pendingExitPlans` map keyed by the ACP request id. The host
  registers a request only after its async snapshot generation check, and an answer
  must find that exact entry. Gate changes happen before the JSON-RPC response so
  same-turn implementation is safe, but consuming the entry and persisting the
  verdict happen only after `respondExitPlan` reports an accepted stdin write.
  Re-focus can replay the card without consuming it; stale and duplicate answers
  have no effect.

- **Legacy primer reads.** Older sessions on disk contain the retired v4 hidden
  primer and bracket-marker turns. `src/grok-primer.ts` therefore keeps only the
  version-agnostic `isPrimerText()` / `isPrimerSummary()` readers. Replay hiding,
  title repair, the empty-session sweep, and rewind/plan-position mapping continue
  to account for those historical turns. `suppressContent` / `SUPPRESS_TYPES` are
  also retained for other hidden maintenance turns; they are no longer a priming
  mechanism.

Codex and Claude plan review follow a different wire path: a normal
`session/request_permission` whose tool kind is `switch_mode` and whose
allow option means "implement this plan". The reply is still the selected
permission option. The card is not the generic permission chrome —
`planTextFromPermissionToolCall` lifts Codex `rawInput.plan` / Claude's
ExitPlanMode content block, and the webview renders grok's plan-review
shape with those adapter mode options. A `switch_mode` card that arrives
with no plan text must not collapse to an "Approved" / "Plan approved"
label. Auto accept does not select that option — `isPlanReviewPermission`
keeps `switch_mode` cards out of `autoApprovePendingPermissions` and the
incoming Auto-accept grant, so a mode flip (even one whose RPC later
fails) cannot implement an unread plan. `CodexBackend` reports
`usesClientPlanGate = false`, so none of the Grok filesystem/terminal
gate, plan-file snooping, or `x.ai/exit_plan_mode` verdict machinery is attached.
A successful Plan `session/set_config_option` still raises `client.planActive`
in the response hook so Auto accept cannot grant that review (or any other
same-chunk permission) before the host await continues. Their Plan/Agent chrome
still follows the agent's mode (`applyAgentModeToHostPlan`, plus Codex
`config_option_update`) so the button cannot stay on Plan after an approved
exit. Codex's effective mode is Plan when `collaboration_mode` is Plan
and otherwise the permission `mode` (`codexEffectiveModeId`) — collaboration
`default` is not flattened to Agent, because the adapter always reports
`agent-full-access` on that same snapshot.

The full pedagogical write-up lives in
[research/understanding-plan-mode.md](../research/understanding-plan-mode.md).

## Module map

| File | Role |
|---|---|
| [src/extension.ts](../src/extension.ts) | Entry point — registers commands, keybindings, output channel |
| [src/sidebar.ts](../src/sidebar.ts) | Webview provider, message routing, fs handlers, native diff opening, logout, generated-media serving (`postGeneratedMedia` → `asWebviewUri`, base64 fallback) |
| [src/diff-view.ts](../src/diff-view.ts) | Pure whole-file native-diff reconstruction (#66) — combines Grok's replaced regions + positioned sites with disk content, bounds expansion size, and finds the first changed line; `planEditRevert` runs the same reconstruction in reverse against the file's current content to decide how a single-edit revert should proceed (write/delete/conflict — docs/UNIVERSAL_DIFF_SUPPORT_PLAN.md) |
| [src/diff-synthesize.ts](../src/diff-synthesize.ts) | Pure universal-diff synthesis — builds the ACP `{type:"diff"}` block from a tool's raw edit parameters so Antigravity/Claude/native-Gemini tool calls get the same inline diff + revert support Grok/Codex already carry natively; `mergeDiffIntoContent` appends without ever replacing existing content, idempotent per path (a native diff always wins) |
| [src/acp.ts](../src/acp.ts) | Provider-neutral ACP client — spawns the selected backend, manages session lifecycle, normalizes through its backend hooks, and emits the extension's established events. `interject` (#52 Steer; `{sessionId, text}` plus additive `content` for images, omitted when text-only), `forkSession` (#48), `submitFeedback` (#114), and worktree RPCs (P2-8) call the unadvertised `_x.ai/*` methods, returning `"unsupported"` on -32601 rather than throwing |
| [src/feedback.ts](../src/feedback.ts) | Pure thumbs-feedback helpers (#114) — availability plus `grok.thumbsFeedback` opt-in (default off), snake_case params, no `turn_number` (agent attributes the current turn); see [research/turn-feedback.md](../research/turn-feedback.md) |
| [src/acp-backend.ts](../src/acp-backend.ts) / [src/grok-backend.ts](../src/grok-backend.ts) / [src/codex-backend.ts](../src/codex-backend.ts) / [src/claude-backend.ts](../src/claude-backend.ts) / [src/gemini-backend.ts](../src/gemini-backend.ts) | Backend contract, Grok identity, Codex/Claude/Gemini host normalization. Codex uses `session/set_config_option`; Claude and Gemini map `configOptions` into the host model picker, list sessions with `{ cwd }`, and map Agent/Auto-accept onto native permission modes. Claude's and Gemini's normalizers also synthesize a diff block onto tool calls that arrive without one (see `src/diff-synthesize.ts` above) |
| [src/gemini-cli-locator.ts](../src/gemini-cli-locator.ts) / [src/agy-acp-adapter.ts](../src/agy-acp-adapter.ts) | Locates `gemini`/Antigravity's `agy` binary (`isAntigravityCli`); the adapter is a standalone ACP-over-stdio bridge in front of `agy`'s own stream-json protocol — tool-call mapping, plan-review bridging (`x.ai/exit_plan_mode`), session listing from agy's SQLite + a JSON resume-id store, transcript-based `session/load` replay, and universal-diff synthesis for `write_to_file` / `replace_file_content` / `multi_replace_file_content` |
| [src/codex-model-cache.ts](../src/codex-model-cache.ts) / [src/claude-model-cache.ts](../src/claude-model-cache.ts) | Short-lived connect warm-up that caches adapter models from a scratch `session/new`, deletes the temporary adapter-owned session, and cleans up the client/cwd |
| [src/provider-ui.ts](../src/provider-ui.ts) | Pure provider presentation/state policy — Grok-first model grouping, empty-model default sentinel, normalized project defaults, adapter listing-time freeze (`adapterActivityAt` pins Codex and Claude to the host-observed clock), clear-all refresh guard (`adapterEntriesEligibleForClear`), and mixed-provider recency merge |
| [src/provider-capabilities.ts](../src/provider-capabilities.ts) | Single source of truth for companion capabilities across 4 providers (`grok`, `codex`, `claude`, `gemini`) and 11 features (`steer`, `rewind`, `fork`, `worktree`, `planMode`, `clientPlanGate`, `vision`, `manualCompact`, `questionRpc`, `feedback`, `subagents`) with runtime probe overlays, user-facing tooltip reason strings, and webview broadcast |
| [src/worktree.ts](../src/worktree.ts) | Pure worktree helpers (P2-8) — parse create/list/apply/remove/status, multi-cwd history merge; wire notes in [research/worktree.md](../research/worktree.md) |
| [src/session.ts](../src/session.ts) | Per-session state bag — one `Session` per live backend process, with `provider` fixed after the first user turn except a confirmed limit-failover switch (AP-06); the sidebar holds a *pool* plus one focused; carries the send queue (#37, per-item `{text, chips}` via `queued-send.ts`), exclusive history-load join (`runExclusiveHistoryLoad` / `loadInFlight`), and optional worktree binding (`cwd` / `worktree`), while cumulative billing stays solely in session-id-keyed metadata (#53) |
| [src/session-pool.ts](../src/session-pool.ts) | Pure reaping policy (`selectReapable`) — idle-TTL + LRU cap over the live-session pool |
| [src/acp-timeout.ts](../src/acp-timeout.ts) | Pure ACP request-timeout policy (#117) — idle+absolute for `session/prompt`, fixed cap for other methods, clamp/resolve helpers for `grok.acp.*` settings |
| [src/acp-dispatch.ts](../src/acp-dispatch.ts) | Pure protocol helpers — line parsing, update routing, response + generated-media extraction, live context extraction (`contextUsedFromUpdateEnvelope`, compact notifications, `parseSessionInfoRpcResult` / `sessionInfoCacheFresh`, `occupancyFromAdapterTurn` / `applyContextOccupancy`), billing helpers (`extractPromptUsage`/`addUsage`/`usageIsRealMeasurement`, including `costUsdTicks`), and the -32601 capability gate behind private RPCs |
| [src/limit-errors.ts](../src/limit-errors.ts) | AP-06 quota/rate-limit classification (pure) — `classifyLimitError` wraps `isRateLimitError` / `isCredentialError` / `isAuthErrorText` (code before text, limit family before auth) and refines rate vs quota per provider with source-commented patterns. Uncertain → `null`. `failoverTargets` never includes the exhausted provider; `switchTranscriptLine` is the reconstructable transcript of a confirmed switch |
| [src/permission-rules.ts](../src/permission-rules.ts) | AP-07 permission-rule engine (pure) — `decidePermission` (safety floor, then Deny-wins, then last-match-wins) over `{tool, kind, command, paths}`. Empty user rules → `ask` (today's card / Auto accept). Workspace rules live in `.grok/permissions.json` and stay inert until the file hash is adopted; global rules are a `PersistedState` record map. `canonicalPath` / `matchPathGlob` / `isPathUnder` are the path toolkit AP-08 reuses |
| [src/protocol.ts](../src/protocol.ts) | Single source of truth for the host↔webview message contract — `HostMsg`/`WebviewMsg` unions + the runtime `HOST_MESSAGE_TYPES`/`WEBVIEW_MESSAGE_TYPES` arrays (kept exhaustive by compile-time `Record` maps). Pure types + two arrays, no runtime deps |
| [src/cli-locator.ts](../src/cli-locator.ts) / [src/cli-path.ts](../src/cli-path.ts) / [src/cli-process.ts](../src/cli-process.ts) | Locate and invoke the `grok` binary cross-platform; `cli-path.ts` is the shared shell-free PATH scan every provider's locator uses, and one shim-aware execution policy covers ACP spawn plus version/update commands |
| [src/codex-cli-locator.ts](../src/codex-cli-locator.ts) / [src/codex-managed-installer.ts](../src/codex-managed-installer.ts) / [src/claude-cli-locator.ts](../src/claude-cli-locator.ts) | Pure, injected Codex discovery with the managed package at lowest priority; Claude discovery is PATH + `grok.claudeCliPath` + well-known user-bin locations only — no managed Anthropic installer. Windows `.cmd` shims are resolved to a native exe (`resolveClaudeSpawnTarget`) because the SDK spawn is `shell: false` |
| [src/terminal-manager.ts](../src/terminal-manager.ts) | Headless shells for the agent's `terminal/*` calls |
| [src/plan-gate.ts](../src/plan-gate.ts) | Plan-mode policy (pure) — workspace-write containment + read-only command allowlist |
| [src/plan-entries.ts](../src/plan-entries.ts) | Normalizes ACP's structured `plan` update into the session checklist (pure) — `parsePlanEntries` (null when the update carries plan TEXT and no `entries` array) + `planProgress` |
| [src/plan-restore.ts](../src/plan-restore.ts) | Plan persist + restore decision (pure) |
| [src/grok-primer.ts](../src/grok-primer.ts) | Legacy primer replay/title detection helpers (pure) |
| [src/chips.ts](../src/chips.ts) | File-chip CRUD (pure) |
| [src/context-chips.ts](../src/context-chips.ts) | The `ContextChip` union beyond files (pure) — diagnostics/terminal variants, type guards, label+icon derivation, and the per-kind prompt serialization collected at SEND |
| [src/prompt-builder.ts](../src/prompt-builder.ts) | Chip → prompt-string with `@path` refs and fenced blocks (pure) |
| [src/slash-filter.ts](../src/slash-filter.ts) | Slash-command autocomplete filter (`filterCommands` ranks name prefix → mid-name → description) + `matchSlashCommand` dispatch gate + hidden-command filter (`filterAdvertisedCommands` drops the config-mutating `/always-approve`) (pure) |
| [src/mention.ts](../src/mention.ts) | The composer's `@` file popover, host half (pure) — `filterMentionFiles` ranking, `buildExcludeGlob` (files.exclude + search.exclude → one findFiles exclude), `orderMentionIndex`, `clampMentionIndexLimit` (`grok.mentionIndexLimit`) and `mergeMentionEntries` (open tabs layered over the capped findFiles snapshot, #69); the webview half (`getMentionQuery`/`applyMentionPick`) lives in webview-helpers.js |
| [src/grok-config.ts](../src/grok-config.ts) | Reads grok's `config.toml` to detect `permission_mode = "always-approve"` so the mode button shows Auto accept (pure) |
| [src/rules-files.ts](../src/rules-files.ts) | AP-04 rule/instruction-file panel — `ruleFileCandidates(cwd, home)` builds the twelve project+global candidates (`AGENTS.md`/`CLAUDE.md`/`GEMINI.md` plus `.grok/`/`.claude/`/`.gemini/`) with an evidence-cited provider list per candidate (empty list ⇒ UI shows "may be read", never a guess); `resolveRuleFileStates`/`ensureRuleFile`/`appendRuleEntry` take an injected `RuleFileFs` (same shape as `grok-config.ts`'s `ConfigFs`) so the fs seam stays testable. The host never parses rule content — `buildRuleAppend` only appends a dated marker block after a user's chat selection, never replacing/reordering/reformatting existing bytes |
| [src/ask-user-protocol.ts](../src/ask-user-protocol.ts) | AP-05 `ask_user` wire + tolerant schema (pure) — the tool descriptor, `normalizeAskUserArguments` (repairs a model's call rather than refusing it), the NDJSON frame codec, `checkHello` (token gate), `formatAskUserResult`, `askTimeoutMs`. Shared by the host, the shipped stdio script and the tests |
| [src/ask-user-server.ts](../src/ask-user-server.ts) | AP-05 host pipe server — one named pipe / unix socket per window, one token per live session, and the outstanding-question table. `spawnSpec(token)` builds the `AcpMcpStdioServer` the CLI spawns (address + token in **env**, never argv). Every exit path cancels, because an unanswered `tools/call` blocks the CLI for ever |
| [src/mode-prefs.ts](../src/mode-prefs.ts) | Remembered-mode policy (pure) — persist Agent/Auto-accept (never Plan), apply on new sessions only |
| [src/view-move.ts](../src/view-move.ts) | View placement (pure) — the view default-homes in the Secondary Side Bar, which Cursor refuses to create. Decides the one first-run correction into the activity-bar container, and which move mechanism applies: `vscode.moveViews` names a CONTAINER (a host may render ours anywhere), the host's own picker names a LOCATION and is the only route to a dock the host draws itself |
| [src/sessions.ts](../src/sessions.ts) | Grok disk-driven session listing/delete plus provider-bearing name overrides (pure) — `indexSessions` (stat-only ordering), `readSessionEntries` (windowed read), `listSessions` (whole-list), `clearSessions`, `discoverRepos` (the repo catalog behind the remote switcher). Codex list/load/delete stays behind `AcpClient` and is shaped/merged by `provider-ui.ts` |
| [src/file-ref.ts](../src/file-ref.ts) | Open-file ref parsing + large-file inline-read guard (pure) |
| [src/file-upload.ts](../src/file-upload.ts) | Pure remote-document upload validation, owned staging-path checks, and session/fork lifetime accounting |
| [src/plan-review.ts](../src/plan-review.ts) | Plan-snapshot Markdown filename generation (pure) |
| [src/voice.ts](../src/voice.ts) | Voice-input pure helpers — STT request/response, ffmpeg args, device parsing, key resolution |
| [src/voice-recorder.ts](../src/voice-recorder.ts) | Batch capture (`ffmpeg` → WAV) + STT REST upload |
| [src/voice-streamer.ts](../src/voice-streamer.ts) | Shared live STT transport: `PcmVoiceStreamer` accepts raw PCM from any producer; `VoiceStreamer` composes it with local ffmpeg capture. Transcripts insert at the composer selection captured on start; a manual Send/Queue discards capture and invalidates late voice callbacks, while spoken `grok send` keeps listening |
| [src/telemetry.ts](../src/telemetry.ts) | Anonymous Aptabase telemetry — pure payload builders + allowlisted `session_start` snapshot (opt-out via `grok.telemetry.enabled`; see [privacy.md](privacy.md)) |
| [src/remote-policy.ts](../src/remote-policy.ts) / [src/remote-frames.ts](../src/remote-frames.ts) / [src/remote-uplink.ts](../src/remote-uplink.ts) / [src/remote-client-state.ts](../src/remote-client-state.ts) | AFK Pilot client — exhaustive protocol policy (with `CLOUD_DISPOSITION`, a small override table applied only when the host IS a cloud environment: `logout`, `githubSignOut`, `refreshProviders`, `setTelemetryEnabled`, `setThumbsFeedback` — each host-local at a desk because the desk owner has another surface, and each meaningless to withhold where the browser is the only one), relay frames/transport, and host-owned `clientId → {cwd, active Session, browser preferences}` tab state. `bracketRemoteSnapshot` caps reconnect and cold-load history at the last ten user messages, re-bases counter-positioned cards, and sends the transcript in one additive `historyBatch` frame inside replay brackets; cold `session/load` events remain live only on the desk until the complete remote snapshot replaces them. Chat/session UI/voice traffic uses targeted `host-to` frames; this includes speech summaries, whose inbound request fields and logical-tab TTS preferences are validated before the host spends the extra xAI call, with the result returned only to the requester. The browser speaks its retained original after a bounded wait if no result returns and ignores a late result. `client-left` removes ephemeral ownership while the live pool member remains reclaimable; only device-global state uses relay broadcast |
| [src/remote-voice.ts](../src/remote-voice.ts) / [media/pcm-worklet.js](../media/pcm-worklet.js) | Remote microphone boundary — one independent producer/stream per browser client, strict PCM chunk/duration/cumulative-byte caps, bounded buffering while that client's hands-free STT reconnects, targeted partial/state messages, and browser AudioWorklet downsampling to signed PCM16 LE / 16 kHz / mono. Send-phrase completion returns `voiceSubmit` to the owning browser, which submits through the ordinary relay-metered `send` or busy-turn queue path; STT never prompts ACP directly |
| [src/keep-awake.ts](../src/keep-awake.ts) | OS wake lock held for the uplink's lifetime, so an AFK machine can't idle-suspend mid-turn — except on a hosted cloud environment, where `shouldKeepAwake`'s `cloudHost` returns false: an OS lock cannot stop a hypervisor pausing the VM (the uplink's `working` heartbeat is what holds it there) and `systemd-inhibit` only fails noisily on every wake. Pure plan builders per platform (`buildKeepAwakePlan`) + the `KeepAwake` runner; `grok.remote.keepAwake` is the opt-out. See [research/keep-awake.md](../research/keep-awake.md) |
| [src/device-login.ts](../src/device-login.ts) / [src/device-login-run.ts](../src/device-login-run.ts) / [src/github-device-login.ts](../src/github-device-login.ts) / [src/github-auth.ts](../src/github-auth.ts) | Headless agent sign-in, plus GitHub connection state. The agent plan names argv **and** flow shape (`needsCode` for Claude paste-code `auth login`; Grok/Codex device-code poll). The runner streams pipes, opens stdin only for paste-code, and confirms Claude with `claude auth status` `{ loggedIn: true }`. GitHub device login is the same runner with `gh auth login --web` then `gh auth setup-git`. `github-auth.ts` reads `gh api user --jq .login` (exit code, then the login) into a Settings row (`githubState`); `GH_TOKEN` / `GITHUB_TOKEN` on the host process is `envTokenInForce`, so a failed call with an env token set is named rather than looking disconnected. It lists repositories for the clone combobox, signs out, and stores a pasted token via `gh auth login --with-token` (stdin only — never logged, never echoed; refused up front when an env token is already in force). See [provider-login.md](provider-login.md) |
| [media/chat.{js,css}](../media/) | Webview UI |
| [media/webview-helpers.js](../media/webview-helpers.js) | Pure webview helpers (file-ref detection, relative-time, mic-button state machine, trailing send-phrase highlight, math extraction `splitMath`/`stripUnsupportedTex`, and the subagent classifier `isSubagentToolCall`/`subagentLabel`) — shared between webview and tests |
| [src/desktop/config-store.ts](../src/desktop/config-store.ts) / [src/desktop/main.ts](../src/desktop/main.ts) / [src/desktop/app-update.ts](../src/desktop/app-update.ts) | Desktop configuration/state host. Dotted settings include `grok.codexCliPath`; the file memento supplies the same provider global-state keys as VS Code, and account login/logout opens the provider CLI in the native terminal. Packaged win32/darwin auto-update uses `electron-updater` against the relay generic feed; check/download failure falls back to the GitHub notice. `electron-builder.yml` explicitly includes the pinned Codex ACP package |

## History at scale

The history dropdown is one recency-sorted view across every connected provider.
Grok's store can grow into the thousands. Its old path read and `JSON.parse`d *every*
`summary.json` on every open, then rendered every row — linear cost that stalled the
popover at scale. It now loads **one page at a time** (`SESSION_PAGE_SIZE = 100`,
newest-first), built from two pure primitives in
[src/sessions.ts](../src/sessions.ts):

- `indexSessions` does **one or two `stat`s per session dir, no reads** — it orders every id
  newest-first by `statSessionActivity`: `updates.jsonl` **mtime** (the persist file a
  `session/load` leaves alone), then `events.jsonl` for older dirs, then `summary.json`
  before a transcript exists. We sort by mtime
  *because the id is a UUIDv7 whose timestamp is creation, not last activity* — an
  id-sort would order by when the session was first opened, which is wrong.
- `readSessionEntries` reads + parses `summary.json` for **exactly the visible page's
  ids** and applies name overrides.

The combined `buildSessionsList` authorizes cwd before scheduling either
provider, so a stale remote project cannot spawn an adapter or mutate a Codex
cache. Codex history is listed from a lazily spawned adapter client without a `cwd`
argument, paginated to the terminal cursor with loop guards, then filtered by
resolved path on the host (case-insensitive on Windows). Per-project cache and
refresh keys use the same `normalizeWorkspaceFsPath` machinery, so adapter cwd
casing drift cannot create a second cache bucket on Windows. Codex and Claude rows keep a
host-owned activity clock after first discovery: opening never advances it, send
advances it optimistically, and turn end reasserts/rechecks the provider lists. The cache holds the complete
Codex listing. `provider-ui.ts` treats Grok pagination as a black box: each mixed
request consumes exactly one ordinary Grok page (including hidden-row slot
consumption and its within-page exact sort), then merges every not-yet-emitted
Codex row at-or-newer than that page's oldest visible Grok timestamp. Once Grok is
exhausted, the Codex suffix continues in pages bounded by the requested `limit`.
The wire cursor carries Grok's untouched
`nextOffset` plus a Codex `{updatedAt,id}` high-water mark; there is no combined
slot arithmetic or look-ahead. Only search may warm the full Grok catalog. Both
fresh and paged final merges collapse rows by globally
unique session id; provider, cwd, and display name are never identity keys. Cold Codex rows in
rename/delete and Pinned are proven through this adapter-backed cache rather
than Grok's disk catalog, without bypassing repository containment or live-owner
checks. Codex delete calls the
adapter capability and reports refusal rather than hiding a session locally.
Provider metadata still resolves an older Grok row with no explicit field to Grok,
but it does not permit two rows with the same global session id.

History is scoped to the **selected repo**. `discoverRepos` enumerates cwd catalogs from
`<grokHome>/sessions` (rejecting temp roots and `<grokHome>/worktrees` — a worktree is
not a checkout you choose between; the one carve-out is `trustedCwds`, the folder VS Code
actually has open, because the selection must always name a catalog row or `clearAllSessions`
silently no-ops). `postSessionsList` indexes that repo *plus* the worktrees belonging to it
(`worktreeCwdsForRepo`, pure), so a worktree session stays reachable after you leave it.
The primary workspace and remote-selected repos use the same parent match:
`sourceGitRoot` holds the CLI's *git root* rather than necessarily the opened folder, so
an opened subdirectory may sit inside that root. The reverse does not match because it
can be an independent nested checkout; missing parent metadata is also excluded rather
than granting destructive access to an ambiguous worktree catalog. The
picker itself is a remote-only affordance: in VS Code the window already *is* the
repository. And because the relay serves a client that can be newer than the installed
extension, the chip renders only once a `repos` frame has actually arrived — an older
host that never sends one gets no chip rather than a dead control.

At desktop width that picker becomes a **projects rail**, which is the same
capability-gated affordance in another shape: `#projects-rail` exists only in the
relay's page, so the element lookup is the entire gate and the VS Code webview renders
nothing new. The rail also waits for a `repos` frame before painting at all, because a host
older than v2.0.5 never sends one and an empty sidebar is worse than a plain
column — with two exemptions, both cases where there is no host version skew to
protect against: the desktop app (renderer and host ship together) and a cloud
machine (the relay provisioned it and installed the host, and says so to the
page before the client boots). Other projects' rows arrive on `repoSessions`
(answering `listRepoSessions`); where that frame never comes the rail degrades
to the selected repo's own list, and a probe that goes unanswered is re-asked
on the next reconnect rather than latched as "this host is too old". A
session's visual section is resolved in precedence order (Pinned, an expanded
Recent section, then project/archive) with one claimed-id set across the final render;
the same id therefore cannot appear in two rail groups. Click and highlight state is
also keyed by id, so two distinct sessions with the same display name remain independent.
Which
section a project sits in — Projects or Archived — is **derived in the client**, never a
stored section: `setRepoArchived` records one timestamped choice per repo in
`grok.repoArchives`, reported back on every catalog row as `archived`/`archivedAt`, and a
project counts as archived when that choice outranks its newest conversation or when
nothing has happened in it for thirty days. Activity newer than the choice simply
overrides it, which is what makes "work in an archived project and it returns" need no
bookkeeping. The age rule runs only on conversations the client actually holds — the
catalog's `updatedAt` is the session *directory's* mtime, which does not move when an
existing conversation continues, so trusting it against an older host would archive a
project in daily use. VS Code, desktop, and cloud hosts all advertise archive support
through those row fields. Archived projects remain in the catalog and use the same
trusted cwd set locally and remotely: sessions, pins, images, files, and routines stay
reachable. `normalizeArchiveChoices` retires superseded choices only when publishing
the catalog; it has no authorization role. Hide closes a folder at the local desk and
is withheld on cloud. Ordering and the VS Code repo picker ignore archive choices.

Selection and conversation ownership are **per remote browser tab**.
`RemoteClientState` maps the current opaque relay `clientId` to its normalized cwd and
active remote `Session`, while a high-entropy logical-tab token in `sessionStorage`
survives replacement relay connections. Presenting the same token atomically transfers
the mapping and marks the old socket stale; a different token cannot take it. Selecting
in tab A targets only A with a cwd-specific
snapshot; tab B can select the same cwd while retaining a different process, transcript,
session-list `activeId`, mode/chips/queue state, and voice stream. Repo history/dot
metadata can still refresh all clients viewing that cwd, but session output never fans
out by cwd. The local VS Code webview remains bound to its workspace, and every
live session created or adopted by its dashboard stays locally owned even while
backgrounded. A departed remote client's live session normally becomes ownerless and can
be reclaimed by either surface. During startup, priming or host-owned queued work keeps
the logical-tab binding across a pre-handshake `client-left`, so the same-token replacement
inherits the session and its queued prompt.

Queued remote text is metered at dequeue, not enqueue: `maybeFlushQueuedSends`
claims it with a host-issued submission id and sends `submitQueuedSend` to that browser
only after the active turn settles. The browser echoes the text and id on the ordinary
`send` path, so both relay limiters run before ACP is prompted. Reconnect snapshots
reuse the claim, and both the browser and host deduplicate its id; duplicate persisted
outbox frames therefore execute one prompt. `beginQueuedSendCommit` claims the ready
prefix and `finishQueuedSendCommit` removes it at the pre-prompt commit point, before
attachments are consumed and `session/prompt` is attempted. A quota rejection leaves a visible **Not sent**
block that can be edited or removed.

If the owning grok process exits, the host clears its pending queue and dispatch state;
queued text is not restored to the composer.

Destructive history actions follow the same ownership boundary. A delete is refused
while the target session is owned by any browser tab or by the local VS Code view, and
Clear all preserves every such session rather than only the requester's active row.
Cold `session/load` also reserves its Grok session id synchronously, before ACP can clear
and later repopulate `Session.activeSessionId`; local/remote resume, delete, and Clear all
all consult that bounded reservation. A same-token replacement joins the reservation's
in-flight operation, keeps the pending id authoritative in snapshots, and receives
completion through whichever relay client currently owns the bound `Session`; a
different logical tab remains blocked. Ownerless live pool members and unreserved cold
history remain deletable. This prevents an owner
from retaining a rendered transcript after its backing process and disk session have
been destroyed.

Loading a conversation renders only its most recent turns and keeps the earlier
prefix unrendered, so a long history opens immediately instead of building every
turn up front; scrolling to the top hydrates the next chunk in place, with plan
and permission cards partitioned to the chunk that owns their turn. Remote
clients never reach that path - `bracketRemoteSnapshot` already caps a remote
snapshot at the last ten user messages, so there is no prefix to page through.

Relay IDs change after a network reconnect, so `media/chat.js` persists both the logical
tab token and `{repoCwd, id, cwd}` in device-scoped `sessionStorage`. Its `ready` binds
the fresh relay id before re-posting `selectRepo` + `resumeSession`; the host immediately
replaces the relay's provisional pre-token snapshot with the inherited tab state.
Same-token handoff therefore works even when replacement `ready` precedes the old
`client-left`. A session owned by another logical tab/the VS Code view is refused instead
of starting a colliding process; a missing session or unavailable repository produces a
targeted error and never silently starts blank. New, Resume, and Select-repo transitions
are serialized in arrival order per tab, while Resume additionally serializes by Grok
session id. A send waiting behind a transition is dispatched through the logical-tab token,
which resolves the current relay id only after the wait; non-`ready` state access never
implicitly recreates a departed client at the workspace cwd. Turn and mid-turn control
messages remain unlocked. `client-left` for a superseded socket cannot release the
replacement's mapping.

The host (`postSessionsList` in [src/sidebar.ts](../src/sidebar.ts)) orders everything
cheaply with `indexSessions`, then drives an **mtime-keyed read cache** so a re-open /
load-more / search only re-reads entries whose `summary.json` actually changed —
steady-state opens cost ~zero reads. **Search is server-side and complete**: a query
warms the whole catalog once (cache-backed) and filters by display name across *all*
sessions, not just the loaded page. One wrinkle the disk scan can't cover on its own:
a *brand-new* session has no `summary.json` yet, so opening history the instant a
session goes live would drop the active row until grok flushes the file. The host fixes
that by synthesizing a top-pinned row from in-memory state for any live pool session not
yet on disk **and scoped to the requested repo's cwds** (first, unfiltered page only —
those ids can't appear on a later page) — a still-focused session from a *different*
repo must not leak into the list being built for the one just selected, or it masquerades
as that repo's newest/active row and the remote auto-open shim mistakes it for an
already-open match instead of resuming or starting the right session. The
webview appends pages on scroll-near-bottom and automatically advances empty or
underfilled pages until overflow/exhaustion (de-duped by id, one request per
boundary, repeated-cursor guard). A visible Load more button backs up the
automatic path, and the empty-state label is withheld while `hasMore` is true.
The search box remains debounced. An opt-in
perf simulation ([test/sessions.perf.ts](../test/sessions.perf.ts) via
`npm run test:perf`, kept out of `npm test`/CI) asserts the op counts at N=5000: first
open drops reads 5000→100 (~98%), steady-state re-open is 0 reads, search warms once
then 0. **Clear all** remains the relief valve for an overgrown store; pagination is
the steady-state fix.

## Design choices worth knowing

- **Pure modules split for testability.** Everything tagged "(pure)" above has no
  `vscode` import, no process spawn, no network — it runs under Vitest in a plain
  Node process. That's *why* the bulk of protocol behavior can be regression-
  tested without launching VS Code or the `grok` binary. See
  [TESTS.md](../TESTS.md).
- **Auto accept (YOLO) is client-side only.** A single `autoApprove` flag —
  toggling Agent ↔ Auto accept doesn't restart the CLI or even send a message.
  When the CLI raises a permission request, the extension just answers "allow
  always" automatically. **AP-07 permission rules sit in front of that grant:**
  `handlePermissionRequest` runs `decidePermission` after the plan-gate block
  and before Auto accept / the card. A floor or Deny match rejects even under
  Auto accept; an allow match answers `allow_once` (so later requests still
  reach the engine) and emits a `hostNotice` transcript line. Empty user rules
  and no floor hit is a no-op — today's Auto accept / card path is unchanged.
  Settings → Advanced lists the floor (not deletable) and every user rule;
  checked-in `.grok/permissions.json` is visible but inert until adopted.
- **Cross-platform shell selection.** `terminal-manager.ts` picks the host shell
  for the agent's `terminal/*` commands via `resolveTerminalShell`: on Windows it
  runs them under PowerShell (`pwsh.exe`→`powershell.exe`→cmd.exe) to match the
  standalone grok CLI (#46 — cmd couldn't run the user's PowerShell profile
  functions or pipelines); on POSIX it uses `$SHELL` (typically `/bin/zsh` on
  macOS) instead of `/bin/sh`, peels grok's `/bin/bash -lc` wrapper, and spawns
  `[host, '-c', script]` so the host cannot exec that wrapper (issue #140). It
  also sets **`GROK_SHELL`** in grok's spawn env (the pure `grokShellEnvValue`)
  to match that shell **on Windows**, so the agent writes the correct dialect
  instead of guessing from its own host detection (§2.9). On POSIX it is never
  set: upstream builds the model-facing `Shell:` from `$SHELL` on Unix and reads
  `GROK_SHELL` there as a path to a shell binary, so running the shell `$SHELL`
  names is itself the alignment. `$SHELL` must also be in the POSIX-grammar
  allowlist and be a regular executable file, since the script now reaches it as
  an explicit `-c` argument. `cli-locator.ts` prefers
  `HOME`/`USERPROFILE` env over `os.homedir()` so tests can override paths.
- **Reasoning effort switches live where the CLI supports it.** Changing effort no
  longer restarts the process: `client.setReasoningEffort` sends `session/set_model`
  with `_meta.reasoningEffort` when the model advertises `supportsReasoningEffort`
  (grok 0.2.101+); the client tracks the effective effort from the `model_changed`
  notification (authoritative) and carries it through model switches (gated on the
  target model's effort menu). Older CLIs, and resetting to the model default, fall
  back to the Summarize/Restart flow.
- **Streaming is rAF-coalesced.** Message and thought chunks buffer into a raw
  string and re-render at most once per animation frame — long responses stay
  smooth under fast chunk rates.
- **`available_commands_update` drives slash autocomplete.** No hardcoded command
  list; the CLI tells the extension what's available, so plugin/skill installs
  surface immediately.
- **Model switching is agent-aware.** Models belong to *agent types*
  (`grok-build`/`grok-build-plan` vs. the `cursor` agent that owns the Composer
  models). The CLI binds the agent when the process spawns and locks it after the
  first turn, so a live `session/set_model` only works
  *within* the same agent — a cross-agent switch errors
  `MODEL_SWITCH_INCOMPATIBLE_AGENT`. So `switchModel` tries the live switch and,
  on that specific error (`isIncompatibleAgentError` in
  [src/acp-dispatch.ts](../src/acp-dispatch.ts)), persists the pick to
  `grok.defaultModel` and restarts — `newSession` re-applies the model before the
  first turn, while the agent is still rebindable. No history → transparent
  restart; with history → a Summarize / Just-Restart choice. (An **effort** change,
  by contrast, no longer restarts on recent CLIs — see the live-effort bullet
  below.) A restart on an empty session (no real conversation — common when
  you flip models/effort right after opening) takes the no-prompt path **and**
  discards the abandoned grok session dir afterward, so repeated switches don't pile
  up identical empty sessions in history; the pure `carrySessionName` moves any user
  rename onto the fresh session so the chosen name survives. The same cleanup runs on
  the effort-change empty-session branch, guarded so a dead client on a session *with*
  history keeps its history.
- **Empty sessions never accumulate (#24).** Beyond the model/effort restart
  case above, *any* time you leave an empty (`hasHistory === false`)
  session — New Session or switching to another — `parkFocused` deletes its on-disk
  dir, so at most one untitled **New session** exists at a time. That departure is also
  what made **opening** a conversation slow: the deletion was announced by rebuilding
  the entire session list — a walk of every catalog in the project, so the cost grew
  with the store — and a fresh ACP adapter process was spawned purely to issue one
  `session/delete`. Both are gone. The removal goes out as a targeted `sessionRemoved`
  frame (`{id, cwd}`), delivered locally and to every remote tab (`message-cwd`
  authorization, because a tab holds pins and previews for projects it is not
  viewing), and the delete reuses the client already attached to the session being
  abandoned. The client prunes that row from its list, pins, rail selection and every
  project preview it holds, adjusting paging only for a row it had actually loaded.
  The skew to respect is therefore the **release order**: a host that sends the frame
  to a client vendored before it leaves the abandoned row on screen until the next
  list, so the relay carrying the client is promoted before the extension that sends
  it. `sweepEmptySessions`
  covers what parking cannot reach — a window closed without a prompt, a host that
  crashed — and runs on activation and after every new/opened session, in that
  session's repo. Each candidate is confirmed by reading `chat_history.jsonl`
  (`isEmptySession`): swept on **zero real user queries** in a history that
  `historyIsIntelligible` could actually parse — an unparseable file is not an empty
  conversation, and that interlock is what keeps a CLI format change from making
  every session look sweepable. Covers both today's sessions and the legacy
  primer-only ones. Live, being-loaded, renamed, pinned, worktree-bound and subagent
  sessions are excluded, as is anything newer than `SWEEP_MIN_AGE_MS` (30 min) —
  parking owns the recent ones, and another VS Code window's live sessions are
  invisible to this process. Detection is
  content-based and agent-agnostic — `extractUserQueries` counts both
  `<user_query>`-wrapped prompts and the unwrapped ones grok/composer sends for slash
  commands — so it's safe for the `grok-build` and `cursor` (composer) agents alike.
- **Generated media is path-based, not an ACP image block.** `/imagine` and
  `/imagine-video` write a file into the session dir and report its *path* as
  JSON-in-text on the completed tool result. The host parses the path, classifies
  image-vs-video by extension, and serves it to the webview via `asWebviewUri`
  (streamed from disk) so even a multi-MB video renders. A host-local
  `showInFolder` action replaces the open action for BOTH generated images and
  videos on a host that advertises the capability (the desktop app, whose media
  handler it owns), and reuses the same path authorization as `openFile`. An
  editor host keeps `openFile` — a tab is somewhere new to put the file, whereas
  the desktop already plays clips inline and enlarges images in place. See
  [research/image-generation.md](../research/image-generation.md).
  Codex image generation is detected only for a Codex-provider `kind:"other"`
  tool titled `Image generation`; its completed captured shape maps to
  `<codexHome>/generated_images/<sessionId>/<toolCallId>.png`. Both adapter ids
  must be UUID-shaped (`exec-UUID` for the tool), and the resolved plus canonical
  file path must remain under `generated_images`; failure is log-only and cannot
  reach the file-read/data-URI fallback. That trusted root feeds the same
  `postGeneratedMedia` path on VS Code, desktop, and remote, with
  the same inline rendering and surface-specific hover actions. Provider scoping
  ensures a Grok tool whose user-derived title contains the phrase is not media.
- **Math renders via vendored MathJax (SVG), extracted before HTML-escaping.** Grok
  answers with TeX (inline `\(…\)`, display `\[…\]`, `\begin{pmatrix}` matrices).
  The pure `splitMath` pulls math spans out *before* the markdown pass escapes
  HTML — so backslashes and braces survive into placeholders, mirroring the
  code-block/table extraction — and `renderMath` in `chat.js` renders each span
  with [MathJax](https://www.mathjax.org) (`media/mathjax/tex-svg-full.js`, a
  self-contained ~2.3 MB IIFE, no network) via `MathJax.tex2svg` (synchronous once
  startup resolves; raw-TeX fallback + an `upgradeMathInDom` pass until then).
  `enableAssistiveMml:false` stops a hidden MathML copy from rendering as a visible
  duplicate, and we supply `mjx-container[display="true"]{display:block}` ourselves
  since manual `tex2svg` skips MathJax's injected stylesheet. Single `$…$` is
  deliberately not a delimiter — it false-matches prose currency. *(v1.4.7 replaced
  KaTeX with MathJax, mainly so every equation is an exportable self-contained SVG.)*
- **Display math + Mermaid diagrams export to PNG/SVG.** Both end up as a
  self-contained `<svg>` in an export host (`.math-export` / `.mermaid-block`)
  carrying the source. A hover overlay (delegated `.expr-btn` handler, mirroring the
  generated-image `buildMediaActions`) offers Copy (the source), Download, and Open.
  Download quick-picks a **PNG** (canvas-rasterized with the VS Code theme
  background — WYSIWYG) or a **transparent SVG** for a dark/light background (math
  recolors `currentColor`; mermaid re-renders per theme via a `%%{init}%%`
  directive). The host (`sidebar.ts exportExpr`) runs the quick-pick + save dialog;
  Open writes the PNG to `globalStorageUri/exports/` and previews it.
- **Mermaid renders async, as a post-pass over the inserted DOM.** Grok answers
  with ` ```mermaid ` fences (flowcharts, sequence/state diagrams, git graphs, …).
  Unlike the synchronous math render, `mermaid.render` is async and needs
  the live DOM (it measures text to lay out nodes), so `renderMarkdown` only turns
  the fence into a `.mermaid-block` placeholder (carrying the source as a readable
  fallback code block) and `renderMermaidIn` in `chat.js` swaps in the SVG
  afterward via vendored [Mermaid](https://mermaid.js.org) (`media/mermaid/`, a
  self-contained ~3.3 MB IIFE, no network). The streaming agent bubble rebuilds
  its DOM every animation frame, so two source-keyed module caches make that
  flicker-free: `mermaidSvgCache` re-applies a rendered SVG synchronously on a
  cache hit, and `mermaidInFlight` stops a diagram being laid out repeatedly before
  its first render resolves. Themed to VS Code dark/light; `securityLevel:"strict"`;
  malformed/half-streamed diagrams keep the readable source. No CSP change (the lib
  has no `eval`/`new Function`; its inline styles are covered by `style-src`).
- **RTL content renders per-block, the chrome never mirrors.** `applyAutoDir`
  (chat.js) stamps `dir="auto"` on every block element `renderMarkdown` emits
  (ul/ol/li, h1–h3, td/th) after each `innerHTML` render site; loose paragraph
  text — which `renderMarkdown` emits bare with `<br>` breaks, never `<p>` — is
  covered by `unicode-bidi: plaintext` on the prose containers in chat.css
  (`.msg .body`, `.thinking-body`, `.plan-body`, `.subagent-result`,
  `.queued-text`), so each line takes its direction from its first strong
  character. Code is pinned LTR (`.code-block pre` + inline `code`:
  `direction: ltr; unicode-bidi: isolate`), list indent uses
  `padding-inline-start`, table cells `text-align: start`. The composer textarea
  and its `#input-highlight` send-phrase mirror are both `dir="auto"` with
  matching `plaintext` so the overlay stays byte-aligned per line.

## Subsystem notes

Largely the same files as § Module map above, answering a different question.
The Module map says **what a file is** in one line; this says **what is easy to
get wrong about it** — the constraint, the ordering hazard, the reason the
obvious change is unsafe. Measured 2026-08-22: of the files in both, only one
row said the same thing twice (dropped); 27 carry genuinely different content,
which is why both sections exist rather than one.

It came from `CLAUDE.md`, where 60,000 characters of it were being loaded on
every turn.

| Subject | What is easy to get wrong |
|---|---|
| Remote repo/session switching (`src/sidebar.ts`, `media/chat.js`) | `selectRemoteRepo` selects the newest session in the chosen repo or starts a new one; `remoteSessionFor` reserves desk adoption for session-less arrivals in the same repo. Host-wide fan-out (voice-config refresh, session list, dots, repo catalog) reads `cwdIfPresent` and skips a tab with no project; `cwd()` stays the request-path accessor. `historyReplay` renders `Loading conversation` and locks the browser repo switcher until replay settles |
| `src/host.ts` | Portable effectful host surface (`Host`) + value types (`Uri`, `HostWebview`, `HostContext`, disposables, editor/workspace watchers). **Does not import `vscode`**; a non-VS-Code host implements this. Paths are absolute strings; `Uri` is only for non-file schemes (diff preview) and command-arg round-trips |
| `src/vscode-host.ts` | VS Code implementation of `Host` (`createVsCodeHost`) plus `createVsCodeHostContext` / `wrapWebviewView` — the only module that maps Host methods onto `vscode.window` / `workspace` / `commands` / `env` / `workspace.fs` |
| `src/sidebar.ts` | Session/UI controller (`GrokSidebar`) — message routing, fs handlers, native diff editor opening (whole-file expansion delegated to `diff-view.ts`), `logout`, generated-media inlining (`postGeneratedMedia`), and the active-editor context chip (`refreshImplicitChip` mirrors the open file + live selection range, applies `grok.includeActiveFileByDefault` live). **Does not import `vscode`**; constructed with an injected `Host` + `HostContext` (extension entry wires the VS Code adapters). **Eye-off on that chip is a standing preference, not a per-file one** (#67): the chip is rebuilt on every file switch, so the choice is persisted to `IMPLICIT_CHIP_HIDDEN_KEY` in globalState on `toggleChip` and re-seeded by the pure `implicitChipStartsHidden(prev, remembered)` in `chips.ts` — the live chip wins when one exists (globalState writes are fire-and-forget), the store seeds a fresh webview/restart. Without it, dismissing the context was futile: the next file switch silently re-enabled it. Every attach flow (Send Selection/File, @-mention, the `+` picker, image paste) routes through `revealAndFocusComposer` → the `focusInput` host message, landing the caret in the composer so the user can type immediately (#43); `insertActiveMention` degrades a Command-Palette Send File with no active editor into the file picker instead of a silent no-op |
| `src/diff-view.ts` | Pure native-diff reconstruction (#66) — `expandDiffToWholeFile` combines Grok's region-only diff block with the current disk file, using `_meta.details[]` site lines / the pre-write echo line and explicit `replace_all` semantics to avoid phantom matches; `firstChangedLine` positions the editor on the first hunk; `MAX_DIFF_EXPAND_BYTES` bounds memory. Unreadable, stale, or oversized files fall back to the region-only diff |
| `src/acp.ts` | Provider-neutral ACP client — spawns the selected backend, manages session lifecycle, and emits the extension's established events (incl. `mediaContent` from `emitToolMedia`). `acpClientCapabilities(provider, grokVersion, versionVerified)` picks the advertised `initialize` shape: withhold `readTextFile` only for a live-verified grok >= 1.0.4 (image-aware `read_file`, #79; measured on 1.0.4, no upper cap — `research/image-read-capability-probe.cjs` re-establishes evidence on a new major); 0.2.x, 1.0.0–1.0.3, Codex, an unknown version, and a cache/unverified banner keep the delegated handshake. `getSessionInfo()` is Grok-only `_x.ai/session/info`: a control-plane meter whose -32601 result becomes `"unsupported"`; it never sends a prompt. Every response writer returns `writeLine`'s accepted/failed boolean; callers commit irreversible UI/session state only after a definite accepted write, while accepted-but-unconfirmed writes are never retried. `interject` (#52), `forkSession` (#48), `submitFeedback` (#114), and the worktree RPCs (`createWorktree`/`listWorktrees`/`applyWorktree`/`removeWorktree`, P2-8) call the **unadvertised** `_x.ai/interject` / `_x.ai/session/fork` / `_x.ai/feedback` / `_x.ai/git/worktree/*`; all return `"unsupported"` (never throw) on -32601 so an older CLI degrades instead of erroring. `submitFeedback` also treats a disabled-feedback internal_error as `"unsupported"`. `PromptResultMeta`/`PromptUsage` are **re-exported** from `acp-dispatch`, not redeclared — the duplicate silently dropped fields added on one side |
| `src/acp-backend.ts` / `src/grok-backend.ts` / `src/codex-backend.ts` / `src/claude-backend.ts` | Provider seam + implementations. Grok is identity behavior. Codex and Claude spawn the pinned `@agentclientprotocol/*-acp` entries under Node with `ELECTRON_RUN_AS_NODE=1` and the user's own CLI (`CODEX_PATH` / `CLAUDE_CODE_EXECUTABLE`). Codex `codexEffectiveModeId` is Plan when collaboration is Plan, otherwise the permission `mode`. Codex MCP tool_calls arrive titled `mcp.<server>.<tool>` with `kind:"execute"`; `normalizeCodexUpdate` remaps that kind to `"other"` so the row shows the title (#115). Claude's package has no CJS main — resolve `package.json` then `bin`. Claude lists sessions over ACP `session/list` `{ cwd }`, maps `configOptions` into the host picker, and leaves Plan enforcement in the adapter (`usesClientPlanGate: false`). Authentication happens entirely inside Anthropic's CLI (subscription or Console, depending on how the user signs in); this host never implements, proxies, holds, or forwards Claude credentials. Omitting `--hide-claude-auth` is deliberate — the flag would reject subscription accounts that already work in official Claude Code. See `research/claude-acp.md` |
| `src/codex-model-cache.ts` | Short-lived Codex connect warm-up: creates an adapter session in a temporary scratch cwd, caches its advertised models, deletes the throwaway conversation through ACP, disposes the adapter, and removes the scratch directory; every failure is log-only |
| `src/provider-ui.ts` | Pure provider presentation/state helpers — deterministic Grok-first ordering, connected-provider model aggregation (empty `modelId` is the default-model sentinel), repository path-key normalization through `normalizeWorkspaceFsPath`, adapter history shaping (`adapterActivityAt`: Codex and Claude pin to the host-observed `activeAt` — first-seen listing time as a baseline, send and turn end advance it; work outside this extension does not promote the row), `adapterEntriesEligibleForClear` so a failed history refresh cannot feed clear-all, and the Grok-page/Codex-high-water recency merge for mixed-provider history. Every final session merge collapses by globally unique session id (never cwd/provider/name), so a live row and an adapter row cannot render twice. A session's provider is immutable and is persisted in `grok.sessionMeta`; missing provider metadata means Grok for existing installs |
| `src/worktree.ts` | Pure worktree helpers (P2-8) — parse create/list/apply/remove/status payloads, path equality, `worktreeDisplayName`, multi-cwd session-index merge (`mergeSessionIndexes`/`collectSessionCwds`); see `research/worktree.md` |
| `src/rewind.ts` | Pure rewind helpers (P2-9) — parse `_x.ai/rewind/points` + `execute`, selectable targets, confirm/label formatters; see `research/rewind.md` |
| `src/feedback.ts` | Pure thumbs-feedback helpers (#114) — availability (`session/new` `_meta.feedbackEnabled` / advertised `feedback` command **and** `grok.thumbsFeedback`, default off), snake_case `x.ai/feedback` params; wire method is `_x.ai/feedback`. The host does not send `turn_number` (six synthetic User-item sources the buffer cannot reconstruct). See `research/turn-feedback.md` |
| `src/run-progress.ts` | Pure Deep Research / Workflow / Goal progress parsers (P2-10) — `isRunProgressUpdate` / `parseRunProgressUpdate` / `workflowControlCommand` from the live `_x.ai/session_notification` rail; see `research/run-progress.md` |
| `src/mcp-tool.ts` | Pure MCP IN/OUT normalizers — `isMcpMachineryRow` / `extractMcpInput` / `extractMcpOutput` / `prepareMcpToolCall`. Providers disagree on every field (`research/mcp-shapes.md`); detection reads provider metadata first, then argument-key heuristics. The host stamps `detailInput` (`null` while args are pending, `{}` for a no-argument call) and emits `commandOutput` joined by `toolCallId` so MCP rows reuse the shell IN/OUT chrome. `extractMcpOutput` renders the complete provider result (Codex text + `structuredContent`, or `error` when non-null; grok `OkayOutput` and sibling `output` variants; Claude a content-block array — non-text as JSON — or a string `rawOutput` shown verbatim; completed update only, never `_meta.claudeCode.toolResponse`). `formatStructured` pretty-prints already-parsed objects only up to `MAX_COMMAND_OUTPUT_CHARS`. grok `search_tool` is stamped `kind:"search"` so it folds into the existing explore group; Codex `mcp__<server>__startup` is emitted (never dropped) so a genuine server failure stays reachable. MCP `commandOutput` always states `agentSawCut: false` — the 100K cap is display-only |
| `src/mcp-connectors.ts` | Pure Tier-1 connector catalog (Linear, Notion, Atlassian, Canva, Stripe, Sentry, Cloudflare, Calendly, Airtable, GitHub). GitHub is `auth: "key"` (PAT via mcp-remote `--header Authorization:${AUTH_HEADER}` + `AUTH_HEADER` in env, never argv); the rest are OAuth. Figma omitted: advertises a `registration_endpoint` then answers 403 Forbidden, including with advertised `mcp:connect` scope. `TIER1_CONNECTORS` order is the inject/walk order (`hostMcpServers`); Settings display sort is `sortConnectorsForDisplay` in `media/settings.js`, not this array. Optional `oauthScope` is measured vendor data, not inferred from `scopes_supported` — Stripe is `"mcp"`; the others stay on mcp-remote's default. `mcpRemoteArgs` / `hostMcpServers` append `--static-oauth-client-metadata @<file>` when a path is supplied (port stays positional `args[1]`) and `--header` for key-auth. `summarizeConnectOutput` prefers an `*Error:` line and drops `at …` frames / `file:///` paths. `oauth-incompatible` is the OAuth DCR/client-metadata rejection kind; the same text on a key connector is `key-rejected`. Persist parse (`grok.mcpConnectors`: ids + endpoints + optional `readOnly`, never tokens — keys live in `HostSecrets` via `mcpConnectorSecretKey`; `connected` is that shared record, `keySet` is this host's secret; `hostMcpServersFor` re-reads that host's own secret whenever it refreshSyncs the record, and a missing or unreadable key skips `hostMcpServers` injection without deleting the record — Disconnect is the only removal), the dedup rule (`hostMcpServers` / `reservedConflictsConnector`), and config-path layers (`mcpConfigPaths` / `mcpConfigLayer` / `collectMcpNameLayers` / `collectMcpNameFiles`; Grok's set includes user-level `~/.claude.json` as a CLI compatibility source) so Settings → Connectors can hide project-file servers from the Grok list and Open the declaring user-level file. File-discovered and grok.com-managed names/URLs win so the same server is not sent twice. See `research/mcp-connectors.md` |
| `src/mcp-connector-auth.ts` / `src/npx-locator.ts` | One-shot `mcp-remote` spawn that opens the vendor OAuth browser (or, for key-auth, sends `--header` + `AUTH_HEADER` in env) and classifies missing npx / cancelled / timeout / port-conflict / refused / oauth-incompatible / key-rejected / failed. A scoped connector writes `{"scope":…}` to a temp file (`writeOAuthClientMetadataFile`) and Connect passes `@<path>` — inline JSON is mangled under Windows `shell: true`; `quoteSpawnArgs` double-quotes whitespace-bearing argv entries only for that shell spawn (CMD joins unquoted), never in `mcpRemoteArgs`; dispose after the child exits. `persistConnectorOAuthClientMetadata` writes the same JSON for `session/new` so grok's spawn agrees (`hostMcpServersFor`). `npxSpawnPlan` / `resolveNpx` scan PATH then the same well-known dirs as ffmpeg (darwin Homebrew/MacPorts; win32 none) so a Finder-launched desktop app finds Homebrew npx; genuine absence still classifies as `npx-missing`. npx is a `#!/usr/bin/env node` script, so on POSIX the plan also returns `env` whose PATH key (`npxChildPath`) is the resolved directory, then the well-known list, then the parent PATH bytes (duplicates kept, key casing unchanged) — Connect passes that to the child (`env: npx.env`, or `withAuthHeaderEnv` for a PAT) so `env node` and npx's `node` grandchild resolve; we do not hardcode an interpreter. Win32 gets a cloned env with PATH left alone (`NPX_WELL_KNOWN_DIRS.win32` is empty; `npx.cmd` + `shell: true`). Reports `EADDRINUSE` without changing ports or retrying; registration and `MCP_REMOTE_PACKAGE` stay pinned. Remote OAuth uses `onAuthorization` and the temporary `mcp-remote-headless.ts` preload to suppress browser launching; `mcp-oauth-redirect.ts` validates the pasted redirect and requests only the reconstructed host callback with redirects disabled. Injected spawn and callback transport; 180s ceiling (`MCP_REMOTE_CONNECT_TIMEOUT_MS`). Wire and lifecycle: `research/mcp-connectors.md` |
| `src/mcp.ts` / `src/acp.ts` | MCP settings inventory — `_x.ai/mcp/list` is read through a Grok ACP session (`findLiveGrokSession` / `grokSessionForMcpList`: a live pooled Grok client, else a lazily started empty Grok session when the provider is connected and Connectors is opened — never on boot; overlapping callers await one in-flight start (`grokSessionForMcpListInFlight`); empty-session recycling owns teardown), accepting bare arrays, `{servers:[]}`, and Grok 1.0.5's extra `{result:...}` envelope while preferring `session.*` state and preserving per-tool metadata. Parses `scopeName`. Classifies into Settings sections (`mcpIsManaged` / `filterMcpSettingsServers` / `mcpSettingsServersForCwd`) and omits project-file servers and host-injected echoes (`mcpSettingsVisible` requires a user-level config layer for Local): managed → Grok.com connectors (`scopeName` is the team name); user-level config → Local Grok connectors (`configFile` basename, desk-only). Origin tags are gone. The stored inventory (`this.mcpServers`) stays complete for dedup; `refreshMcpServers` stamps `mcpServersCwd` and `mcpSettingsServersForCwd` classifies against that workspace once, storing the global-only view (`mcpServersView`). Project-file rows never enter that view, so a second tab, remote snapshot, or newly focused workspace renders it with no further cwd comparison. `mcpNameCatalogFor` reads Grok config files for the catalog cwd (`mcpConfigPaths` with `provider: "grok"`) because `this.mcpServers` is Grok's inventory — a focused Codex/Claude session must not skip `.mcp.json` / `.grok/config.toml` / user-level `~/.claude.json`. Host-inject reserved identity (`reservedMcpIdentityFor`) still uses `session.provider`. `_x.ai/mcp/servers_updated`, `init_progress`, `mcp_initialized`, and `server_status` update `grokMcpReserved` even before a catalog read (dedup does not wait on `_x.ai/mcp/list`); they merge into the stored inventory only while no catalog is stamped or the notifying session's cwd matches `mcpServersCwd`. `-32601` degrades to an empty catalog. Remotes may view and refresh it (`listMcpServers` inbound view); live posts are device-wide (`mcpServers` in `DEVICE_GLOBAL_REMOTE_TYPES`) because the stored view is global, so a worktree-cwd read still reaches a tab selected on the parent repo. A snapshot uses `mcpServersMessage` (the stored view). `transformHostMsgForRemote` emits `projectMcpServerForRemote` (page fields only — never `command`/`args`/`url`/`tools`/`error`/`tag`/`configFile`; `scopeName` stays on the allowlist) because `mcpServers` is `allowlist`, not `mirror`. Connect/Disconnect and `completeMcpConnectorOAuth` are inbound `full`. Keys are write-only; remote OAuth sends `mcpConnectorAuthorization` only to the requesting client (`mirror`, project auth `none`). `session/new` and `session/load` send `mcpServers` from `hostMcpServers` (not a hardcoded `[]`) |
| `src/acp-dispatch.ts` | Pure protocol helpers — line parsing, update routing, response builders, generated-media extraction (`isMediaGenToolCall`/`extractGeneratedMediaPaths`), context gates (`gateZeroTokenMeta`, `contextUsedFromUpdateEnvelope`), session/info parser + cache TTL (`parseSessionInfoRpcResult`, `parseSessionInfoContext`, `sessionInfoCacheFresh`), live `_x.ai/session_notification` consumers (`contextUsedFromCompactNotification`, `autoCompactStartedNote`, `isSubagentLifecycleUpdate`), adapter occupancy (`adapterContextOccupancy`, `occupancyFromAdapterTurn`, `applyContextOccupancy`), usage accounting, turn-failure triage, and replayed-command restore (`commandOutputForToolCall` / `commandOutputFromLiveTerminal` / `capCommandOutput`). Shell `commandOutput` always states `agentSawCut: true`. `isRateLimitError` keeps limits out of auth recovery; `isAuthErrorText` gates one guarded reload+resend; `isCredentialError` alone opens sign-in; `promptErrorText` keeps entitlement failures in chat. AP-06's `classifyLimitError` wraps these rather than replacing them. |
| `src/limit-errors.ts` | **Quota / rate-limit failover (pure).** `classifyLimitError(provider, message, code?)` returns `"rate"` \| `"quota"` \| `"auth"` \| `null`. False-positive limits are worse than missed ones, so the word "rate" in ordinary prose is `null`. Rate vs quota is refined per provider from documented copy (Grok OSS `sampling/error.rs`, Anthropic `rate_limit_error` / `enforced_spend_limit_reached`, OpenAI `rate_limit_exceeded` / `insufficient_quota`, Gemini `RESOURCE_EXHAUSTED`). `failoverTargets` is other *providers*, never another model of the same account. The host posts `limitOffer` instead of a generic `agentError`; Continue rebinds the session (`keepTranscriptOnStart`) and resends only to the target, using that provider's `rememberedEffort`; every switch is a `hostNotice` line from `switchTranscriptLine`. No automatic switch. |
| `src/protocol.ts` | **Single source of truth for the host↔webview message contract.** Two discriminated unions — `HostMsg` (host→webview) + `WebviewMsg` (webview→host) — plus exhaustive `Record<Union["type"], true>` maps that compile-time-force the runtime `HOST_MESSAGE_TYPES`/`WEBVIEW_MESSAGE_TYPES` arrays to equal the unions' discriminants. `sidebar.ts` types `post`/`emit`/`Session.buffer` against `HostMsg` (the host→webview half used to be `any` — the "post one shape, handle another" gap around restore/pagination/media); the webview keeps a mirror of the type arrays in `webview-helpers.js` and `test/protocol.test.ts` asserts TS↔JS set-equality + that `chat.js` handles every host type. All type imports are `import type`, so this module carries no runtime deps and the test can load it without vscode. Typing it caught two latent mismatches (`session.currentModelId` can be undefined; `exitPlanRequest.req` carries `planPath`/`planName`) |
| `src/provider-capabilities.ts` | **Single source of truth for companion capabilities.** Matrix of 4 providers × 11 capabilities with explicit support states (`"yes" | "no" | "probe"`), probe-override functions (`effectiveGrokPlanMode`, `effectiveGrokInterjectContent`, etc.), and user-facing explanation reasons in English. Feeds `AcpClient` (`interject`, `submitFeedback`), backend client-plan gates (`usesClientPlanGate`), and is broadcast to the webview via `providerCapabilities` message for visible-disabled action buttons and tooltips. |
| `src/queued-send.ts` | Pure queued-follow-up helpers — `enqueueQueuedSend` / `chipsForQueueSend` / `queuedSendsMessage` (additive `items` + `queued`) / `takeQueuedSendsPrefix` / `dequeueQueuedSends` / `queuedFlushText` / `claimQueuedSendDispatch` / `restoreQueuedChips`. Session stores `{text, chips}[]` (SOT; `""` is image-only); combined text is the derived one-prompt flush/handshake. Image chips keep the `[Image #N]` stamped at attach — dequeue / prefix-flush / restore never compact them. Old `dequeueSend {index:0}` is the pending block; chip-aware clients use `clearQueuedSends`. The wire keeps `items: string[]` so an old webview still renders text |
| `src/session.ts` | Per-session state bag (`Session`) — one per live backend process, with `provider` fixed after the first user turn except a confirmed AP-06 limit-failover switch; the sidebar holds a *pool* of these + one `focused`; `buffer` is typed `HostMsg[]`; `queuedSends` is the host-owned pending-send list (#37) — each entry is `{text, chips}` snapshotted at queue time (`src/queued-send.ts`), flushed as one combined prompt via `buildQueuedPromptWithImages` (per-contribution tags, `[Image #N]` stamped once at attach via `allocateImageIndex` / `Session.imageIndexHighWater`, authored text copied verbatim); includes relay round-trips and background flush. `decideSessionStart` is the opportunistic-start policy (`ensure` vs `replace`): boot / client-ready / resume / `ensureClient` reuse a matching live client and refuse while a turn is in flight; intentional restarts replace. `runExclusiveHistoryLoad` / `Session.loadInFlight` make `replayLoadedHistory` join a second overlapping load instead of starting another `session/load` (cannot abort the first; a second stream would interleave and trip `emit`'s `!session.replaying` remote gate). The barrier settles with the owner's outcome (a permanently attached no-op catch keeps a reject with no waiter from becoming unhandled). `replaying` stays a boolean: any load in progress. A host that abandons a send after `userMessage` emits a generic `error` with `INTERRUPTED_SEND_TEXT` and additive `code: "interrupted-send"` (`INTERRUPTED_SEND_CODE`; not `agentError`, which would clear the replacement's busy lock). Provider sign-out parks conversation drafts in `SessionMetaOverride.queuedDraft`; `restorePersistedDraft` clears them only when a focused or attached remote composer can render the transient restore frame. A replacement also holds `Session.strandedDraft` while no provider or live tab can receive it. Draft-bearing sessions survive parking and reaping. Billing lives only in the session-id-keyed META usage ledger. |
| `src/auth-recovery.ts` | Pure auth helpers: `beginAuthRecovery` claims one expired-token reload+resend per owning session; `oauthShadowsXaiApiKey` detects `initialize._meta.defaultAuthMethodId === "cached_token"` plus `XAI_API_KEY`, which drives a once-per-install `grok logout` warning instead of inferring the condition from error prose |
| `src/session-pool.ts` | Pure reaping policy (`selectReapable`) — idle-TTL + LRU cap over the live-session pool (never focused/working/needs-you, `needsProvider`, or draft-bearing sessions) |
| `src/cli-locator.ts` / `src/cli-path.ts` / `src/cli-process.ts` | Locate and invoke `grok` cross-platform. `findCliOnPath` (`cli-path.ts`) scans PATH in-process — PATHEXT on Windows, regular executable files on POSIX — before a hidden `where` / `command -v`, and every provider's locator goes through it while keeping its own binary/shim priority. `execGrokCli` is the single path for version/update commands and shares `grokCliNeedsShell` with the ACP spawn, so Windows `.cmd`/`.bat` shims work for every invocation. `resolvePlanModeAvailability` owns the Plan version gate (retry + `grok.cliVersionCache` fallback) |
| `src/open-timing.ts` | In-memory session-open phase clock (`OpenClock`). `startSession` brackets resolve / approve-gate / dispose / prep / `grok --version` / client / spawn+init / `session/new` / `session/load` / replay posts and writes one `session open:` line to the Grok output channel. Whatever the phases do not claim prints as `other`, so **the line always accounts for its own total** — a slow open cannot read as fast. Replay is `replay(post)` — last host post, not a webview-complete signal. The clock is **owned by the caller** for opens that start with a click: `openSession` and `openRemoteSession` make it and thread it down, and `resolve` is the window before `startSession` (load reservation, workspace-switch queue, cwd resolution, the `session-meta.json` read, and the wait on the exclusive start lock). `approve-gate` is separate and almost always 0 — deciding whether a repo's forced auto-approve needs consent, including the config reads that decide it and any modal that results. It is not called `consent` because the config reads happen even when no dialog appears, and on a network filesystem that I/O would otherwise be reported as a person thinking. A re-entry (the Windows reactive downgrade) folds the whole first attempt into a named `downgrade` phase rather than into `resolve`, so a 120s initialize timeout points at the right subsystem. That window is not small — 86-131ms on the QA fixture and 225-352ms once `session-meta.json` reaches the 1.47MB a heavy real store carries — and before it was measured it fell outside the line entirely. `new` is the create path's RPC, and it is there because a reporter's log spent 2.2-4.8s in it on EVERY `events: 0` open with nothing naming it. `npm run e2e:open-timing` is the proof. No store, no telemetry. |
| `src/codex-cli-locator.ts` / `src/codex-managed-installer.ts` / `src/claude-cli-locator.ts` | Pure injected Codex discovery: `grok.codexCliPath` override, PATH (`codex`/Windows shims), the newest `openai.chatgpt-*` bundle across `~/.vscode/extensions`, `~/.cursor/extensions`, and `~/.vscode-server/extensions`, then the extension-managed copy. Claude discovery is `grok.claudeCliPath` → PATH (`claude.exe` first, then Windows shims resolved to the native exe via `resolveClaudeSpawnTarget`) → well-known user-bin locations; there is no managed Anthropic installer. The Claude SDK spawns `CLAUDE_CODE_EXECUTABLE` with `shell: false`, so a `.cmd` cannot be returned. `GrokSidebar.locateProvider` owns every sidebar lookup, including session start. The Codex managed installer pins one official release/target hash table, streams and hashes the archive before unpacking, uses the dependency-free tar reader to preserve the canonical package layout and regular-file POSIX modes under versioned global storage, and atomically publishes it; cancel/failure removes every staging artifact |
| `src/desktop/config-store.ts` / `src/desktop/main.ts` / `src/desktop/app-update.ts` / `src/desktop/paths.ts` | Desktop host configuration and lifecycle. The main `BrowserWindow` is created `show:false` and shown on `ready-to-show`; `pinAppDocumentZoom` sets `setVisualZoomLevelLimits(1,1)` + `setZoomFactor(1)` on construct and every app-document `did-finish-load` (including reload). Desktop `getHtml` bakes `has-rail desk-with-ft` plus the files shell so the first frame is the large layout (remotes still wait for `repos`; VS Code never mounts it). The first-frame rail rule is `width: var(--rail-width, 260px)` so drag/restore/coordinator updates win after boot. View-menu Zoom In/Out/Actual Size call `window.__grokFontScale` (CSS `--chat-zoom`); they are not Chromium zoom roles. The JSON config store exposes the same dotted settings (including `grok.codexCliPath`); the file memento at `globalState.json` supplies the sidebar's global state, and login/logout opens the selected provider CLI in the native terminal. **First-run project:** `ensureWorkspaceRoot` (`electron-host.ts`) restores prefs, else one-shot discovery, else `provisionDefaultProjectDir` (`paths.ts`) — `~/AFK Pilot/My First Project` (`desktopUserHomeDir`, USERPROFILE-first on Windows) or silently `userData/My First Project` when that mkdir fails. The **root** and the **first project** are separate: the root is the one directory every project goes in (`projectRoot`, which is what makes a remote-supplied name safe), and the first-run folder is a project inside it. A machine that already has `~/Grok Build` keeps it as its root forever — `LEGACY_PROJECT_ROOT_DIRNAME`; nothing is moved, because relocating somebody's work to improve a folder name would split their projects across two roots. `project-create.ts` stays pure, so the caller looks at the disk and passes `legacyRootExists`. Seeded only when there are genuinely no projects; never re-created after the user removes it (`discoverySeedCompleted`). No `git init`. An empty open set after that is user-owned: `presentEmptyProjectState` unlocks the baked "Starting" welcome (`onboarding` `no-project`) rather than hanging. `workspaceRoot()` still returns `""` when nothing is open. Packaged win32/darwin auto-update uses `electron-updater` against the relay generic feed (`desktopUpdateFeedConfig`); check/download failure falls back to the GitHub notice (`noticeIfUpdateAvailable`). `electron-builder.yml` and `.vscodeignore` pack only the adapter's own files (`package.json` / `dist` / `LICENSE`); the lockfile records its declared tree so vsce's `npm list --production` after `npm ci` succeeds, but `@openai/codex` binaries stay out of both artifacts. Feed contract: [docs/desktop-update-spec.md](docs/desktop-update-spec.md); first-run folder: [research/default-project.md](research/default-project.md) |
| `src/terminal-manager.ts` | Headless shell children for the agent's `terminal/*` ACP calls. The **shell is the host's choice** (grok delegates every command over ACP, it never runs them itself), so on Windows `resolveTerminalShell` runs them under PowerShell — `pwsh.exe`→`powershell.exe`→cmd.exe fallback — to match the standalone CLI (profile functions + pipelines that cmd couldn't run; #46); on POSIX it uses `$SHELL` when set (typically `/bin/zsh` on macOS), else `/bin/sh`. Resolution is cached per-process |
| `src/chips.ts` | File-chip CRUD (pure) + vision-input policy: `isVisionImagePath`/`isVisionMime` whitelist (png/jpe?g/gif/webp — SVG deliberately stays a *path* chip so grok can read/edit the source; BMP excluded), single `EXT_MIME` table deriving `mimeFromPath`/`extFromMime`, `MAX_VISION_IMAGE_BYTES` (20MiB, the documented xAI cap), `makeImageChip` + `allocateImageIndex` (the `[Image #N]` index is stamped once at attach from `Session.imageIndexHighWater`, which resets only when nothing is staged — composer empty and queue empty — so a plain send starts at `#1` while a shown number never moves; the composer label, the bubble chip and the prompt tag all read that stored index) + `originRelPath` for disk imports + opaque `previewId` correlation for browser-owned thumbnails |
| `src/context-chips.ts` | **Context chips beyond files (pure).** The discriminated `ContextChip` union — today's `FileChip` becomes the `{kind?: "file"}` variant, so an ABSENT `kind` still reads as a file chip and no existing producer changed. `DiagnosticsChip` / `TerminalChip` carry only what re-finds the source (`{scope, path?, severity, count}` / `{label, bytes}`) plus a `relPath` an OLD client can render without throwing. `serializeContextChip(chip, payload)` renders the fenced block per kind — diagnostics as `path:line:col  severity  message`, grouped by file, capped at `MAX_DIAGNOSTIC_ROWS` decided from the count BEFORE formatting; terminal as a `console` fence capped at `MAX_TERMINAL_CHARS` cut from the FRONT. Both land **outside** the `<vscode-context>` envelope, because `parseAttachmentContext` reads a `- <path>` line as a path and terminal output is full of them (#151); messages are collapsed to one line for the same reason. Content is never on the chip — the payload comes from `PromptBuilderDeps.contextChipPayload`, filled at send by `sidebar.ts`'s `contextChipPayloads`. `media/webview-helpers.js` carries the twins `contextChipLabel` / `contextChipTitle` / `formatChipBytes` / `parseContextBlocks`, pinned by `test/context-chips.test.ts` |
| `src/prompt-builder.ts` | Chip → prompt payload (pure): whole-file chips become a plain path list (not `@`-reads), **split by origin** into **"Attached file(s):"** (explicit intent) vs **"Currently open in the editor (for context):"** (ambient, `isImplicitChip`); selected-range chips become fenced blocks. File-path context rides a machine-readable `<vscode-context>` envelope (`CONTEXT_TAG_OPEN`/`CLOSE`) parsed back on restore. A confirmed `slashCommand` (from `matchSlashCommand`) must stay at position 0, so the envelope + snippets TRAIL the command text. `buildPromptWithImages` appends host-pre-read `{type:"image"}` blocks + trailing `[Image #N]` tags (with a do-not-Read hint); no images → byte-identical to `buildPrompt`. `buildQueuedPromptWithImages` copies each contribution's text verbatim and emits tags from the type-time indices. See `research/compact.md`, `research/vision-input.md` |
| `src/slash-filter.ts` | Slash-command autocomplete filter + `matchSlashCommand` (pure) — `filterCommands` / `updateSlash` match advertised names **and descriptions** case-insensitive: name prefix, then mid-name, then description-only, stable within each tier (#110). **Skills vs commands:** grok's `available_commands_update` stamps `_meta.scope` + `_meta.path` on skills (`isAdvertisedSkill`); builtins omit those keys. Autocomplete offers skills anywhere in the message (they load mid-prompt as text) and commands only at position 0 (they only dispatch there). The webview copy lives in `webview-helpers.js` (`filterCommands` / `isAdvertisedSkill` / `getSlashQuery` / `applySlashPick` / `highlightQueryParts` / `appendHighlightedText`); matched runs are painted as text nodes + a `.slash-hl` span, never via HTML. The dispatch gate: token shape `^\/([A-Za-z0-9][\w.:-]*)(?:\s|$)` (rejects Unix paths like `/tmp/foo`) validated against the CLI's advertised `availableCommands` (shape-only before the list arrives) — still position-0 only, for commands. Also `filterAdvertisedCommands`/`HIDDEN_SLASH_COMMANDS` — drops commands that misbehave over ACP at ingestion so they're absent from both autocomplete and dispatch: the config-mutating no-op `/always-approve` (#31) and `/context`, which renders only in the CLI's own TUI (#39; `/session-info` is the working equivalent) |
| `src/mention.ts` | The composer's `@` file popover, host half (pure) — `filterMentionFiles` ranks the index against the typed token (basename-prefix → basename-substring → path-substring → subsequence; shorter path wins in a tier), `buildExcludeGlob` merges `files.exclude`+`search.exclude` into the findFiles exclude (findFiles' default applies files.exclude ONLY — node_modules lives in search.exclude), `orderMentionIndex` (shallow-first) is the empty-`@` order. The webview half (`getMentionQuery`/`applyMentionPick` — token detection + pick rewrite) lives in `webview-helpers.js`; the host caches one findFiles snapshot per `MENTION_INDEX_TTL_MS` (`mentionFindFilesIndex` in `sidebar.ts`, concurrent keystrokes share one in-flight build) and answers `mentionQuery`→`mentionResults`. **The findFiles cap is the #69 trap:** past it VS Code returns an *arbitrary* subset, so real files vanish from `@` while loose subsequence matches still show — `clampMentionIndexLimit` reads the user-tunable `grok.mentionIndexLimit` (default `MENTION_INDEX_LIMIT` 5000, floor `MENTION_INDEX_LIMIT_MIN` 100, no ceiling; a config change drops the cache), and `mergeMentionEntries` layers the currently-open tabs (`openWorkspaceFileEntries`) over the snapshot **at read time, never into the cache** — returning the same Map instance when nothing is new — so an open file is always mentionable regardless of the cap and closing the tab stops injecting it. A pick posts `addMentionFile`, which attaches the file through the same `addDroppedFile` pipeline as drop/+, while the `@rel/path ` text stays in the composer |
| `src/grok-config.ts` | Reads grok's `config.toml` `[ui] permission_mode` (pure) — `configForcesAlwaysApprove` (project `.grok/config.toml` over global `~/.grok/config.toml`) drives the honest "Auto accept" label when always-approve is set globally (#31) |
| `src/plan-gate.ts` | Plan-mode policy (pure) — workspace-write containment, read-only command allowlist, permission/plan-file classification, `applyAgentModeToHostPlan` (grok's writable `current_mode_update` is descriptive; Claude/Codex follow the agent's mode), `isPlanReviewPermission` (Auto accept does not implement a `switch_mode` card), and `planTextFromPermissionToolCall` (Codex `rawInput.plan`, Claude ExitPlanMode content block) |
| `src/permission-rules.ts` | **AP-07 permission-rule engine (pure).** `decidePermission(rules, facts)` is the whole verdict: the built-in safety floor first (delete commands on `/` / `/usr` / `C:\Windows`, writes to `.env*` / `*.pem` / `id_rsa*` / `id_ed25519*` / `*.key` — the same names `findWorkspaceSensitiveFiles` flags), then any matching Deny, then last-match-wins among remaining rules. Empty user rules and no floor hit → `{action:"ask"}`, which the host treats as a no-op so Auto accept and the card behave as they do today. Allow answers `allow_once` so the CLI keeps asking and the floor still sees later requests. Workspace rules persist in `.grok/permissions.json` (`version: 1`); they are **inert until the file hash is adopted** (`grok.permissionRulesAdopted` in PersistedState) — a checked-in file from a foreign repo never auto-activates. Global rules are a record map (`grok.permissionRules`). `canonicalPath` / `matchPathGlob` / `isPathUnder` are the path-glob toolkit AP-08 reuses (`src/**` does not match `srcfoo`). The evaluation-order sentence `PERMISSION_RULES_ORDER_COPY` is painted in Settings → Advanced; the floor is its own non-deletable list |
| `src/plan-entries.ts` | **Structured plan checklist (pure).** `parsePlanEntries(update)` normalizes ACP's `plan` update into `{id, content, status, priority?}[]`: ids are the agent's when given and otherwise derived from index+text (stable while a step keeps its place and wording, de-duplicated on collision), unknown/missing statuses read as `pending`, contentless rows are dropped. It returns **null** when there is no `entries` ARRAY at all — which is every grok/Antigravity update, since those carry plan TEXT — and the empty list when an entries array is present but yields nothing (that retires the rail rather than leaving a stale one). There is deliberately no text parser for plan prose. `planProgress` gives `{done, total, active?}`; `media/webview-helpers.js` carries the JS twin `planEntriesProgress`, pinned to it by `test/plan-entries.test.ts` |
| `src/ask-user-protocol.ts` | **AP-05 `ask_user` protocol (pure).** The single tool the host's own MCP server exposes (decision §18.2 — every extra tool is paid for by every session in every turn), plus everything both ends of the pipe must agree on. `normalizeAskUserArguments` is deliberately **tolerant**: a missing `header` is derived on a word boundary (`deriveHeader`), missing `options` mean a free-text question, bare strings are accepted where option objects were specified, duplicate labels and a lone option are dropped, and counts past the 4/4 caps are trimmed. Only a missing `question` fails — and it returns a message that becomes an `isError` tool RESULT, never a throw, so the model can repair its call inside the same turn. (A strict schema is why the same feature failed in Kilo and OpenCode: the model stops using a tool it keeps failing to call.) `parseClientFrame` re-normalizes on arrival — the far side of the pipe is another process's JSON — and returns `undefined` for anything unusable so a stray line never kills a healthy connection. `checkHello` is the token gate; `askTimeoutMs` maps `companions.askTimeout`. `resources/mcp/ask-user-server.cjs` restates these constants because it ships as plain CJS with no build step; `test/ask-user-server.test.ts` pins the two copies together |
| `src/ask-user-server.ts` | **AP-05 host pipe server.** ACP hands MCP servers to the CLI as a stdio spec only, and the **CLI** owns that child's stdin/stdout — so the extension host cannot register itself. It ships a script instead and keeps a second channel for it: `net.createServer` on `\.\pipe\companions-ask-<uuid>` (Windows) or `<tmpdir>/companions-ask-<uuid>.sock` (POSIX), one listen per window (`listen()` is idempotent and returns `undefined` rather than throwing — a pipe that cannot bind costs question cards, never a session start). `register()` mints a 256-bit token per session; `spawnSpec(token)` returns `{name:"companions", command: process.execPath, args:[script], env:[ELECTRON_RUN_AS_NODE, address, token]}` — address and token travel in **env** because argv is world-readable and a Windows named pipe has no ACL of its own. `revoke(token)` (session restart) and `dispose()` (window reload, deactivation) both cancel every outstanding question first, as does a socket that dies mid-question (`onWithdraw`); `settle()` enforces one terminal frame per question, the same stale-card invariant the RPC path keeps |
| `src/plan-restore.ts` | Plan persist + restore decision (pure) — appendPlanEntry + decideRestoreState + `countsAsUserBubble` (the host's replay counter must count exactly what the webview bubbles, or post-restore verdict positions inflate and their cards land at the end) |
| `src/grok-primer.ts` | Legacy-only `isPrimerText()` (detects primers persisted by older builds so they stay hidden + excluded from plan positions) + `isPrimerSummary()` (matches their primer-derived titles for the empty-session sweep/title repair) |
| `src/sessions.ts` | Disk-driven session listing/delete + name overrides + **empty-session detection** (`extractUserQueries`/`classifyUserQueries`/`isEmptySession`, all pure) — reads grok's `chat_history.jsonl` to confirm a session holds no real user query. `cliSessionTitle` (pure) picks grok's own title for a history row, rejecting legacy primer-derived ones. Also `discoverRepos` (pure) — the repo catalog behind the remote switcher — and `readContextUsage` (pure) — reads a session's `signals.json` for the context-donut seed/refresh (see § ACP surfaces → `_meta.totalTokens`). `resolveGrokHome` resolves grok home the way the CLI does (`GROK_HOME` override, else USERPROFILE-first on Windows / HOME elsewhere — the CLI's Rust `home_dir()` ignores HOME on Windows); `isPathInside` is the segment-boundary check behind serve-from-disk media |
| `src/file-ref.ts` | Open-file ref parsing (`path#L<n>`, `path:line[-end|:col]` — suffixes end-anchored so drive colons survive) + `fileUriToPath` (`file://` → fs path, incl. Windows drive-letter + UNC forms; backs `refFromUri` and the drag-drop flow) + large-file inline-read guard (pure) |
| `src/file-upload.ts` | Pure remote-document upload boundary — basename sanitization, `.md`/`.txt`/`.pdf`/`.csv`/`.xlsx`/`.docx` allowlist, strict base64/size validation, owned UUID staging-path checks, and session/fork reference accounting |
| `src/plan-review.ts` | Plan-snapshot Markdown filename generation for the "open plan as editor tab" action plus the pure conversation-scoped exact-shape/canonical `isTrustedPlanReviewPath` fence |
| `src/mode-prefs.ts` | Remembered-mode policy (pure, #25) — `modeToRemember` (persist Agent/Auto-accept, never Plan) + `startsInYolo` (new-session-only application) |
| `src/view-move.ts` | View placement (pure) — the chat view is **default-homed in the Secondary Side Bar** (`viewsContainers.secondarySidebar`, hence the `engines` floor of `^1.106`; Cursor 3.x is on base 1.125). package.json contributes one extension-owned container per dock location (`grokSidebar` = secondary side bar, homes `grok.chat`; `grokPrimary` = activity bar and `grokPanel` = panel, both empty by default and existing only as chat move targets; `grokProjects` = activity bar, homes `grok.projects` and nothing else). **The rail's container is deliberately NOT a chat move target** (`PROJECTS_CONTAINER_ID`): it shipped inside `grokPrimary`, which `moveViewContainerFor("sidebar")` also hands the chat view, and one container serving both roles welds the two views together — move chat and the rail follows it, drag the rail and chat goes too. **Cursor refuses the secondary-side-bar container outright** — the chat view lands in Explorer and `workbench.view.extension.grokSidebar` never registers, which is why `grok.open` resolves its command per invocation (`revealCommandFor`) instead of hardcoding one. `viewPlacementCorrection` moves the chat to `grokPrimary` **once, on a first-ever run only** (`isFirstEverRun` over `globalState.keys()`, ignoring keys we write ourselves): nothing reports a view's location back, so after that a user who moved the chat with the editor's own Move To is indistinguishable from one who never touched it. **The gear's Move view is a single `Move view…`, shown ONLY where that container was refused**, and it opens the host's own picker via `workbench.action.moveFocusedView` *with the view id argument* (Cursor never sets the `focusedView` context for webview views, so the argument is what makes it work). Container-targeted moves (`moveViewContainerFor`) name a CONTAINER and cannot choose a dock the host draws for itself — Cursor renders `grokPanel` in the primary side bar; the picker names a LOCATION and can. That picker command does not wait for the pick, so nothing may follow it |
| `src/projects-rail.ts` | Pure VS Code projects-rail helpers — `partitionRailRepos` / `sameRepoCwd` (open folder first, rest by name); shared `RAIL_PREVIEW` (3) / `RAIL_EXPANDED` (20); `collectRecentSessions` / `RAIL_RECENT_CAP` (10) for the cross-project RECENT list |
| `media/projects-rail.{js,css}` | Self-contained primary-side-bar projects rail webview (not `chat.js` — a second chat client would double-own sessions). Section order is PINNED → RECENT → PROJECTS → PROJECT ARCHIVE; cross-group duplication is intentional (RECENT is a shortcut, not a partition — dedupe is PER GROUP only; owner 2026-08-13, after one-visual-home claiming made a session vanish from under its project), and selection remains id-keyed even when display names match. Recent is collapsed by default, Pinned stays static, the other groups and each repo fold, lists reveal 3→20, and webview state persists folds/reveals. The workspace repo is first with a Current tag; colour swatches post `setRepoColor` and paint immediately via `createPendingOverlay` (the next `repos` frame that names that cwd is the authority). Session rename uses the same overlay against the next list frame. Host mirrors catalog frames via `mirrorToProjectsRail` in `sidebar.ts` |
| `src/telemetry.ts` | Anonymous Aptabase telemetry — pure builders (`aptabaseHost`/`osNameFromPlatform`/`shouldSendTelemetry`/`sanitizeSessionStartProps`/`buildSessionStartEvent`) + a fire-and-forget `postEvent`. One `session_start` per session on the first real user message; the builder allowlists keys and drops path-like / malformed values; `host` is shape-validated (`HOST_NAME_RE`), not vocabulary-allowlisted; send-path flags come from the last `providerState` / `voiceConfigured` refresh (`lastProviderConnected`, `lastVoiceConfiguredByCwd`) plus sync session/store reads (`session.provider`, `connectedConnectorStore`, `session.worktree`, existing install id), never a rediscovery; no SDK |
| `src/remote-policy.ts` / `src/remote-frames.ts` / `src/remote-uplink.ts` / `src/remote-client-state.ts` | AFK Pilot relay client — exhaustive message policy, additive wire contract (`host-to` targets client-id lists; `client-left` releases per-client state), outbound ws transport, and pure per-tab cwd/session ownership. `parseRelayFrame` allowlists every remote filesystem selector (session ids, catalog cwd targets, mention-relative paths, and upload basenames); destructive session helpers independently require a contained direct-child session path. `media/chat.js` persists a high-entropy logical-tab token, a live page-owner marker, and the host-authoritative repo + Grok session id in `sessionStorage`; `window.__grokTabTokenReady` settles duplicate-tab collision detection before the browser wrapper's first handshake. A clean `pagehide` clears the owner marker so reload keeps its identity; copied/live owner state must answer a bounded `BroadcastChannel` probe, a positive `occupied` reply mints a fresh token and clears copied conversation state, silence on a working channel retains the identity for crash/discard recovery, and missing or throwing channel support remains conservative. A reconnect presenting the same validated token atomically inherits the old relay client's ownership before `client-left`, while a superseded socket and unrelated token cannot claim it. New/Resume/Select-repo share one per-tab transition queue, Resume additionally locks the Grok session id, and a client-ready process start joins that same tail so a following send waits; `handleSend` also awaits the per-Session start exclusive (`runExclusiveSessionStart`), and cold local/remote loads reserve that id until startup settles (bounded lease backstop); a same-token replacement joins the existing load, keeps its pending `activeId`, and receives completion through the current session owner, while resume, delete, and clear still refuse a different tab/view. A `send` arriving during a transition waits for its session to become live without blocking mid-turn controls. Remote-created sessions infer and persist worktree bindings exactly like local New. `postVoiceConfigured` skips a destination whose last posted fingerprint (`voiceConfiguredFingerprint`) matches; writers seed or invalidate. The reconnect snapshot and credential-failure `value:false` seed that cache (the auth.json watcher is not tightened — null filename still matches). A replaced local renderer drops `local` (`forgetPostedVoiceConfigured` from `resolveWebviewView` and `postInitialState`) so a fresh webview is not starved of an identical payload; `releaseRemoteClient` drops `remote:<id>`. Repo metadata may refresh every tab viewing that repo, but chat/session UI, host-operation notices, and voice target only the requesting/owning client. Local VS Code retains its independent workspace-root focus. Until a device is linked, nothing runs. `linkRemoteDevice` POSTs `buildLinkStartBody` to `/api/link/start`: the unchanged legacy `name` (`HOST (Windows 11)`) plus optional `clientLabel` / `platform` / `osLabel` for richer device rows. The uplink `hello` frame (`helloFrame`) is additive: same proto, optional `client` object from the same `relayClientMeta` mapping so already-linked devices backfill on connect. `resolveInjectedDeviceToken` (next to `RELAY_URL_ENV`) honours `GROK_RELAY_DEVICE_TOKEN` only when a development build also overrode the relay URL; a production build always returns no token. Desktop main calls `consumeInjectedDeviceToken` (read, then delete the env entry) so `buildEnv` / `{...process.env}` ACP and terminal children cannot inherit the credential |
| `src/keep-awake.ts` | OS wake lock held for exactly the uplink's lifetime (device token stored + extension host alive), so an AFK machine can't idle-suspend mid-turn — `caffeinate -i -s -w` (macOS), a `SetThreadExecutionState(ES_CONTINUOUS｜ES_SYSTEM_REQUIRED)` PowerShell (Windows), `systemd-inhibit --what=idle:sleep --mode=block` (Linux), no-op under WSL. Pure plan builders (`buildKeepAwakePlan`/`shouldKeepAwake`/`keepAwakeFallbackWhat`) + the `KeepAwake` runner; `refreshKeepAwake` in `sidebar.ts` re-asserts it on link/unlink/dispose/config change **and on device-login start/settle** — a headless sign-in counts as work (`deviceLoginInFlight`, an operation-token set) so the relay keeps a cloud machine awake while a vendor is being polled. `shouldKeepAwake({ cloudHost: true })` is always false: there the `working` heartbeat, not the OS, is what prevents a pause. **Display sleep is deliberately NOT blocked**, and every failure is log-only — never a notification, a throw, or a retry loop. `grok.remote.keepAwake` (default true) is the opt-out; see `research/keep-awake.md` |
| `src/voice.ts` | Voice-input pure helpers — STT request/response/error, per-platform ffmpeg args, DirectShow device parsing, API-key resolution. `resolveVoiceKey` order: `grok.voiceApiKey` → `GROK_VOICE_API_KEY` → `XAI_API_KEY` → `authKey` (the `grok login` token). `extractGrokAuthKey` reads that token from `~/.grok/auth.json` (#51) — xAI-issuer-only, refuses a known-expired entry (skew + numeric/ISO expiry parse), so Voice works signed-in with no separate key; disclosed in `docs/privacy.md`, setup in `docs/voice-setup.md` |
| `src/voice-recorder.ts` | Batch capture: `VoiceRecorder` (spawns `ffmpeg` → WAV, graceful `q`-stop) + `transcribeAudio` (POST to `api.x.ai/v1/stt`) + `resolveWindowsAudioDevice` |
| `src/voice-streamer.ts` / `src/remote-voice.ts` | Shared live STT: `PcmVoiceStreamer` accepts raw PCM16/16 kHz/mono bytes; local `VoiceStreamer` feeds it from ffmpeg and AFK Pilot feeds it from `media/pcm-worklet.js`. Stop waits for the `transcript.done` emitted after that stop's `audio.done` (earlier smart-turn finalizations do not satisfy it; 5-second best-transcript fallback), and browser capture waits for the worklet's ordered final-PCM/flush acknowledgement (500 ms bound) before teardown. Remote voice ownership is per client/tab (there is no repository-wide lease or fan-out); `RemotePcmIngress` buffers the bounded hands-free reconnect gap and owns duration/chunk/cumulative-byte limits. Before the host confirms STT is listening, `media/chat.js` retains up to 16 seconds / 512 KiB of PCM and flushes it in order; exhausting that bound cancels with a visible retry error instead of silently dropping the start. A `client-ready` resync cancels any host ingress for that rebuilt page before its snapshot is produced, so an uplink outage cannot restore host-only listening state. A detected send phrase returns a targeted `voiceSubmit` to the browser, whose normal `send` / busy-turn queue path crosses the relay for the same metering as typed input; the host never prompts ACP directly from STT. Remote audio is ephemeral and the xAI key stays host-side. **Dictation inserts where the caret is** (design from #72, Tarcisio Miranda, ported onto this architecture): `captureVoiceInsertion` snapshots the composer selection into `voiceBefore`/`voiceAfter` on start, so partials and the final transcript land there — or replace that selection — preserving the sentence suffix instead of always appending. The capture happens whenever a start is POSTED, deliberately NOT inside the `voiceConfigured` guard: the client is not the authority on the key, and a host that records anyway then rendered `"" + transcript` and wiped the draft. `renderVoiceInsertion` re-captures lazily as a belt, so anchoring-at-start is only observable once the caret moves between start and the first partial — which is exactly what its regression test does. A manual Send/Queue calls `stopVoiceForManualSend` (local: `voiceStop { discard:true }`; remote: the existing `stopBrowserMic` PCM path, never `SpeechRecognition`) and sets `voiceDiscarded` so an in-transit result cannot refill the composer the send just cleared; the host bumps a generation counter for the same reason. Spoken `grok send` deliberately keeps streaming |
| `src/speech-summary.ts` | VS Code-only spoken-reply summarization: `summarizeForSpeech` posts the already-cleaned spoken text to xAI Responses (`grok-4.3`, reasoning off, `store:false`, 10s timeout) using the Voice credential path; every missing-key/API/timeout failure returns the original text |
| `media/settings.{js,css}` | Shared settings surface — a view over existing gear prefs/actions plus the voice send-phrase / dictionary setters. Nav order General → Voice → Notifications → Providers → Connectors → Advanced → About (Chat is folded into General; each nav row is icon + label). Connectors is gated on `capabilities.mcpSettings` (OPT-IN: absent/false = hide the nav row; desktop and VS Code set true) and has three sections: On this computer (host-owned Tier-1 list — Connect / Disconnect on the desk and on remotes when `mcpConnectors.remoteConnect` is present; key-auth rows paste a token in-row and never render it back; remote OAuth opens a consent link and accepts the failed callback address; older hosts offer no remote controls; disconnect copy says running sessions retain their tools; `sortConnectorsForDisplay` is connected-first then name, case-insensitive; vendor marks from `media/connector-logos/<id>.webp` on those rows only, hidden until load and removed on error), Grok.com connectors (account-level, Grok only — Open grok.com in the section header; `scopeName` is the team name), and Local Grok connectors (this machine's Grok config files — header Open posts `openGlobalConfig` on the desk even when the section is empty, lucide `settings` gear (`ICON_SETTINGS`); remote gets a sentence, not a disabled button). Project-file servers are omitted. Origin tags are gone. General order is purpose → text size → coding display toggles → steer → usage stats → thumbs. Providers nav uses a bot icon; Connectors uses the plug. About carries versions, the host-gated CLI update action, Report a bug / Request a feature (GitHub tracker), contact `mailto:collinlerche@gmail.com`, and the non-affiliation disclaimer as the last element on that page only. The gear has no Version & about entry. Desktop/remote open the in-page overlay from gear → **Settings**; VS Code opens an editor-area tab (`grok.settings` + view/title gear). Overlay clients get a top-left **← Back to app** link above search (Escape-equivalent, focus returns to the opener, inside the Tab trap); the VS Code tab does not. Remote keeps host-owned settings and connection status, the device-manager link, the live Grok inventory, and the grok.com link; Advanced host-local rows stay hidden. Tier-1 connector management uses the capability gate described above. Overlay is keyboard-contained (Tab trap + sibling `inert`/`aria-hidden`, focus restored after `paint`, Escape or Back to app returns to the opener). `update()` is a no-op when the displayed snapshot/env is unchanged (`paintKey`); a host-driven change while the phone category `<select>` is open (`describeChrome.navMenuOpen`) defers `paint` until that control blurs, so a live frame cannot close the native picker. Restore defaults only resets toggles/selects/sliders (`restoreTargets` / `restoreChanges` — never send phrase or dictionary terms); a page with nothing restorable has no button, and the action first expands an in-surface confirm listing the rows that will change. Snapshot-on-open for the tab; changes post the same `set*`/`open*` messages plus `setVoiceSendPhrase` / `setVoiceKeyterms` / `setTelemetryEnabled` / `setThumbsFeedback`. General includes *Thumbs feedback to SpaceXAI* (`grok.thumbsFeedback`, default off) as a desk toggle and a remote read-only status (same split as Anonymous usage stats — the phone cannot write a privacy setting the policy rejects). Grok.com connector rows show `scopeName` when the CLI sends a team name |
| `media/chat.{js,css}` | Webview UI — chat rendering (markdown, LaTeX via MathJax, Mermaid), inline generated media, math/diagram export, session Markdown export (`exportCurrentSession` — ⋯ overflow on surfaces that ship `#session-head-actions`; VS Code gear Session item when that slot is absent → desk `openText` + `filename`, desktop Save As / VS Code untitled tab, remote `<a download>`). A cold `historyReplay` renders the last `HISTORY_WINDOW_USER_TURNS` (80) counted user bubbles (`splitHistoryWindow` in `webview-helpers.js`) and prepends earlier turns on scroll-up (`loadEarlierHistory` / `hydrateHistoryChunkWithCounters`, never unloads the live tail; each prepend takes only the deferred plan/permission cards whose turns it renders — `partitionHistoryCards`; prepend restores the anchor line in `prependHistoryNodes`). Find calls `expandHistoryAll` before searching. Live one-at-a-time turns are not windowed. Desktop View-all / proposed diffs / Read rows open the in-app preview overlay when `capabilities.previewInApp` is present (`openPreviewOverlay` — a Read fetches the file via `readProjectFile` and numbers it on the same `.tdl` gutter as a diff, with the agent's range marked; Copy / Save As / Open in file panel), history popover. RTL content: `applyAutoDir` stamps `dir="auto"` on every markdown block element, `unicode-bidi: plaintext` on prose containers covers loose paragraphs (renderMarkdown emits them bare + `<br>`, never `<p>`), code/pre pinned LTR, composer + its highlight mirror `dir="auto"` — content direction only, the chrome never mirrors. Tool calls grouped per batch + summarized by ACP **kind** (`categorize`/`summarizeTools`/`titleKind` — "Explored N items"/"Edited N files", not "Ran N commands" for reads); narration **interleaves** with its tool groups (`addToToolGroup`). Codex-styled tool/thinking rows, one lucide category icon per row (`toolIconFor`). Thinking traces **hidden by default** (`grok.showThinking`, #26) behind a `thinking-hidden` body class, with a **"Thinking…"** stand-in + `ensureActivityIndicator` (always some live-turn progress affordance); floating **scroll-to-bottom** button (#28) in `.composer` (`state.stickToBottom` is a user-gesture bit — wheel/touch/scrollbar/keys — not a 40px distance check; `stickThresholdPx` scales the near-bottom slack with line height). `scrollToBottom` / `forceScrollToBottom` / the pin MutationObserver no-op while `state.replaying` (ACP `session/load` *and* in-memory buffer rebuilds — do not treat that flag as "ACP load" elsewhere); the closing `historyReplay` frame follows the pin once so a large restore is not ~N synchronous layouts. Send/Stop (`state.busy`) is **event-sourced per session** — `agentStart` sets it, `agentEnd`/`agentError`/`exit` clear it, `clearMessages` resets it — so a focus swap/replay lands on the true state; **typed text or a staged attachment never cancels** (#37, Claude-style): while busy, Enter *and* the button click queue the message (`queueFromComposer` posts `queueSend` with chips when `capabilities.queueSendChips` — the queue is **host-owned per session**, see `session.ts` / `queued-send.ts`); a composer holding only an explicit chip is send-intent (the ambient editor chip is not). The pending message renders as **ONE user block pinned to the end of the chat** (`renderQueuedBlocks` — italic, dashed border, clock tag, plus the queued chips; Edit restores text+chips via `clearQueuedSends { restore:true }`, Remove discards) rebuilt from `queuedSends` snapshots so it **survives focus switches**, the button face tracks the composer (text or attachment → queue-send arrow, empty → square Stop, locked startup → disabled spinner with no cancel), and clicking Stop hands queued text and chips back to the composer + posts `clearQueuedSends { restore:true }` before the cancel (a halt must not auto-fire queued sends). An older host without the capability is refused with a visible error rather than queueing text alone. **Find in conversation (#99)** lives in this file (`openFind`, `window.__grokFind`) + `media/chat.css` — do not add a new `media/*.js` (the relay vendors a fixed list). See § ACP surfaces |
| `media/webview-helpers.js` | Pure webview helpers shared with tests — file-ref detection, relative-time, mic-button state machine, trailing send-phrase highlight, the `@`-mention composer half (`getMentionQuery` token detection / `applyMentionPick` pick rewrite — see `src/mention.ts`), subagent classifier (`isSubagentToolCall`/`subagentLabel`), failed-tool reason (`toolFailureText`), the restore parsers that peel `buildPrompt`'s shapes back into chips (`parseAttachmentContext`, `parseSelectionBlocks`, `parseImageTags` — each strips only the leading/trailing shapes we emit, so a literal match mid-message survives restore), session Markdown export (`exportSessionMarkdown` / `exportSessionFilename`; `replayedUserBubbleVerdict` + `truncateExportEvents` keep the log aligned with visible turns), chat pin slack (`shouldStickToBottom` / `stickThresholdPx`), `createPendingOverlay` (optimistic colour/rename: a local overlay the next frame for that entity clears), and `contextOverheadTokens` / `nextContextBreakdown` / `contextBreakdownIsCurrent` (legend remainder, bound to the session/info `used` it was measured with). Open-path window (#102): `flattenHistoryMessages` / `splitHistoryWindow` / `countHistoryReplayCounters` / `partitionHistoryCards` |
| `media/file-panel.{js,css}` | Shared project-file panel (`createFilePanel`). Title strip is a folder icon + project name at rail-row weight/case (no uppercase) plus file-type tab icons via the same `fileIcons` mapping the tree uses. `planStrip` is the pure A/B/C layout planner (named tabs → icon-only inactive → one `…` overflow chip); `stripShrinkState` (compact ≤360px with tabs, extreme ≤240px) only shrinks the project title. `.gfp-tabs` keeps a 6px padding-right gap floor (measured into `trailingWidth`). Every rendered tab keeps its file icon; the active tab always shows its X; inactive tabs never render one; the strip never scrolls. Tree mode paints the title with the selected-tab content background and no blue top accent (`gfp-title-selected`). Viewer toolbar is three primitives: `.gfp-seg` / `.gfp-seg-on` (mode segments), `.gfp-action.gfp-icon-only` (bare bar-icon actions), `.gfp-action` (text buttons); Cancel/Save/⋯ sit in `.gfp-viewer-end` (`margin-left:auto`). Both `…` menus toggle on their own button (`beginMenu` + capture closer skips that anchor). Tree-row ⋯ actions show on hover, on `:focus-visible` (the row or a descendant), and while that menu is open (`.gfp-menu-open` + `aria-expanded` on the ⋯); a mouse click does not pin them via `:focus-within`. Touch keeps them in-flow. Tree-row context menu and the ⋯ menus (`openRowMenu`) offer **Open in default app** (files only, host-local), **Reveal in \<file manager\>** (host-local, label from `ui.revealLabel`), then **Copy relative path** (listing `/` form) and **Copy path** (absolute via `joinHostPath` of the scope cwd + `relPath`, desk separator — a backslash in cwd means Windows). Remote is copy-only with no leading gap. Directories skip Open in default app and lead instead with **Refresh this folder** (`refreshFolder` — drops the child container's `data-loaded` and re-runs `toggleDirectory`, so the folder re-lists without visibly closing). Copy needs no host, so the menu is never empty. No host round-trip. **Refresh (#134).** A listing is cached for the life of the scope (`state.tree`, `children.dataset.loaded`, the `scopes` map that outlives a project switch) and nothing invalidates it, so a file the agent just wrote is invisible until somebody asks again. `refreshTree` (header `.gfp-refresh`, lucide `refresh-cw`, icon-only) asks: the root **and every expanded folder** are fetched in one `Promise.all` and the DOM is rebuilt once from that map (`renderDirectory`/`makeTreeNode` take a `listings` map and paint remembered folders already open), so the tree never collapses-and-regrows and never flashes "Loading…" — the old rows stay and dim (`.gfp-refreshing`). Open tabs, filter text and scroll survive; a failed root listing keeps the tree and appends the reason (`.gfp-refresh-error`) instead of replacing it. It shares `state.rootLoad` with the first load, so the control is disabled while either is in flight and a second press cannot start a second listing; the dim/blocked state is `state.refreshing` — **per scope**, because the panel element is shared and a refresh still running on a project you left must not freeze the one you moved to (the adapters ignore the abort signal, so that wait is ~30s on remote). Which folders to re-list comes from `expandedPaths()`, read **from the DOM** by descending only into expanded nodes — deliberately not a remembered Set: bookkeeping kept beside the tree has to be corrected everywhere the two can diverge (collapsing a parent, a folder deleted on disk, a failed listing, a listing truncated before the folder appeared) and each correction was its own defect, whereas a folder inside a collapsed parent is never *reached* and one no longer on disk is not there to *find*. Hidden in the viewer (no listing on screen; the viewer's own Reload covers the open file), which is also why it may carry the trailing group's `margin-left:auto` — it is hidden only when a tab exists for `.gfp-tabs` to grow instead. The empty state offers the same cure where the doubt is (`.gfp-status-actions` → the folder's own refresh, or the tree's at the root). Reuses the existing `list` access call: no new frame, no capability flag, no version gate. Desktop and wide-browser mounts opt in to maximize (`mount.maximize` / `#desk-ft-maximize`, lucide `maximize-2`/`minimize-2`): panel takes the content row, chat hides (`desk-ft-maximized` on body), state is not persisted, Escape restores. The control hides while the panel is an overlay (phone / below the 899 dock breakpoint), where it would fight the already-full-viewport layout. |
| `media/syntax-highlight.js` | The file panel's syntax highlighter — hand-rolled and dependency-free because a CDN highlighter cannot load under our CSP and vendoring Prism would put ~200KB in the vsix and every phone page load. Seven rulesets (c-family, `#`-comment, sql, css, markup, json/yaml/toml/ini, diff) over ~60 extensions; `languageForPath` resolves by basename then extension, returning `""` for anything with no ruleset so the caller paints plain text rather than guessing. `highlightCode` is the ONLY function turning file bytes into markup — it escapes on both paths (inside a token and between tokens), and above 256KB it returns plainly-escaped text so a phone never runs seven alternations over a 2MB file. Deliberately approximate: a miscoloured token costs nothing because none of this touches a byte that gets saved. **Rule regexes must use non-capturing groups only** — rules are merged into one alternation and identified by group index, so a stray capture silently shifts every later token type; `test/syntax-highlight.test.ts` asserts that mechanically, along with the load-bearing invariant that stripping the markup reproduces the source byte for byte. Loaded before `file-panel.js` in `getHtml()` and, on the relay, ahead of the panel there too; the desktop preview overlay also reads `GrokSyntaxHighlight` at click time (`languageForId` / `languageForPath`). **Vendored** like the rest of `media/`, so a change here needs the relay's own sync step before its clients see it |
| `scripts/install.{ps1,sh}` | Auto-detect a code-compatible CLI (`code` → `code-insiders` → `cursor` → Antigravity), build .vsix, install; optional CLI arg / `-Cli` / `CODE_CLI` targets a specific IDE; `-All`/`--all` installs into **every** detected known IDE in one run (build once). `install.ps1` rewrites `REMOTE_RELAY_URL` to the staging relay from `.env` and sets `GROK_ALLOW_STAGING_RELAY_VSIX` so that local vsix can build; `scripts/check-production-relay.mjs` (`check:relay`, on `prepackage`) refuses `npm run package` unless `REMOTE_RELAY_URL` equals `PRODUCTION_RELAY_URL` in `src/remote-frames.ts` |
| `scripts/uninstall.{ps1,sh}` | Uninstall `PawelHuryn.grok-vscode-phuryn` (same optional CLI targeting + detect chain) |

Remote reconnect and cold `session/load` history flow through the pure `bracketRemoteSnapshot`: only the last ten real user messages and everything between their boundaries cross the relay; plan/permission coordinates are filtered and re-based to that window. During a cold load, `emit` keeps buffering and posting live to the local VS Code webview but suppresses the remote leg until replay finishes; a second overlapping load on the same `Session` joins the first (`runExclusiveHistoryLoad`) so that gate cannot drop mid-replay, and observes that load's outcome. The browser then receives the retained transcript in one additive `historyBatch` frame inside `historyReplay` brackets. A client attaching mid-load waits for that completed snapshot instead of receiving a partial buffer. A remote `resumeSession` that misses the on-disk catalog while the first boot pass is still running (`runFirstBootScan`: repo catalog AND the deferred session-list, not `postRepoCatalog` alone) waits for that pass — bounded by `FIRST_BOOT_SCAN_WAIT_MS` (~8s) so a stalled boot cannot hang resumes — then retries the lookup; every resume refusal carries additive `error.resumeFailed: { id }` so the browser outbox can fail closed.

Pure modules (`acp-dispatch`, `auth-recovery`, `protocol`, `chips`, `prompt-builder`, `queued-send`, `slash-filter`, `mention`, `grok-config`, `cli-locator`, `npx-locator`, `open-timing`, `sessions`, `plan-gate`, `permission-rules`, `plan-restore`, `grok-primer`, `file-ref`, `file-upload`, `plan-review`, `mode-prefs`, `view-move`, `voice`, `session-pool`, `worktree`, `rewind`, `feedback`, `run-progress`, `mcp`, `mcp-tool`, `mcp-connectors`, `webview-helpers`) were split out specifically so protocol behavior can be unit-tested without spawning processes. (`session.ts` is a state bag plus the start-intent helpers `decideSessionStart` / `turnIsInFlight` / `runExclusiveHistoryLoad`; `protocol.ts` is pure types + two runtime arrays.)

### Session pool (Agent Dashboard)

The sidebar runs a **pool of live sessions** (one `grok agent stdio` process each) with one `focused` (the one rendered). Each session's state is a `Session` (`src/session.ts`); the sidebar holds `focused` + a `Set<Session>` `pool`. **Re-focus is lossless**: every session buffers the webview posts that built its chat (`Session.buffer`), so `focusSession` is `clearMessages` + replay of that buffer — no grok reload, no kill, even mid-turn/mid-approval. The webview (`resetForNewSession`) marks existing transcript nodes `data-pending-clear` instead of destroying them; `appendTranscriptChild` drops those nodes in the same task as the first replacement, and `flushPendingTranscriptClear` does so on the next animation frame if none arrives. `body.identity-restoring` suspends that next-frame flush — a restore is already in flight, so wait for the class to lift (`syncIdentityRestoreHold`) and flush then only if nothing replaced the nodes. While a conversation is still on screen (`welcomeHoldActive` — pending-clear or not, so a remote snapshot's `initialState`-first order cannot stamp the empty state before `clearMessages` marks the nodes) or `body.identity-restoring` is set (a cold load of a remembered conversation, when the welcome starts visible and there are no `.msg` nodes — hide it, do not stamp), the welcome stays hidden and unstamped (`pendingWelcomeReveal`) and the title, worktree flag, in-progress rename, composer focus, and the reader's pin stay put (`pendingSessionChromeReset`) — a resync must not blank the name, steal focus, re-pin to the bottom, or paint Starting / Loading conversation / Connected over a conversation that is about to reappear. Desktop launch still lands the caret once (`takeDesktopLaunchComposerFocus`: first focused window-focus or the first settled `historyReplay` of this document) so a restored conversation is ready to type into; a later resync of an already-open surface does not, and VS Code never takes this shot. A replacement drops those holds; only the empty-state flush applies them (`resetSessionChrome`, including the pin). A held resync that turns out to be a different conversation re-pins and focuses from `sessionName`. Clicking a *cold* session (no live process — reaped or pre-window) loads it from disk into a fresh member (`openSession`); a live one re-focuses instantly. Two guards keep it safe: a **per-session `gen`** (bumped only when *that* session's client is torn down, captured by handlers when wired) so a backgrounded session's in-flight events aren't judged stale when focus moves; and **session-scoped `emit(session,…)`** (buffers + forwards only when focused) vs UI-wide `post(…)` (status dots, sessions list). **Status dots.** Every history row shows a dot whose color is a *derived* value (the pure `computeDot` in `src/session-pool.ts`), not the raw `status`: **gray** at rest (idle / read / cold / loaded-from-disk all collapse to one gray — the user never learns warm-vs-cold), **blue** working, **yellow** needs-you, **green** finished-and-unread, **red** errored-and-unread. The green/red is an **unread badge**, not a live state: a turn that ends while the session is *not* focused sets a persisted `unread`/`unreadError` flag in `SessionMetaOverride` (globalState); focusing/opening the session clears it (`markRead`). So the badge **survives reaping AND reload** (it's meta-backed, decoupled from the live process) — that's the payoff: come back and the green dots are exactly the sessions with results you haven't seen. No timer/decay — a never-opened session stays green (it *is* still unread). `computeDot({liveStatus, unread, unreadError})` precedence: working → needs-you → unread(error?) → none. Dots are pushed one-at-a-time via `pushDot`→`sessionDot` (cheap, no disk read) plus a full `dots` map (`dotForId` over disk entries + pool) on each list refresh; palette in CSS — Working=`--vscode-charts-blue`, Needs-you=`--vscode-charts-yellow`, unread=`--vscode-charts-green`, error=`--vscode-errorForeground`, none/default=`--vscode-descriptionForeground`. **Reaping** is the pure `selectReapable` (`src/session-pool.ts`): an **idle TTL** (untouched ≥1h, swept every 5 min) + an **LRU cap** (~8 live; least-recently-used eligible evicted past it), but it **never** reaps the focused or a `working`/`needs-you` session (so the cap can be exceeded when everything spare is busy, by design). Reaping is silent — kill the process, recompute the dot (an unread reaped session **stays green**; a read one goes gray); re-click reloads from disk. Recency (`Session.lastActiveAt`) is stamped on create/focus/made-busy via `touch`. **CLI-update guard:** the explicit *Update Grok Build CLI* action (`updateGrokCliOnDemand`) confirms before tearing the pool down if any session is `working`/`needs-you`; the silent startup auto-update has nothing in flight by construction, so it doesn't ask. Per-workspace scope. The impure `voice-recorder`/`voice-streamer` (ffmpeg spawn + STT fetch/WebSocket) are smoke-tested manually via the `research/voice-*.cjs` probes (`voice-stt-probe`, `voice-e2e-verify`, `voice-stream-probe`, `voice-stream-verify`, `voice-cost-probe`).

**Empty-session cleanup (#24, #97).** Two mechanisms, and the split matters. `parkFocused` / `parkRemoteSession` use `hasHistory === false` to delete an untouched session's on-disk dir the moment the user leaves it, so at most one untitled **New session** exists per view. `sweepEmptySessions` covers what parking cannot reach — a window closed without a prompt, a crashed host, a remote tab that vanished — and runs on activation and after every new/opened session, scoped to that session's repo. It deletes only when `isEmptySession` confirms **zero real user queries** in `chat_history.jsonl`; that content check is authoritative regardless of `num_messages` (an agentic legacy primer turn could balloon past any gate), and `EMPTY_PRIMER_MAX_MESSAGES` now gates only the no-content title fallback. **The predicate used to require our hidden primer, which made it a silent no-op for every session created after v2.2.0 retired it** — that is #97, and the lesson is that a compatibility check keyed on a thing we stopped emitting stops being a check at all. Guards, each load-bearing: live sessions, sessions with a load reservation in flight, renamed, pinned, worktree-bound, `session_kind: "subagent"`, an unreadable (as opposed to absent) history file, and anything newer than `SWEEP_MIN_AGE_MS` (30 min). That last one is sized for the case this process cannot see: **another VS Code window's live sessions are invisible here**, so the delay — plus the fact that only a zero-user-message session is ever swept, and grok re-persists it on its next turn — is what bounds a cross-window delete. It costs nothing, because an orphan is stamped when its window opened and is already old by the next activation. `historyIsIntelligible` is the other interlock and the more important one: "no real user queries" is evidence of emptiness **only if the file could be parsed at all**, because a reader that skips what it doesn't recognize cannot tell "nothing was said" from "grok changed the format" — without it a single CLI schema change turns the sweep into a shredder that finds every session empty. `provenNonEmpty` caches the ids a repo has already cleared so repeat sweeps cost a readdir plus stats, not a re-read of every summary. The newest-`SWEEP_SCAN_LIMIT` slice is joined by every `indexSessions` entry with `hasTranscript === false` (summary-only, no `events.jsonl`) so old probe leftovers cannot hide behind 300 newer conversations. **Verify a predicate change against a real store before shipping it** (`isEmptySession` over `~/.grok`, reporting rather than deleting): the pre-ship dry run covered 1435 sessions and had to show zero candidates carrying a real title or any user query.

**Grok credential probes must not create project history.** `reprobeProviderCredentials` still calls `session/new` (that is the observation), but in a scratch cwd, then `removeSessionFromDisk` after `dispose`. Grok has no ACP `session/delete` (`AcpClient.deleteSession` always throws for that provider); a workspace-cwd probe left a summary-only "Untitled" directory the catalog listed and the CLI could not load. Codex/Claude probes already use scratch + ACP delete. `clearAllSessions` disposes ownerless live pool members in the repo **before** deleting directories (same reason as `deleteSession`) and refreshes the local rail preview.

**History row names (#96).** Precedence is `customName` → grok's own title (`cliSessionTitle`: `session_summary`, else `generated_title`, both rejected when primer-derived) → `autoName` → `Untitled (<date>)`. `maybeGenerateTitle` writes the truncated first user message to **`autoName`, never `customName`** — a name the user typed and a name we guessed are different claims, and writing the guess into the rename field made it permanent, so the CLI's own title for the same session could never surface. Sessions named before this keep what they have: an auto title already sitting in `customName` is indistinguishable from a rename, and guessing wrong there would discard names people typed. `autoName` is stored through `capAutoName` (`AUTO_NAME_MAX_CHARS` 120, whitespace-collapsed, nearby word-boundary cut) at every write and again inside `updateSessionMeta`; `PersistedState` caps every stored `autoName` on load and writes once if anything changed, so a fat disk copy cannot rebase back in. `customName` is never touched.

### History pagination

After Grok exhausts, `mergeProviderHistoryPage` continues paging the remaining Codex rows by the requested `limit` and advances the Codex `{updatedAt,id}` high-water cursor; it never emits the whole suffix in one frame.

`buildSessionsList` authorizes the requested cwd before either provider is touched, so a stale or closed remote binding cannot schedule a Codex adapter scan or read/update its cache. The webview auto-requests successive pages while an open list is empty or underfilled, stops at overflow/exhaustion (or a repeated cursor), and keeps an explicit **Load more** button; it never shows `No sessions yet` while `hasMore` is true.

The history popover loads sessions **one page at a time** (`SESSION_PAGE_SIZE = 100`, newest-first) so it stays responsive with thousands of sessions on disk. The cost of the old behavior (read + `JSON.parse` *every* `summary.json` on every open, then render every row) grew linearly and stalled the popover at scale.

**Scope — the selected repo.** `discoverRepos` (`sessions.ts`, pure) enumerates cwd catalogs under `<grokHome>/sessions`, rejecting temp roots and `<grokHome>/worktrees` (a worktree isn't a checkout you choose between). `postSessionsList` indexes the selected repo **plus the worktrees belonging to the same checkout** (`sessionCwdsForRepo` uses the selection's nearest git root when available, otherwise its exact catalog cwd, then `worktreeCwdsForRepo` requires equality with recorded `sourceGitRoot`; `mergeSessionIndexes` combines their catalogs) — this keeps a selected subdirectory and its repository's registered worktrees together without letting an independent nested checkout inherit its parent's sessions. A worktree whose parent identity cannot be established is excluded. `trustedCwds` (the workspace root) bypasses the managed-worktree exclusion so the selection always names a catalog row, or `clearAllSessions`/`selectRepo` would silently no-op. The picker is **remote-only** (`#repo-btn`; in VS Code the window already *is* the repo) and, because the relay serves a client that can outrun the installed extension, the chip appears only once a `repos` frame has arrived — an older host that never sends one shows no chip rather than a dead control. Selection and active conversation are **per browser tab**: `RemoteClientState` maps the current opaque relay client id to its cwd and `Session`; live chat/session UI/voice messages target only that id. `media/chat.js` stores `{repoCwd,id,cwd}` in device-scoped `sessionStorage`, clears it only for an explicit repo/New choice, and posts `selectRepo` + the existing `resumeSession` after each reconnect snapshot. That reconnect restore omits `resumeSession.claim`; only an explicit user action (rail/history/pinned/Continue here) sets the bit. A claim of a conversation another remote tab holds transfers the same live `Session` object (`transferRemoteResume` in `sidebar.ts`) and tells the previous holder with additive `error.code: "session-superseded"` — no cold-load, no second ACP process, and `this.focused` is left alone so a desk-visible session is co-attached. A non-claim resume of a held conversation is still refused (older clients that never set the bit behave as before). Session-bound messages from a tab with no active mapping are refused (`REMOTE_REQUIRES_BOUND_SESSION`) rather than adopted or created. The host attaches a resume to an ownerless live pool member (no duplicate process), cold-loads it only when absent, and visibly refuses a missing/deleted session. A desk cold-load reservation is not stolen; refusing the claim in that brief window is correct. **Ownership follows the live composer, not focus:** every live session created or adopted by the VS Code dashboard remains locally owned while backgrounded; a departed remote client's member becomes ownerless and reclaimable. Resume transitions serialize by Grok session id; New serializes by client id, while turn/control traffic remains unlocked. The local VS Code webview keeps its independent workspace-root `focused` session. Remote cwd-bearing actions remain gated by `allowRemoteRepoTarget`; roster reconciliation deletes departed client ownership without disposing the pooled session so a new relay id can reclaim it. Pins remain global metadata, while the selected row is per tab.

**Two pure primitives** replace the monolithic full-scan `listSessions` (which is kept only for callers that genuinely need the whole list):

- `indexSessions({fs, grokHome, cwd})` — one or two `stat`s per session dir, **no reads/parses**, returning every id ordered newest-first by `statSessionActivity`: `updates.jsonl` mtime (the persist file a `session/load` leaves alone), then `events.jsonl` for older dirs that never grew an updates log, then `summary.json` when there is no transcript yet. Opening/restoring rewrites the summary and `events.jsonl` without a turn, so those are not the preferred recency signal. We sort by mtime here *because the id is a UUIDv7 whose timestamp is creation, not last activity* — id-sort would be wrong.
- `readSessionEntries({fs, grokHome, cwd, ids, overrides})` — reads + parses `summary.json` for **exactly the given ids** (the visible page) and applies `customName` overrides. The exact `updated_at` order is re-applied within the loaded page (mtime is only an approximate sort key).

**Host orchestration** (`postSessionsList` in `sidebar.ts`): `indexSessions` orders everything cheaply; a `mtimeById` map drives an **mtime-keyed read cache** (`sessionCache`, `readEntriesCached`) so a re-open / load-more / search reads only entries whose files actually changed — the cache key is the recency clock, so a mere `session/load` (which does not touch `updates.jsonl`) is a hit and a real turn is a miss. Grok pagination remains the single-provider black box: one consumed-slot page, hidden rows included in `nextOffset`, with exact `updated_at` sorting only inside that mtime-selected page. Codex rows are cheap adapter entries and the per-project cache holds the complete listing. A combined page merges the next Grok page with every Codex row at-or-newer than its oldest visible Grok timestamp; the final Grok page drains the Codex remainder. Its cursor is `{grokOffset,codexHighWater:{updatedAt,id}}`, so no combined code interprets Grok slots or looks ahead. Codex and Claude adapter `updatedAt` is stabilized into `SessionMetaOverride.activeAt` on first discovery; only send advances that host clock optimistically, with turn end reasserting and delayed preview refreshes rechecking the final order. It posts `{type:"sessions", entries, activeId, dots, offset, total, hasMore, nextOffset, providerCursor, query}`. **Search** is server-side and complete: a non-empty `query` warms the whole Grok catalog once (cache-backed) and filters by display name across both complete provider listings. The cache is invalidated on `rename` (changes displayName without touching mtime), `delete`, and `clearAll`. **Live-session synthesis:** a brand-new session has no provider row yet, so on the **first, unfiltered page** `postSessionsList` injects a synthesized `liveSessionEntry` for any live `pool` member absent from its backing store, carrying its default/remembered provider immediately; the next refresh replaces it once the provider lists it. `hasMore` is computed before injection (load-more pages are store-only), and ids are de-duplicated globally.

**Webview** (`media/chat.js`): `offset === 0` replaces the list, `offset > 0` appends (de-duped by id); a scroll-near-bottom listener requests the next page (`hasMore`/`sessionLoading` guards keep it to one request per boundary); the search box posts a **debounced** (180ms) `listSessions` with the query; a host-driven refresh that arrives unfiltered while a search is active triggers a **sticky-search** re-request so the filtered view isn't clobbered. The "Clear all" footer shows when any non-active session exists (loaded *or* on a later page, via `total`). **Popover positioning:** `positionDropdownPopover` **right-aligns** the popover to the panel edge (`right: 6px`/`left: auto`) and grows it leftward — width-independent, no measurement. It **caps min/max width to the panel** (`min(360|280, parentWidth − 2·EDGE)`, overriding the CSS `min-width: 280px`/`max-width: 360px`) so a long session name **ellipsizes** in a narrow panel instead of overflowing. **Resize/hide:** a `window` `resize` listener re-fits it live while open; a `document` `visibilitychange` listener `closePopovers()` when the view is hidden (a resize while hidden fires no event, so close rather than reappear stale).

**Perf simulation** (`npm run test:perf`, `test/sessions.perf.ts` via `vitest.perf.config.ts`) — opt-in, out of `npm test`/CI. At N=5000 it asserts the op counts: first open drops file reads from **5000 → 100** (~98%), steady-state re-open is **0 reads** (mtime cache), search warms once then **0 reads**, with a modeled-latency projection (~3250ms → ~310ms) and a real in-memory parse-cost wall-clock (~2.7x, disk-latency-free lower bound).

## Provider Capabilities Matrix

All companion capability checks are centralized in `src/provider-capabilities.ts` — replacing scattered ad-hoc checks (`steerableProvider`, `rewindCapableProvider`, `usesClientPlanGate`, `feedbackOffered`) with a single pure source of truth.

| Capability | Grok | Codex | Claude | Gemini |
|---|---|---|---|---|
| `steer` | yes* (`probe`: `_x.ai/interject`) | no | no | no |
| `rewind` | yes (`_x.ai/rewind/*`) | no | no | no |
| `fork` | yes (`_x.ai/session/fork`) | no | no | no |
| `worktree` | yes (`_x.ai/git/worktree/*`) | no | no | no |
| `planMode` | yes* (`probe`: CLI >= 1.0.0) | yes (`collaboration_mode`) | yes (`ExitPlanMode`) | yes (`x.ai/exit_plan_mode`) |
| `clientPlanGate` | yes (host fs/terminal gate) | no (adapter-enforced) | no (adapter-enforced) | no (adapter-enforced) |
| `vision` | yes (image content blocks) | yes (image content blocks) | yes (image content blocks) | yes (image content blocks) |
| `manualCompact` | yes (`/compact` slash) | no | no | no |
| `questionRpc` | yes (`x.ai/ask_user_question`) | no | no | yes (`x.ai/ask_user_question`) |

`questionRpc` describes the NATIVE RPC only. Since AP-05 the interactive question
card is available on all four providers: the three that answer `no` above are
offered the host's own one-tool MCP server (`ask_user`) instead, which reaches the
same card over a local pipe — see § ACP surfaces implemented.

| `feedback` | yes* (`probe`: `grok.thumbsFeedback`) | no | no | no |
| `subagents` | yes (`subagentUpdate` rail) | no | no | no |

*\* Subject to runtime probe overlays (`effectiveGrokPlanMode`, `effectiveGrokInterjectContent`, user settings, etc.).*

When a capability is unsupported, action affordances (e.g. Rewind, Edit, Steer) are rendered visible-disabled with a descriptive tooltip in English explaining why the action is unavailable on the active companion.

## Agent step rail (`plan.entries`)

Claude, Codex and Gemini send ACP's structured `plan` update — a whole checklist
of `{content, status, priority}` on every change. grok and Antigravity send the
same notification carrying a plan STRING instead. Both reach
`sidebar.ts`'s `applyPlanUpdate`, which stashes the prose in
`Session.lastPlanText` exactly as it always did (the `exit_plan_mode` fallback)
and then asks `parsePlanEntries` for a list. A `null` answer ends it: no field
write, no message, no rail. **A grok plan text is never mined for steps** — a
regex over prose fakes a protocol parity that does not exist and breaks on the
next formatting change (feature-plan decision §10.5, confirmed 2026-09-07).

A real list replaces `Session.planEntries` and is emitted as `planEntries`. That
message is **replacing state**, so it is in `GrokSidebar.TRANSIENT_TYPES` (never
buffered — a buffered copy per update would replay N stale checklists on every
focus switch) **and** in `sessionUiSnapshot`, which is what puts the rail back
after a focus switch, a reload, or a remote attach. The snapshot emits it only
when a list exists; every snapshot path is preceded by `clearMessages`, which has
already emptied the rail. The list is the memory of the RUN: a finished turn does
**not** clear it. A session start/resume (`startSessionBody`) and a rewind
(`applyRewindToView` → `clearPlanEntries`, which announces the drop) do.

`media/chat.js` renders it as `#todo-rail`, a sticky card directly above the
composer: head with the title, a `done/total` counter and a collapse toggle
(remembered in `localStorage` per session id), then one row per step — tick,
spinner, or outline dot by status, with the active step highlighted. An empty
list hides the card rather than rendering an empty one, and the open
conversation's history row carries the same `done/total`. Every element access is
null-guarded, so a shell that predates the markup simply shows nothing.

## Context chips (`@problems`, `@terminal`)

The composer stages a discriminated **`ContextChip`** union (`src/context-chips.ts`):

```ts
type ContextChip =
  | ({ kind?: "file" } & FileChip)   // today's chip — an ABSENT kind IS a file chip
  | DiagnosticsChip                  // kind: "diagnostics"
  | TerminalChip;                    // kind: "terminal"
```

`FileChip` keeps its name and its shape, so every persisted draft, replayed
buffer and remote frame written before this existed still reads correctly, and
the thirty-odd producers of one were untouched. The generic chip helpers in
`chips.ts` (`removeChip`, `toggleChip`, `consumeChips`, `isImplicitChip`,
`isExplicitVisibleChip`) are typed on the structural `ChipIdentity` rather than
on `FileChip`, which is what lets them serve the union without a second
implementation.

**Content is collected at SEND, never at attach.** A chip carries only what
finds the source again plus what the label shows — `{scope, path?, severity,
count}` or `{label, bytes}`. `addContextSourceChip` probes the host facade to
decide whether a chip may exist at all (no problems, no captured output → a
message, no chip), and `contextChipPayloads` reads the source **again** in the
same tick the prompt is built, after every attachment await. Nothing travels
between the two moments: a chip staged before a build sends the problems the
build produced. A source that throws at send costs its own block and is logged —
never the turn.

Both variants also carry a `relPath`. That is not decoration: a client
predating the union renders chips with `chip.relPath.split(…)`, so its absence
would be a TypeError that takes the whole composer down. Carrying it makes an
unknown `kind` degrade to a generic chip — the additive-field rule applied to a
field the OLD side reads.

**Serialization sits OUTSIDE the `<vscode-context>` envelope**, beside the
selection snippets, and that placement is load-bearing.
`parseAttachmentContext` takes the rest of a `- <path>` line verbatim as a path,
and terminal output is full of lines that start with `- ` (#151). Each variant
emits one fenced block:

| Kind | Block | Truncation |
|---|---|---|
| diagnostics | a header line — *Problems in the workspace (2 errors, 1 warning):*, or *Problems in `src/a.ts` (…)* when the chip is scoped — over a ```` ```text ```` fence of `path:line:col  severity  message (source)` rows, grouped by file | `MAX_DIAGNOSTIC_ROWS` (200), decided from the COUNT before a row is formatted; the cut is stated in the header and as a trailing `[… N more not shown …]` |
| terminal | a header line — *Terminal output from `bash` (1234 characters):* — over a ```` ```console ```` fence | `MAX_TERMINAL_CHARS` (20 000), cut from the **front** — a build log's last screen is the one that says what went wrong — marked `[… earlier output truncated …]` |

Diagnostic messages are collapsed to one line, so a multi-line message cannot
put arbitrary text at the start of a line either. `media/webview-helpers.js`
carries `parseContextBlocks`, which peels these back out of a replayed prompt
after `parseSelectionBlocks` (the order they were written in) and re-renders
them as chips instead of an inline dump.

Later chip types (Git diff, symbols, folders, URL) follow the same four steps:
a variant with a `kind` and a `relPath`, a probe + a collector on the host
facade, one `serialize…` branch emitting a fenced block outside the envelope,
and a `parseContextBlocks` header shape.

**Host facade.** `getDiagnostics({scope, path?})` flattens
`vscode.languages.getDiagnostics` to portable `HostDiagnostic` rows (1-based,
forward-slashed, workspace-relative). `getTerminalCapture()` answers from a
rolling per-terminal buffer fed by shell integration
(`onDidStartTerminalShellExecution`) — VS Code exposes neither a terminal buffer
nor a terminal selection, and the copy-the-selection alternative would take over
the user's clipboard on every attach, so a terminal without shell integration
reports nothing and the caller says so. `revealContextSource(source)` is the
typed intent behind clicking a chip.

**Wire.** `mentionResults` gained an additive `sources` array (the virtual
`@problems` / `@terminal` rows, prefix-matched by `filterMentionSources`; the
FILE ranking is untouched and `files` still means "paths the mention catalog can
resolve"). Picking one posts `addContextChip` (`propose`, same tier as
`addMentionFile`); the `+` popover posts the same message. Clicking a staged
chip posts `openContextChipSource` (`host-local` — a remote has neither panel).

## ACP surfaces implemented

Release-blocking provider invariants: queued sign-out drafts are persisted in
`grok.sessionMeta` at capture and cleared only after successful replacement
startup; `needsProvider` and draft-bearing sessions survive every park, release,
sweep, and reaper path, including detached remote tabs. Sign-out notices are
transient desk frames, never replayable session history. Codex credential probes
use `isCodexCredentialError`; login actions poll until completion and Re-check
bypasses history freshness. Durable `recheckConnection` is host-local, while a
remote `retryProviderSession` can restart only an already-connected provider.

- `initialize` → `session/new` / `session/load` → `session/set_model` → `session/prompt` lifecycle
- **Provider seam and selection.** `grokBackend` leaves the existing lifecycle and wire shapes unchanged. `CodexBackend` and `ClaudeBackend` spawn their pinned ACP adapters, normalize into the same `AcpClient` events and model/usage/tool/permission shapes, and leave Plan enforcement in the adapter. Claude is not listed in public store copy. `provider-ui.ts` combines connected-provider models for a new session, while a resumed or running session is permanently bound to its persisted provider. Every remote snapshot mirrors the additive, view-only `providerState` frame (`id` + `connected` plus host-probed version facts when known); it enables the same multi-provider glyph/badge, picker, and About rules as the desk. When two or more providers are connected, session rows show the lab logo and hide the idle gray status badge (colored working/needs-you/unread/error badges stay). Accounts/connect/sign-out remain desk-only, and one connected provider or an old host that sends no frame retains the original dot-only presentation. Desktop unlink lives only in Settings → Account (`unlinkRemoteDevice`, native confirm); the gear never offers it. VS Code keeps the Command Palette path. `unlinkRemoteDevice` is host-local. The gear's provider rows show only when no provider is connected or one needs login; healthy connected accounts live in Settings → Providers.
- **Model switching is agent-aware.** Models belong to agent types (`grok-build`/`grok-build-plan` vs. Composer's `cursor`). The CLI locks that binding after the first turn, so a cross-agent live switch raises `MODEL_SWITCH_INCOMPATIBLE_AGENT`; `switchModel` persists the pick and restarts, with `newSession` applying the model before the first turn. Empty sessions restart transparently and the abandoned empty directory is removed; sessions with history offer Summarize/Just-Restart. `carrySessionName` preserves a user rename. Versioned IDs resolve through `resolveModelId`, and the webview recomputes the per-model context window on `modelChanged`.
- Streaming `agent_message_chunk` + `agent_thought_chunk`
- `session/update` `plan` → the step rail. Structured `entries` (Claude/Codex/Gemini) become `Session.planEntries` + the transient `planEntries` message; a plan STRING (grok/Antigravity) keeps its existing `lastPlanText` behaviour and produces no rail. See § Agent step rail.
- Sessions: list/resume via `session/load` (grok stores them at `~/.grok/sessions/<urlencoded-cwd>/<id>/`); rename/delete metadata in `context.globalState["grok.sessionMeta"]`. We never edit grok's own session files. **Per-session delete** is the trash button on each history row (`deleteSession` → `deleteSessionDir`), **including the active one**: the host disposes the live `Session` *before* touching the disk — the live CLI owns the conversation and re-persists it, which is why deleting it first simply did not stick — then re-homes every watcher that was reading it onto the next row in that project's list (`neighbourAfterDelete` over `buildSessionsList`, same home for every watcher) and mints a blank conversation only when the project is empty. An unused empty conversation in the project is adopted instead of minting a second (`findUnusedEmptySession`). A viewer of a different conversation is not moved. Still refused: a live conversation the requester is *not* the one watching. Gated on the `deleteActiveSession` host capability so an older host is never offered a control it answers with a refusal. **Clear all** (#19) is a footer button in the history popover (shown only when ≥1 non-active session exists) → `clearAllSessions` in `sidebar.ts`: a confirmed sweep of every session dir for the *selected repo* *except* the focused one, via the pure `clearSessions({fs, grokHome, cwd, exceptId})` in `sessions.ts` (best-effort — a locked dir is skipped, not thrown), then purges the removed ids' `grok.sessionMeta` overrides and tears down any backgrounded live pool members it deleted. **Scale (history pagination):** the popover loads **one page at a time** (newest-first by last activity) so it stays fast with thousands of sessions — see § History pagination. Clear-all is the relief valve for an overgrown store; the pagination is the steady-state fix. **The name is also chrome, not just a history row (3.1.0):** the focused conversation's title sits in `.top-bar` (`#session-name-chip`) in VS Code and in `#session-head-title` remotely, ellipsised with the full text as its `title`, and renames in place — pencil on hover, tap on touch (`@media (hover: none)` unhides the pencil), Enter/blur commits, Escape or an unchanged/empty value cancels. It reuses the existing `renameSession` message; only the *display* needed a new one. That new `sessionName` HostMsg exists because the webview's `state.sessions` is populated by the history popover and follows its search box — VS Code cannot read a name out of a list it has never opened. It carries the same string `sessionDisplayName` gives a history row (so the two can't disagree) and is re-sent wherever the view's conversation changes: `startSession`, `postSessionsList`, the remote snapshot, and `focusRemoteSession` — that last one because it clears the client and then builds its own targeted list instead of going through `postSessionsList`, which is a real regression an integration test now pins. The client gates the rename affordance on having *received* the message, never on a version, so an older host simply shows no pencil.
- **Provider state.** `grok.providerConnections` records explicit account connections, `grok.providerModelCache` seeds the deterministic Grok-first all-provider picker for an empty session, and `grok.projectProviderDefaults` stores the last provider/model per normalized project path. A successful adapter connect warms the model cache through a throwaway scratch session before the first real turn. VS Code globalState and the desktop file memento use these exact keys. Grok connection migration is inferred once from existing-use evidence; Codex and Claude are never connected merely because a binary is locatable. An account that is configured but answers an auth-shaped failure (`isCodexCredentialError` / `isClaudeCredentialError` for adapter probes and `isCredentialError` for generic failures — never the billing/entitlement family) from the model warm-up, the history listing, or a session start is marked `needsLogin` on the additive `providerState` frame: it stays connected (disconnecting would hide every conversation it owns for a fault one sign-in fixes), and the gear's Accounts row plus the model picker offer the connect flow instead of a sign-out or a model row nobody can select. Picker rows always carry the provider mark (`providerLogoMarkup`), even with one agent connected; a fixed **Manage providers** footer opens Settings → Providers. `modelPickerLabel` promotes Claude's versioned `description` lead so the row reads "Sonnet 5", matching Grok/Codex names that already include a generation. A last-provider sign-out mints replacements bound to no agent at all (`Session.needsProvider`) rather than to the opposite, disconnected one; reconnecting any provider adopts every one of them — local, remote and detached — and returns their held drafts. Empty sessions may switch providers through `switchModel`'s discard-and-restart path; after the first user turn the webview scopes models to `session.provider`, composer copy is provider-aware, and `providerForRequestedModel` backstops provider-blind old-client/race picks before an adapter sees them. About reads the Grok CLI version normally, probes and caches `codex --version`, and labels the pinned `codex-acp` package separately; remote About only renders these host-reported facts. `grok.codexCliPath` remains a VS Code setting and a desktop JSON-config key honored by `locateProvider`, with no desktop settings-row message plumbing.
- `initialize` uses `acpClientCapabilities`: only a live-verified grok >= 1.0.4 withholds `readTextFile` so the CLI's image-aware `read_file` runs (#79). Measured 1.0.4+ builds treat client fs as all-or-nothing, so that also stops write delegation. 0.2.x, 1.0.0–1.0.3, Codex, an unreadable version, and a cache/unverified banner keep `readTextFile: true`. Handlers remain for `fs/read_text_file`, `fs/write_text_file`, and `terminal/{create,output,wait_for_exit,kill,release}` — the write handler + plan-gate branch stay so delegated 0.2.x writes and a later CLI that honours `writeTextFile` independently still have the hook. `terminal` is a separate capability and is unaffected. Drift probe: `research/image-read-capability-probe.cjs`.
- `session/request_permission` → chat card with `allow-always` / `allow-once` / `reject-once`, diff editor preview for `kind:"edit"`. **AP-07:** `handlePermissionRequest` runs `decidePermission` after the plan-gate block and before Auto accept / the card (`applyPermissionRules`). Floor or Deny → reject + `hostNotice`; allow → `allow_once` + `hostNotice`; `ask` / empty rules → today's path. Plan-review (`switch_mode`) is never auto-decided. The card's additive `ruleSuggestions` (outside `.card-actions`) persist an allow-rule from a concrete prefix/glob. Adapter `switch_mode` reviews (Claude ExitPlanMode, Codex plan approval) are the same permission RPC — Auto accept still must not answer them — but the card is the grok plan-review chrome: `planTextFromPermissionToolCall` lifts Codex `rawInput.plan` / Claude's content-block plan, `addPlanReviewPermissionCard` renders that text with the adapter's mode options, and a card that arrived with no plan text must not collapse to "Approved". The reply stays `permissionAnswer`. **Keyboard (#68):** the CLI's option order isn't guaranteed, so `orderPermissionOptions` (webview-helpers.js) sorts approve-first/reject-last (unknown kinds rank between — never the default, never below reject) and `defaultPermissionIndex` picks the tab stop: `allow_once` only, **never `allow_always`** (a keystroke must not widen scope for the session). The card takes focus only when `shouldFocusPermissionCard` says the composer is genuinely idle — empty, not replaying, no IME composition in flight (`state.composingIME`, since a preedit buffer isn't in `input.value`). Roving tabindex + arrows move within the group; Escape returns to the composer **without answering** (never an implicit reject); and `isTypeThroughKey` redirects a printable keystroke to the composer instead of activating a button, which is what makes taking focus free. The reporter's "wait 1s after typing" timer is deliberately **not** implemented — the action a key takes must follow visible focus, never invisible timing, least of all when that action approves a command.
- `x.ai/ask_user_question` (#12) → inline question card. Renders each question's options (single question + single-select resolves on one click; otherwise pick-then-Submit; Skip → cancel) and replies `{ outcome: "accepted", answers, annotations }` (or `cancelled`) — `answers` keyed by question text → chosen label; the reply **must** carry `outcome` (an empty ACK is rejected). On answer the card collapses to the question + a green `✓ <choice>`. On resume the question replays as a `tool_call` (questions in `rawInput`) + completed `tool_call_update` (answer text); chat.js suppresses the generic tool chip and rebuilds a read-only "You answered" card from that replay. Pure builders: `makeQuestionResponse` (`acp-dispatch.ts`), `buildQuestionAnswers` (`webview-helpers.js`). Wire format: `research/ask-user-question.md`. **The free-text "Other" is ours, not the CLI's (#85):** the tool contract promises every question carries one, the CLI usually does not send it, and the first fix keyed the input on a supplied option labelled `other` — so in practice most cards had no free-text path at all. `addQuestionCard` appends the option itself when absent (case-insensitive dedupe, so a CLI that does send one never gets two) and the typed value replaces the synthetic label inside the existing `answers` map — no ACP change, so it works against whatever grok is installed. Consequence worth knowing: `hasOther` is now always true, so the Submit/Skip footer renders on single-question cards that used to resolve on one click. One click still submits; the footer is what makes a typed answer committable.
- **`ask_user` — the same question card for all four companions (AP-05).** grok raises questions over `x.ai/ask_user_question`; Claude, Codex and Gemini have no such RPC, so the host offers them a one-tool MCP server instead. ACP passes MCP servers as a **stdio spec** and the CLI spawns them, so the host cannot be the server — it ships `resources/mcp/ask-user-server.cjs`, the CLI runs it, and that script talks back over a local pipe:

  ```
  CLI ──spawn──► ask-user-server.cjs ──named pipe / unix socket──► extension host ──► question card
        stdio          (MCP JSON-RPC)         (NDJSON + token)          (webview)
  ```

  **Where it is attached.** `hostMcpServersFor(session)` (the `mcpServers` callback on `AcpClient`, resolved inside `session/new`) appends `askUserMcpServer(session)`. It returns nothing for **grok** — a second affordance for the same card would cost every grok turn the tool's tokens — and nothing when the provider already loads a server named `companions`, following the connectors' rule of skipping ours rather than shadowing theirs. Any failure there is caught: `mcpServers` is resolved during `session/new`, so a throw would surface as a session that never opens.

  **IPC protocol (NDJSON, one JSON object per line, UTF-8).** `ASK_USER_IPC_VERSION` is 1; the host refuses any other. Client → host: `{t:"hello", v, token}` (the first frame on every connection — nothing else is read until it is accepted, and a connection that stays silent for 1s is dropped), `{t:"ask", id, questions}`, `{t:"cancel", id}`. Host → client: `{t:"ready", v}`, `{t:"denied", reason}` (the child exits on this — its session is gone), `{t:"answer", id, outcome:"accepted"|"cancelled", answers?, annotations?, auto?}`. `answers` is keyed by question TEXT, exactly like the grok RPC, so `buildQuestionAnswers` and the card need no second shape.

  **Why the token.** The address and the token travel in the child's **environment**, never in argv, because a process list is readable by anything on the machine — and on Windows a named pipe has no file permissions at all, so the token is the entire access control rather than a second layer.

  **The responder abstraction is what makes this one card.** `Session.pendingQuestions` is a `Map<id, QuestionResponder>` (`{answer, cancel, abandon}`), not a set of ids. `showQuestion` / `answerQuestion` / `cancelQuestion` in `sidebar.ts` are the only places a card is raised or settled and none of them knows its transport. `abandon` is deliberately not `cancel`: on the grok path the CLI owns the request and has already settled it, so a sweep must stay silent or it writes a stale response on a live pipe; on the MCP path nothing else will ever reply, so it must cancel or the CLI blocks inside `tools/call` for the rest of its life. A third transport means a third responder and nothing else.

  **Cleanup is the correctness property.** Session restart (`revokeAskUserToken` + `dropPendingQuestions`), the child's socket dying (`onWithdraw` → `questionResolved`, card taken down), window reload and extension deactivation (`GrokSidebar.dispose` → `AskUserServer.dispose`) all cancel outstanding questions. `test/ask-user-ipc.test.ts` runs each against the real script as a child process — never a CLI binary.

  **Auto-continue (`companions.askTimeout`, default `off`).** The timer runs in the HOST, so a closed webview can neither swallow a question nor extend its deadline. While a timer is armed the card mirrors its in-progress selection back as `questionDraft`; on expiry the host sends that draft only if it is **complete** — a half-filled answer map would read to the model as a confident answer to questions the user never reached — and otherwise cancels. Either way `questionResolved {auto:true}` collapses the card as "Continued automatically", never as "You answered".

- `session/set_mode` wired; the picker exposes **Agent**, **Plan**, and **Auto accept** (the auto-approve mode — internally still the `"yolo"` mode id / `autoApprove` flag; "YOLO" survives only in the picker description). A user Plan pick commits `client.planActive` in the set-mode / `set_config_option` response hook — after a successful reply, before the next ACP line — so a same-chunk `session/request_permission` cannot still see Auto accept (`effectivePlanActive` reads that bit for every provider). Grok also uses the bit as the fs/terminal gate (`usesClientPlanGate`); Codex/Claude leave write/shell enforcement in the adapter and do not, because their Plan is native — but Codex plan review is an ordinary `request_permission` whose `allow_once` means "implement this plan", so the hook still has to suppress Auto accept. A rejected transition keeps the previous badge and gate. Plan is hard-disabled only for a verified CLI below `GROK_REQUIRED_VERSION`; an unverified probe with no matching `grok.cliVersionCache` entry stays fail-closed but clickable (`recheckable`) so a Plan pick re-probes. The CLI's non-plan mode id is `"default"` (not `"agent"`), captured as `ACT_MODE_ID` in `sidebar.ts`. **Config-forced auto-approve (#31):** grok's global `permission_mode = "always-approve"` (config.toml, set via Shift+Tab or `/always-approve`) auto-approves every session server-side and is invisible over ACP, so `newSession` reads the config (`configForcesAlwaysApprove` in `grok-config.ts`) and starts the session in "Auto accept" with a one-time notice, instead of a misleading "Agent"; the extension can't disable a global config setting per-session. **Mid-turn switching (#64):** the mode picker stays live during a running turn (webview gate is `busyLocked`, the startup window, not `busy`) — the host `setMode` already tolerated mid-turn calls (client-side `autoApprove` gate; `client.setMode` best-effort), so flipping to Auto accept takes effect immediately AND `autoApprovePendingPermissions` resolves routine tool cards already on screen (not just future requests). `isPlanReviewPermission` (`switch_mode`) is excluded from that sweep and from the incoming Auto-accept grant — Codex `implement_plan` and Claude `acceptEdits`/`default` need an explicit card verdict, including when the subsequent mode RPC fails. Approving a grok plan restores the **pre-plan** Auto accept choice synchronously in `handleExitPlan`, before the native approval response releases same-turn implementation. Cancel deliberately lands on Agent.
- **Plan mode uses native verdicts plus a client-side safety gate.** `makeExitPlanResponse` returns the CLI's typed success outcome: Approve → `approved`, Keep planning → `cancelled`, Cancel → `abandoned`. The original plan turn then continues natively: approval implements in that turn, cancellation stays in Plan and grok revises/re-asks, and abandon switches the CLI to its default mode and ends the turn without a continuation. `handleExitPlan` settles all safety state before sending the response: approval restores the remembered pre-plan Auto accept choice and lowers the gate, rejection keeps the gate raised, and abandon lowers it but deliberately lands on Agent. Approve/reject comments go through `_x.ai/interject` **before** the response (fire-and-observe, never awaited inline). Clicking a verdict collapses the plan card immediately. A successful response write emits buffered `planResolved`; a failed write does not persist the verdict or consume `Session.pendingExitPlans`, so re-focus rebuilds an actionable card without a separate pending/failure UI. A submitted comment remains owned by the live `Session.inFlightPlanComments` map only until the interject response: success retires it synchronously, while a controlled restart moves unresolved text into the ordinary queue for the replacement client. A dead process drops its ordinary queue. The map is cleared on restart and is never persisted. Their `userMessage` uses the same `steer:true` shape as `steerSend` and does **not** increment `Session.userMessageCount`, because an interjection has no rewind point. `afterHistoryEvent` is the common within-turn replay boundary on plans, permissions, and usage; it places a final approval before same-turn implementation, while `afterUserMessage`/`afterInterjection` remain compatibility and interjection boundaries for older records. Cold replay strips the CLI interjection envelope and `<user_query>` wrapper for display/copy while retaining raw text for classification. Unsupported/error falls back to the ordinary queued-send path with the exact user text. An abandon comment always joins that queue after the native response and becomes a real prompt once the abandoned turn settles, because there is no continuation step to drain an interjection. The supported native-verdict path has no verdict-time `session/cancel`; cancellation is reserved for forcing an old/unverified CLI out of an unavailable Plan turn. There is no durable plan-comment journal, bracket marker, synthetic verdict prompt/lifecycle, or plan-processing indicator. `src/plan-gate.ts` remains: `terminal/create` can still escape the CLI's own plan gate, so non-read-only terminal commands stay blocked while planning. On grok 1.x, Plan-mode file safety rests on grok's native edit refusal (writes are not delegated); the `fs/write_text_file` branch is kept for 0.2.x (still delegated) and for a later CLI that honours `writeTextFile` independently. Plan review is fed by `req.plan` (`exit_plan_mode.planContent`); the plan.md snoop is a fallback. **Legacy reads remain load-bearing:** `isPrimerText()` / `isPrimerSummary()`, the media-side `PRIMER_PATTERN`, empty-session sweep, title repair, and prompt-index compensation keep old on-disk sessions usable. `suppressContent` / `SUPPRESS_TYPES` also remain for non-primer hidden maintenance turns.
- `grok.defaultEffort` → forwarded as `--reasoning-effort <value>` **before** the `stdio` subcommand (it's an agent-level flag; after `stdio` the CLI errors "unexpected argument"). Offered values are grok's accepted set `none|minimal|low|medium|high|xhigh` (`max` is rejected with exit code 2, so it's not offered — #3/#4). Args are built by the pure `buildGrokAgentArgs()`. **Changing effort applies LIVE when the CLI supports it** — the current model advertising `models[]._meta.supportsReasoningEffort` (grok 0.2.101+) gates `client.setReasoningEffort` → `session/set_model` with `_meta.reasoningEffort`, so `setEffort` (`sidebar.ts`) changes effort on the running session with no restart. Falls back to the restart path (`pickRestartMode`/`restartSession`) when the CLI lacks the capability, when the live switch is rejected, or when unsetting back to the model default (`""` can't be expressed as an override). On Claude/Codex, `AcpClient.newSession` applies the spawn effort via `session/set_config_option` after `session/new` (spawn flags are grok-only).
- `grok.chatFontScale` (#14, webview-only — **not** ACP) → local VS Code chat-panel zoom as a percent (default 100, clamped 60–300). The host bakes the initial value into `<body style="--chat-zoom:…">` in `getHtml` (no flash) and `postFontScale` re-posts a `fontScale` message on `onDidChangeConfiguration` for a live, reload-free update. `applyChatZoom` uses that host value only in the local webview and runs before boot `input.focus({preventScroll:true})`; AFK Pilot uses only its browser-local `grok.remote.fontScale` slider (80–160%), so desktop zoom changes never multiply remote zoom. Desktop View-menu zoom and Cmd+=/wheel share `setClientFontScale` / `__grokFontScale`; Chromium `zoomFactor` stays 1. Body height is `calc(100% / var(--chat-zoom, 1))` (html `height:100%; overflow:hidden`, not `100vh`). `zoom` scales the whole chat — text, icons, spacing — but not the rest of VS Code (the point vs. global `Ctrl/Cmd+Shift+=`). Supports User/Workspace scope for free. `chatFontScale()` in `sidebar.ts` reads + clamps. The **scroll-to-bottom button** (#28) lives inside `.composer` (`position:absolute`), so it rides this same zoom scale instead of fixed viewport pixels. Desktop chrome wrap / file-tree inject call `__grokResetDocumentScroll` (desktop-only) so a wrap cannot leave `documentElement` scrolled.
- `grok.showThinking` (#26, webview-only — **not** ACP) → whether grok's reasoning traces are shown; **default false**. The host bakes a `thinking-hidden` body class into `getHtml` (no flash) + posts `showThinking` on init and on `onDidChangeConfiguration`; chat.js's `applyThinkingVisibility` toggles the class (`body.thinking-hidden .msg.thinking { display:none }` — works on replayed/old sessions, instant flip). When hidden, the pure-CSS `blink-dots` **"Thinking…"** stand-in (brain icon) shows while grok reasons. A **continuous-indicator guarantee** (`ensureActivityIndicator`/`turnHasVisibleActivity`, called after each `TURN_PROGRESS_MSGS` event) keeps **the provider activity verb (Grokking / Opening AI / Clauding) / a running tool / Thinking…** on screen for the whole live turn — never a dead frame (skips the locked priming window + replay). Toggle in Settings → **General** (a `.settings-switch`).
- `grok.soundNotifications` (#59, webview-only — **not** ACP) → an optional short tone on turn completion/error, **default false**. Synth tones via Web Audio in `chat.js` (`playNotificationTone` — rising two-note chime for `done`, lower falling tone for `error`; no bundled assets, CSP-safe, offline). `unlockAudio` creates the `AudioContext` only while `soundNotifications` or `processingSound` is on (so a later settings flip still unlocks on the next gesture) and suspends it again after the handshake and after the last note — a running-but-silent context holds the OS audio session and blocks sleep (#107). `maybeNotifySound` gates on the setting **and** the panel being unfocused (`document.visibilityState === "hidden" || !document.hasFocus()`) — the "notify me when I've stepped away" case, not a beep on every turn you're watching. The `agentEnd`/`agentError` handlers additionally skip replayed events, so reconnect snapshot history cannot notify. `agentEnd` is in `SUPPRESS_TYPES`, so the hidden priming turn can't beep. Wired the `steerByDefault` way (host posts `soundNotifications` on init + `onDidChangeConfiguration`; gear `.popover-switch` posts `setSoundNotifications`).
- `grok.readRepliesAloud` (webview-only — **not** ACP) → local VS Code speech synthesis for the latest completed reply and live question/permission waits, **default false** and independent from AFK Pilot's browser-local `grok.remote.tts` / `window.grokRemoteTts` preference (also **default false**). `spokenTextFromMarkdown` removes fenced code before either direct speech or summarization: VS Code uses `grok.summarizeRepliesAloud`, while AFK Pilot keeps `grok.remote.ttsSummary` per browser and reports the whole preference bundle through `remotePreferences`; `RemoteClientState` owns it per logical tab, so switching conversations cannot reset it. Both summary switches are forced off when their matching read-aloud preference is off. The opt-in sends cleaned text host-side through `summarizeForSpeech`, never changes the visible/model reply, and falls back to the original text on host failure or when no `speechSummary` arrives before the browser deadline; a remote result is targeted only to its requesting logical tab, and a late result cannot speak twice. Permission speech deliberately omits tool details; question speech uses the question text. Cold loads, warm focus-buffer replays, and remote snapshots all use nest-safe `historyReplay` brackets so completed history stays silent; an in-flight replay seeds only its live prefix for one eventual completion.
- `grok.defaultMode` (#25, webview-only — **not** ACP) → the remembered last non-plan mode (`"" | "agent" | "yolo"`), applied to **new** sessions only (resumed ones keep their verdict-driven plan/agent state). `setMode` persists it on every user switch except Plan (a transient per-task choice); the new-session path flips `autoApprove` when it's `"yolo"`. Mirrors how `defaultModel`/`defaultEffort` already persist.
- **Telemetry** (`grok.telemetry.enabled`, default true; not ACP). One anonymous Aptabase `session_start` fires from the first real-send path, so empty/abandoned sessions never report. It carries install/mode/model/effort, `hostKind` (desktop vs vscode), app purpose, disclosed UI preferences, voice-input-available/streaming/language-set booleans, grok/codex/claude connected, the selected CLI (`provider`), connector count, worktree, returning-install (`returningInstall`), origin/device, and a shape-validated host app name—never prompts, code, paths, or free-text settings. `reportSessionStart` reads `lastProviderConnected` / `lastVoiceConfiguredByCwd` from the last refresh (never `locatedProviders` / `resolveVoiceApiKey` on send) and `session.provider` / `connectedConnectorStore()` / `session.worktree` / the existing install id as sync fields. VS Code telemetry, extension telemetry, and official-extension identity must all allow it. The desktop asar carries `grokExtensionName` so rewriting `package.json` `name` does not silently fail that identity check. The same sidebar send path covers both hosts. The manual telemetry probe uses the dev project; unit tests are network-free.
- `available_commands_update` → slash autocomplete + the `matchSlashCommand` dispatch gate (a typed `/command` must stay at position 0 of the prompt's text block or the CLI won't dispatch it — the confirmed-command send flips the context envelope behind the text). Skills advertised with `_meta.scope`+`_meta.path` are offered anywhere and pass through as text (the CLI loads them mid-prompt). A confirmed `/compact` reports `totalTokens: 0`, which `gateZeroTokenMeta` strips; the fresh count comes from the `auto_compact_completed` notification rail, not that turn's meta (see the `_meta.totalTokens` bullet). Wire evidence: `research/compact.md`, `research/grok-build-oss-findings.md`.
- `current_mode_update` → bottom-toolbar mode button. For `usesClientPlanGate` providers the update is descriptive (a writable mode does not lower the client gate). For Claude/Codex (`applyAgentModeToHostPlan`) the host's `planActive` follows the agent's mode, including Codex `config_option_update` after plan approval. Codex reports two axes; `codexEffectiveModeId` is Plan when `collaboration_mode` is Plan and otherwise keeps the permission `mode` (`agent` / `agent-full-access`) so Auto accept is not flattened to Agent.
- `_meta.totalTokens` → context donut. The trustworthy live Grok count comes from every `session/update` envelope via `contextUsedFromUpdateEnvelope`; prompt-result metadata remains a non-zero-only compatibility fallback through `gateZeroTokenMeta`. Cold restore seeds from persisted `signals.json`, then refreshes Grok from `_x.ai/session/info` (a no-prompt control-plane snapshot); opening the donut refreshes that structured snapshot on a 3-second TTL and shows legend addends (System, Messages, Reasoning/overhead when > 0 via `contextOverheadTokens`, Free) separately from informational rows (Tool definitions, usage categories) whose tokens are already counted in those addends. Those addends are one snapshot bound to the `used` they arrived with (`nextContextBreakdown`); overhead is `contextOverheadTokens` from that snapshot's used, never live occupancy minus stale addends. A used-only occupancy frame keeps the snapshot. While the popover is open and live `used` moves, the webview re-posts `refreshContextDetails` (`sessionInfoStale` bypasses the 3-second TTL so the race at open actually refetches); header and rows become current together rather than hiding the group. `contextBreakdownIsCurrent` is the currency check for that refetch, not a render gate. After `/compact`, `_x.ai/session_notification`'s `auto_compact_completed.tokens_after` wins; without it the RPC supplies the count. Only an explicit RPC -32601 plus an advertised `session-info` command permits the hidden legacy `/session-info` prose fallback — a transient/malformed RPC response leaves the existing meter intact. Claude/Codex `usage_update.size` is the window (`contextUsage.window`) even when the live model id is an unmatched alias such as `opus[1m]`. Their occupancy is the largest single call's prompt (`occupancyFromAdapterTurn`), never Claude's turn-level usage sum (`PromptResponse.usage` is `accumulatedUsage`), and persists through `usageLog`/`contextUsageFromLog`; that adapter ledger is not used for Grok's structured snapshot. Automatic compaction emits a dedicated started/failed notice; a failed auto-compaction suppresses a false manual "Compacted." success bubble. See `research/claude-acp.md`.
- **Steer — mid-turn injection (#52).** `_x.ai/interject` (`{sessionId, text}` plus additive `content` when there are images → `{status:"queued"}`) queues into the session's pending-interjection buffer, drained at the agent's next safe point. It **does not cancel** the turn and loses no in-flight tool work — that is the whole difference from Stop-and-resend, and it's pinned by the `interject` live test (the model must obey mid-stream *and* the turn must not end cancelled). **`grok.steerByDefault`** (webview-only, default **false**; Settings → General → *Steer by default*, hidden when the CLI can't interject) is the policy: off = today's queue (with an on-demand Steer button), on = a send-while-busy **skips the queue** entirely. It branches in `queueOutgoing` (chat.js) — the single choke point both typed sends and dictated utterances pass through — and falls back to queueing on the three nothing-to-steer-into cases (`busyLocked` priming, `!steerSupported`, not busy). A queued block gains a **Steer** button while `body.turn-busy` (attachments included); clicking it posts `steerSend { text, chips?, fromQueue:true }` then `clearQueuedSends`. `steerSend` (`sidebar.ts`) snapshots the queue synchronously, builds the payload with `buildPromptWithImages` / `buildQueuedPromptWithImages` (same image encoder as a send), and interjects. Text-only omits `content` so the legacy `{sessionId, text}` wire stays byte-identical. Images ride `content` (`[{type:"text", text}, {type:"image", mimeType, data}, …]`): the Text block is the rewritten prompt (failed-orphan tags never emitted; no `[Image #N: path]` form) and wins over `text` on a capable CLI. `cliHonorsInterjectContent` / `AcpClient.honorsInterjectContent` require a **live-verified grok 1.x**; a 0.2.x or unverified CLI that would ignore `content` **queues the whole item** (does not latch `steerUnavailable`). On -32601 the host emits `steerUnavailable` (latches the button off), **re-queues text and chips**, and warns — a capability gap must never eat a message. Claude Code stays unoffered (`steerableProvider`).
- **Turn footer duration + status.** The per-turn agent footer (Copy + timestamp) gains a duration/status line — "Worked for 12.4s", "Cancelled after 4.1s", "Failed after 8.7s". Live turns are measured by the host: `beginTurn` stamps `Session.turnStartedAt`, and every turn-ending emit site (`agentEnd`/`agentError`/`exit`, via `turnEndFields`) attaches additive `status`/`durationMs` to the same message — `agentEnd` reads the prompt result's `stopReason` (`turnStatusFromPromptResult`: `"cancelled"` → cancelled, anything else → completed), a thrown prompt is `failed`, the cancel-recovery notice is `cancelled`, and `exit` carries `failed` only when a turn was in flight. Restored turns read the persisted rail: the replayed `turn_completed` (`subagentUpdate` additive `turnDurationMs`/`turnStatus`) takes an explicit `duration_ms` when the CLI sends one, else the wall-clock gap from the turn's user-message timestamp (`replayedTurnDuration`); an old transcript with neither shows no line. Subagent `duration_ms` never reaches the footer — it stays on the subagent card. Resume shows headless `session_kind` rows in the history list (only `subagent` is hidden).
- **Thumbs on an agent turn (#114).** Grok-only and **opt-in** (`grok.thumbsFeedback`, default off, Settings → General → *Thumbs feedback to SpaceXAI* — desk toggle; remotes show a read-only On/Off status, same as usage stats). Off never shows thumbs; on means when the provider supports it (`decideFeedbackAvailability` requires `userEnabled`). The turn footer (Copy + timestamp) gains up/down once `feedbackAvailability` is true — `session/new` `_meta.feedbackEnabled` or an advertised `feedback` command; Codex/Claude never advertise it — and **only on the most recent completed turn of this process**. A cold `session/load` shows none (the agent's counter restarted). Click sends `_x.ai/feedback` (`ClientFeedbackInput`, `rating_type:"thumbs"`, `rating_value` -1/0/1, no `request_id`, **no `turn_number`**): the agent attributes the rating from its own session tracking. Host-side User-item mapping is not sent because six `SyntheticReason` variants all produce `ConversationItem::User` and most never reach this host (`research/turn-feedback.md`). `-32601` and "Feedback is disabled." latch the buttons off (`feedbackAvailability` false). `client_type` is the host (`extension`/`desktop`) even when a phone clicked; inbound remote is `propose`. No comment box. Local `Session.turnRating` only.
- **Fork — branch the conversation (#48).** Gear → Session → *Fork conversation* calls `_x.ai/session/fork` with its three required camelCase fields. It copies session files only, never workspace code. `forkDisplayName` adds one leading `(Fork)` tag; `sessionDisplayName` rejects a **legacy** primer-derived summary so invisible historical plumbing cannot propagate into fork names. The source name is resolved before the RPC and written to the fork before `openSession`. Forking is whole-session only because a partial target truncates `chat_history` without matching `updates.jsonl`.
- **Worktree sessions (P2-8).** Gear → *New worktree session* (or `Grok: New Worktree Session`) creates an isolated git worktree via the **unadvertised** `_x.ai/git/worktree/create` (`sessionId` + `sourcePath`, optional `label`) under `~/.grok/worktrees/`, then starts a fresh session whose process **cwd is the worktree path** — so agent edits don't touch the main checkout until merged. *Apply worktree* merges edits back (`_x.ai/git/worktree/apply` with `sessionId` + `worktreePath`); *Remove worktree* disposes processes on that path then `_x.ai/git/worktree/remove`. All four RPCs return `"unsupported"` on -32601 so an older CLI degrades. History scans the workspace cwd **and** known worktree paths (pure `mergeSessionIndexes` / `collectSessionCwds` in `worktree.ts`), stamps a `WT <label>` badge (`worktreeLabel`) on rows, and reopens with the correct cwd (`resumeSession` now carries `cwd`). Meta (`SessionMetaOverride.worktreePath`/`worktreeLabel`/`sourceGitRoot`) survives reload. Probe-confirmed against grok 0.2.111; pure parsers in `src/worktree.ts`, wire notes in `research/worktree.md`.
- **Rewind (P2-9).** Hover a user bubble → *Rewind* (or `Grok: Rewind Conversation`) → `_x.ai/rewind/points` lists restore targets and `_x.ai/rewind/execute` rolls the conversation back — truncating chat **and restoring files on disk** from grok's `rewind_points.jsonl` snapshots (the CLI's own file-snapshot feature; a chat action reverting code is confirmed via a QuickPick first). Both RPCs return `"unsupported"` on -32601. Pure `parseRewindPoints`/`parseRewindExecute` in `src/rewind.ts`; wire notes in `research/rewind.md`. **Rewinding to N DISCARDS N and its response** along with everything after (probe-verified, `research/rewind-semantics-probe.cjs` — the name reads like the opposite, and reading it that way cost an extra turn per edit; the tip IS a legal target).
- **Edit and resend (#56).** Edit targets the bubble's own rewind point because execute discards its target. Steered messages have no rewind point, so live/replayed steer bubbles are marked and excluded from mapping. If no exact point exists, the host offers copy-to-composer rather than guessing. Rewind/Edit truncate the live DOM, `Session.buffer`, persisted plan/permission cards, usage log, and `lastPlanVerdict` consistently. Legacy primers and marker turns remain filtered from user-facing point counts. `rewindSession`, `editLastMessage` and `uiConfirmAnswer` are `propose`, not host-local — they move together because `confirmInChat` only settles on an answer, so gating the answer would hang the confirm a remote had just been shown.
- **Find in conversation (#99).** In-webview find (`openFind` in `media/chat.js`) — ⋯ → Find in conversation (beside Export), Cmd/Ctrl+F on desk/VS Code only (`!IS_REMOTE`; Mac find is Cmd+F so it does not collide with `grok.composerForward`), and Command Palette `grok.findInSession` (`{type:"findInSession"}`, host-local, transient). Slides in under the top bar. `CSS.highlights` + `Range` (no `<mark>`); without the API, count + navigate + scroll. Collapsed tool/command/subagent rows are searched and expanded on hit; `body.thinking-hidden` matches are counted (`N in hidden thinking traces`) and stay out of next/prev until included. Navigation calls `setStickToBottom(false)` and never `forceScrollToBottom`. See `research/find-in-session.md`.
- **Run progress (P2-10).** Deep Research / Workflow / Goal turns stream progress on the live `_x.ai/session_notification` rail; the pure `isRunProgressUpdate`/`parseRunProgressUpdate` (`run-progress.ts`) normalize `workflow_updated`/`goal_updated` into a teal progress card with Pause/Resume/Stop (`workflowControlCommand` maps a control to the CLI's slash). See `research/run-progress.md`.
- **`_meta.usage` → billing breakdown (#53).** Nested inside the prompt result's `_meta` (and mirrored on the rail's `turn_completed.usage`), extracted by `extractPromptUsage`. `usage.*` is whole-prompt billing across `modelCalls`; flat token siblings are the last model call; `totalTokens` is context size. `costUsdTicks` is fixed-point USD (**10^10 ticks = $1**, per xAI's `UsageTotals` contract). Missing cost/cache fields stay absent and render no row. `addUsage` sums tokens and cost through the same total, gated by `usageIsRealMeasurement` so `/compact` cannot double-count stale usage. Each prompt lives only in the session-id-keyed `SessionMetaOverride.usageLog` at its replay-stable `afterUserMessage` coordinate (successful zero-inference turns use a coverage-only entry); live completion, restore, and rewind all derive the aggregate from that persisted ledger. `enforceCompleteSessionCost` removes aggregate cost unless those coordinates span the whole conversation, while per-turn cost remains visible. Rewind truncates the ledger before recomputing, so discarded turns subtract both tokens and cost exactly.
- **Vision input (image paste/upload).** Ctrl+V in the composer, drag-drop, and the + picker attach raster images (png/jpe?g/gif/webp — SVG deliberately stays a *path* chip so grok can read/edit the source; 20MiB cap per the xAI docs) as inline `{type:"image", mimeType, data:<base64>}` content blocks in `session/prompt`, plus a `[Image #N]` tag line in the text block (`envelope\n\ntext\n\ntags` — text keeps position 0 so `/commands` still dispatch; disk imports carry the origin path in the tag, `[Image #1] (assets/x.png)`, so grok can act on the real file). **The CLI still advertises `promptCapabilities.image:false` but actually accepts the blocks** (verified 0.2.87; behavior over advertisement — no capability gate, the `vision-prompt` live test pins it instead; see `research/vision-input.md` + `research/vision-probe.cjs`). Pending images live in a session-NEUTRAL staging dir (`<globalStorage>/image-staging/` — a grok session dir gets deleted by the empty-session cleanup, which would kill a pending screenshot), are pre-read + validated at send (any unreadable image **blocks the send** with chips intact — a dangling tag sends the model hunting for an image it never got), and are unlinked after send / chip removal, with an age-gated sweep on activation for orphans. `[Image #N]` is stamped once at attach (`allocateImageIndex`, `chips.ts`; `Session.imageIndexHighWater`) and never rewritten — the composer label, the bubble chip and the tag all read that stored index. The high-water resets only when nothing is staged (composer empty and queue empty), so a plain send still starts at `#1` while `#2` stays `#2` after `#1` flushes or is removed; tags may be non-contiguous. Authored text is copied verbatim. The CLI resolves a reference against the images on THIS message (measured, `research/image-index-probe.cjs`); a number from an earlier turn matches nothing. The webview holds send while a paste's FileReader is in flight (`pendingPaste`) and keeps the text half of a mixed clipboard; chips are host-owned — `send` carries only the text.
- **Remote document upload.** `initialState.capabilities.uploadFile === true` is the additive host capability proof; an updated web portal may then send `{type:"uploadFile",name,data}` with base64 bytes. The host re-validates the sanitized basename, six-extension allowlist, and shared 20MiB cap, stages to `<globalStorage>/file-staging/<uuid>/<basename>`, and routes the path through `addDroppedFile`, producing the same explicit path chip and `Attached file:` prompt as local drag-and-drop. Sent documents are retained in `SessionMetaOverride.uploadedFiles` until the last referencing session/fork is deleted because Grok reads the path lazily; only unreferenced orphan directories are swept after seven days.
- **Command details (#41).** The extension runs every shell command itself (the mandatory `terminal/*` handlers), so each command row carries an always-available, collapsed-by-default expandable detail — a Claude-Code-style **IN/OUT block** on the shared code-chip surface: IN = the FULL command text from `rawInput.command` (grok truncates its titles), OUT = the complete captured output — each **capped to the first `MAX_COMMAND_PREVIEW_LINES` (6) logical lines, no inner scroll** (#71; pure `commandTextPreview`), the rest reached via **"View all"**: VS Code posts `openText` (untitled tab; commands carry `initialState.commandLanguage` from `commandLanguageForDialect`, output omits language so the editor can detect); desktop with `capabilities.previewInApp` opens the in-app overlay (`openPreviewOverlay` — highlighted via `GrokSyntaxHighlight`, Copy / Save As through filename-bearing `openText`; a Read fetches the whole file via `readProjectFile` and marks the agent's lines on the `.tdl` gutter, with **Open in file panel** when the path is in-workspace); an older desktop host that never sent the flag still opens the bare viewer window; on a remote client (no host editor) it **expands inline** (`IS_REMOTE`, the same audience-split the diagram export uses) — a single long logical line shows in full. Snapshotted at `terminal/release` in `acp.ts` (`commandDone` → `commandOutput` host message; the buffer is exactly what grok received — same byte cap — plus a 100K display cap via `capCommandOutput`). Every tool row is ONE line (`.tool-item-label` span, CSS-ellipsized at the row edge as backstop; `toolLabel`'s 40-char clamp is the deliberate scannable-summary trim — the full text lives in the detail; an unpredicted title over `TOOL_LABEL_MAX` (50) is `middleElide`d (both ends kept) and the label span's `title` is the full string; a COMMAND row instead reads "Run \<program\>" via the pure `commandProgramLabel` (webview-helpers.js): first token, path-stripped/de-quoted, + one non-flag **bare** subcommand (a *quoted* next token, or one containing a path separator, is an argument/banner not a subcommand, so it's dropped — `Write-Output '=== 1. git status ==='` → "Write-Output"; `node research/x.cjs` → "node"), first-statement-only, "command" fallback. A **`(cd dir ; cmd)` subshell** (grok's POSIX navigate-then-run idiom, emitted even against PowerShell) is unwrapped — the `( )` stripped and a leading `cd <dir>` prelude skipped — so it labels the real command (`(cd … ; node x)` → "node"), not "(cd"; the cd-skip is scoped to subshells, so a user-typed `cd src && npm test` still reads "cd src" — so `git status`/`npm test`/`Get-Date` stay scannable while a `node -e "…"` payload collapses to `node`); a lone RUNNING command's group is expandable immediately (`cmd-single` — header chevron shows mid-run and expanding auto-opens the row's IN/OUT; multi-tool groups keep their chevron until the batch closes); expandable command rows carry a TRAILING chevron (› collapsed ↔ v expanded, `expanded` class) + hover brightening (`has-details`, `wireCommandToggle` — plain rows like the output poller don't react), and clicks inside the block never collapse it (text selection). Exit codes merge into the output: exit 0 with output is silent, exit 0 with **no** output renders a muted `✓ done · no output` marker (no empty `(no output)` pre), non-zero renders an `[Error] exit N` marker + error-tinted output **and rolls the failure up to the row (`.tool-failed`) + group (`.has-error`)** — a failed command signals at a glance like a `status:"failed"` tool (`markToolFailed`), not just inside its OUT box, a live kill (`commandOutput.cancelled`, always stated by this host — true only from `commandDone` when `exitCode` is null; false on every other `commandOutput`, including session/load hydration) renders muted `[Cancelled]` — the webview keys that marker on the field when present (`commandOutputWasCancelled`), and on `exitCode == null` when an older host omitted it (those hosts never hydrated replay output); not `state.replaying`. Output attaches by **two paths, one per agent execution model**: **grok-build** delegates commands via `terminal/create`, so the client snapshots the buffer at release and attaches by exact command string (`commandOutput` → `state.pendingCommandDetails`, `attachCommandOutput` in chat.js) with a standalone fallback row so output is never dropped; the **cursor/Composer** agent runs commands in its own CLI-side persistent shell (never `terminal/create`), so its output rides the **completed `tool_call_update.rawOutput`** and is attached by **`toolCallId`** (`maybeAttachToolResultOutput` + the pure `extractToolResultOutput` in webview-helpers.js — unfenced string + the same 100K `capCommandOutput` as the host, so a buffer rebuild of a live Claude row still has OUT; order-independent, because Composer runs commands in parallel and finishes them out of issue order, so a FIFO/order-based guess would misattribute). Do **not** gate that attach on `state.replaying`: `historyReplay` also wraps in-memory rebuilds (`focusSession` / `rehydrateWebviewFromFocused`), and live Claude has no `commandOutput` in the buffer. Both paths reference the same `details` node stored on the `pendingCommandDetails` entry (keyed by both `command` and `toolCallId`), which survives a lone command's flatten to `.tool-flat` (the flatten MOVES the chevron + details nodes). `grok.expandCommandOutputs` (webview-only, default false; Settings → General → **Expand tool details** — label renamed to match the two Command-Palette commands, setting KEY unchanged; mirrors the showThinking pattern via `applyExpandCommandOutputs`) pre-opens every detail **and** every detail-bearing tool GROUP (a group is detail-bearing iff it has a `.has-details` row — a command's IN/OUT, a Read file's View all, or an edit's diff; search/list-only groups stay collapsed). **Read rows (#122)** reuse the same IN/OUT + `appendCommandPreview` View all chrome: IN is the path, OUT is `content[].content.text` (or `extractToolResultOutput`); the title adds `lines a-b` from `rawInput.offset`/`limit` (1-based; 0→1) or completed `FileContent.total_lines`, and invents no numbers when those are absent. — the group open state is applied both live (`applyExpandCommandOutputs`) and at group finalization (`closeToolGroup`) — except on a group the **user** has toggled (`el._userToggled`, set by the header click), which `closeToolGroup` leaves alone: expanding a *running* batch to watch it must not be undone the instant it finishes. An explicit setting change or Expand/Collapse-All latch flip still force-applies through `applyExpandCommandOutputs`, so a global action always wins. On-demand, the Command Palette `grok.expandAllToolDetails` / `grok.collapseAllToolDetails` (titled *…(This Session)*) → `sidebar.setAllToolDetails` **`emit`s** (not `post`s) a `setAllToolDetails` host message that flips a per-session **latch** — `state.toolExpandOverride` (tri-state `null | true | false`) in chat.js. The latch force-expands/collapses **every** group (incl. explore/edit) + IN/OUT box — including a still-running batch — and keeps applying to later tool calls, because all render points flow through the effective-expand helpers `groupShouldExpand`/`detailShouldExpand` (`override ?? persisted-setting`). It beats the persisted setting (last action wins; flipping the gear switch or the `expandCommandOutputs` message clears it), **rides the session replay buffer** (the `emit`, not a `post`) so it survives Agent-Dashboard focus-swaps, and resets on a cold reopen (`resetForNewSession`) — never persisted to disk. CSS shows a running group's chevron once it's `.expanded`. Failed non-shell tools surface their reason via `toolFailureText`, which mines variant-keyed `rawOutput` blobs (e.g. `list_dir` → `NotFound`, `read_file` → `FileReadError`) after `message`/`content`/`error`, else the generic "Tool call failed." Radius scale: bubble 12 → block code/IN-OUT 8 → inline chip 6 (`--chat-radius-*`). `session/load` does not re-run terminals. grok 1.0.5+ replays a completed execute `tool_call` whose `content` is raw stdout — never `rawOutput.output_for_prompt`, which prefixes `exit: N`. Claude replays two rows: a pending `tool_call` whose `content` is the tool description and whose `rawInput.command` (also `title`) is the command, then a completed `tool_call_update` whose `rawOutput` is the bare stdout string and whose `content` is that stdout wrapped in a markdown console fence — prefer the string, join the command by `toolCallId`, `exitCode` stays null. Codex `session/load` emits no tool rows (the `formatted_output` branch matches the live normalizer only). The host emits the same `commandOutput` (`commandOutputForToolCall`, replay-only, same 100K cap, `cancelled: false`). An unrecognized provider shape degrades to no OUT. **MCP calls reuse this shell:** `prepareMcpToolCall` (`src/mcp-tool.ts`) folds grok `search_tool` into the explore tool-group (`kind:"search"`) and emits Codex `mcp__<server>__startup` (a failed startup keeps its status and error text), reads provider metadata before argument-key heuristics, always stamps `detailInput` on recognized MCP rows (`null` while Claude args are pending, `{}` for a no-argument call), and emits `commandOutput` joined by `toolCallId` from the complete measured per-provider result (not ACP `content`; grok/codex omit it): Codex `result.content` text plus pretty-printed `structuredContent` (or `error` when non-null), grok `OkayOutput` and any sibling `output` variants, Claude a content-block array (non-text leaves a JSON trace) or a string `rawOutput` shown verbatim (the completed update only — not `_meta.claudeCode.toolResponse`). That MCP `commandOutput` always states `agentSawCut: false` so the webview does not claim the agent saw the 100K display cut; shell `commandOutput` states `agentSawCut: true`. An older host that omits the field is worded as `output truncated` with no attribution (`commandOutputTruncationNote`). Webview qualification is `rawInput.command` / `.cmd` / `detailInput`. Session export keeps MCP rows on the generic tool path — `detailInput` is not a shell command. Shapes: `research/mcp-shapes.md`.
- **Edit diffs inline (#45).** Every `kind:"edit"` tool row carries an always-visible `+N −M` change count (green/red, rolled up onto the collapsed group header as **path-deduped** totals, e.g. *Edited 2 files · +7 −2*) **plus** an expandable inline diff on the same detail surface commands use — so an edit is auditable in chat even under **Auto-accept**, where no permission card fires. This is decoupled from the permission flow on purpose: the diff `content` block (`type:"diff"`, with `oldText`/`newText` = the replaced region) rides the `tool_call_update` regardless of permission mode, and whether a permission card even appears is grok-build/platform-dependent, not a config toggle (verified 0.2.99: default config sends 0 permission requests for in-workspace edits; `research/edit-diff.md`). The pure `computeLineDiff(oldText,newText)` (`webview-helpers.js` — LCS backtrack, CRLF-normalized for compare+display, empty region = 0 lines so a new file reads as pure adds) yields the `+/−` counts and the diff rows. **A block is expanded into one hunk per replaced SITE** (`extractDiffSites` in chat.js): the block's `oldText`/`newText` is the search *pattern*, so a `replace_all` block describes only the token and alone renders 148 occurrences as one meaningless `+1 −1` — `_meta.details[]` is the complete account (one entry per site, `{old_string, old_line, new_string, new_line, context_before, context_after, line_prefix}`, real 1-based **post-edit** file lines; verified 12/12 against ground truth, `research/edit-diff-lines.log`). Per delivery shape: **completed + session/load** carry `details[]` → a site each, rendered `line_prefix + old_string` → `line_prefix + new_string` (there is **no `line_suffix`** on the wire, so the line's tail is genuinely unavailable and is not reconstructed); the **echo** carries no `details[]`, only a first-site-only block `_meta.{old_line,new_line}` → one approximate hunk the completed update then upgrades; neither (a whole-file Write's `_meta:{}`) → one hunk seeded 1 via `fileLineOr1` (also the guard for absent/bogus — 0/negative/non-number). A creation (`old_string:""`) takes no prefix, so a new file stays pure adds instead of the prefix becoming a phantom del. `buildInlineDiffRegion(hunks)` renders them **Codex-style**: **ONE `.tool-diff-region` per BLOCK holding N hunks** (never one region per site — 148 sites would otherwise stack 148 boxes) of `.tdl` rows, each a `[+/− sign][line-number gutter][code]` grid with a colored left-border stripe + subtle per-line tint; each hunk seeds its own gutter, and a quiet `.tdl-sep` hairline parts **non-contiguous** hunks (never before the first, never between contiguous ones). the region shows the first `DIFF_PREVIEW_LINES` (12) rows with a **"Show more"/"Show less"** toggle that grows it **inline, no inner scroll** (#71; remote-safe, unlike the host-local `open diff →`); `MAX_INLINE_DIFF_LINES`=400 is the hard budget **across** a block's hunks (1000 sites must not paint 2000 rows), the `open diff →` remainder note hidden until expanded — but the `+N −M` is summed over **every** site *before* the cap, so a capped render still reports the true magnitude. Del shows the old-side number, add/context the new-side. The gutter track sizes per region to the widest number rendered (`--tdl-num-w` = `max(4ch, digits+1)`) — 4ch through 999, wider only for a 1000+ line file. The `+/−` glyph is a color-blind affordance beside the border. Colors are **Codex's exact palette** (`--tdiff-*` CSS vars, dark default + `body.vscode-light` override): add `#40c977`/`#00a240`, del `#fa423e`/`#ba2623`, backgrounds the same at 23%/15% alpha — reused for the `+N −M` stat too. `applyToolDiffs`/`attachDiffPreviewToToolItem` handle BOTH the live `tool_call_update` and the session-load completed `tool_call` replay shapes (#30); a single call can carry multiple `diff` blocks (rendered as an array), each getting exactly **one** `open diff →` (per-site would emit 148 buttons; its payload stays the block's own `oldText`/`newText`). **grok reports each edit's diff twice** — an optimistic pre-write echo, then the authoritative completed update — and for a whole-file Write that OVERWRITES a file the echo carries `oldText:""` while the completed one carries the real prior content, so the echo must not win: idempotency is keyed on `item._diffSig` (the serialized diffs, **sites included**, so the `details[]`-seeded render outranks the echo's approximate one), making an identical replay a no-op while a *different* later diff repaints the row (reusing the detail node, or `wireCommandToggle`'s listener would strand + double-bind). **Totals paint as each edit lands**, not at batch close — the data is on the wire ~2.2–3.3s before the turn ends (`research/edit-diff-timing-probe.cjs`). `recomputeGroupDiffTotals` *recomputes* from the rows' `_diffStat` (so a corrected row replaces rather than double-counts) and `paintGroupDiffTotals` owns a `.tool-group-diff-totals` span, re-painted after each of the two things that wipe the header: `addToToolGroup`'s per-call `hdr.innerHTML` rebuild and `closeToolGroup`'s label reset. Painting earlier still, from the initial `tool_call`'s `rawInput`, is deliberately NOT done: it buys ~30ms and reconstructs the same wrong `oldText:""` for a Write. None of this expands anything — totals ride the *collapsed* header; only `grok.expandCommandOutputs` / the latch open rows. The native `open diff →` link (→ `sidebar.ts` diff editor) stays on VS Code and older hosts; desktop `previewInApp` routes it into the same overlay at full size (`requestDiffPreview`) instead of a bare window, with **Open in file panel** when the path is in-workspace. `summarizeTools` counts edits by unique `toolFilePath` so "Edited N files" matches the deduped header totals.
- **Generated media (v1.4.x).** `/imagine` and `/imagine-video` report generated paths in completed tool results. `isMediaGenToolCall` / `extractGeneratedMediaPaths` detect them; `postGeneratedMedia` forwards only paths whose canonical targets remain in the provider's generated-media root. `asWebviewUri` streams trusted files; a size-capped `data:` fallback is allowed only after that trust check when the surface has no served root. Codex inference additionally requires UUID / `exec-UUID` adapter ids and resolved containment under `generated_images`. Wire format + probes: `research/image-generation.md`.
- **Subagent rows.** A delegation is a tool call whose `_meta["x.ai/tool"].name` is `spawn_subagent` (grok-build) or `Task` (the Composer agent) — that marker is **authoritative both ways** for the pure classifier (`isSubagentToolCall`, `webview-helpers.js`): grok titles Grep/Read calls with their query/filename, so a grep FOR "spawn_subagent" is titled exactly that and title matching false-cards; without `_meta`, exact tool names + `rawInput.subagent_type` + the legacy background heuristics apply (the `get_command_or_subagent_output` poller stays excluded). The row renders *Subagent · \<description\>* (never downgraded by Composer's untitled completion update — `setSubagentTitle`) with blink-dots, then a duration stamp and a click-to-expand result under an "Output of the subagent:" label; `cleanSubagentOutput` strips the CLI envelope (plumbing tags, lead-ins, one wrapping `<response>` pair, the Agent ID hint) — every pattern optional and never throwing. Completion shapes differ: grok-build's foreground `tool_call_update` carries `rawOutput.SubagentCompleted` (output + duration_ms); Composer's carries only untitled text (`rawOutput {type:"Text"}`, no duration); a **background** spawn "completes" instantly with a started-ack (skipped — not the result) and its real output arrives on the `get_command_or_subagent_output` poller's `TaskOutput`, matched back to the card by task id (`maybeFinishSubagentFromTaskOutput`). **On cold `session/load` that poller output is FLATTENED to a text blob** (`=== Task … === / Command: [subagent:…] / … / === Output ===`, not the live structured shape), so the restore path parses it back with the pure `parseSubagentTaskResult` (`webview-helpers.js`, subagent-only — a backgrounded shell command polls the same tool and is left as a normal row) and `maybeFinishSubagentFromTaskText` folds it into the card + drops the redundant poller row (else a restored background delegation showed a stuck card + a raw `[subagent:…]` row). **Failure/cancel are threaded through BOTH the tool-channel completion AND the lifecycle rail** (a completion routinely races ahead of the lifecycle, so the marker can't live only on one) — a failed card reads red (`.subagent-failed`), a cancelled one muted (`.subagent-cancelled`); `finishSubagentCard`'s already-done early-return upgrades a late status marker, not just a late duration. The restore-created card is marked `subagentReplayed` and excluded from **live** lifecycle matching (mis-attribution guard) but NOT during replay (`state.replaying || !replayed`), so it still consumes its own re-forwarded lifecycle. The `subagent_spawned`/`subagent_finished` lifecycle stream is delivered **two ways**: on the LIVE `_x.ai/session_notification` rail (→ `xaiNotification`; the pure `isSubagentLifecycleUpdate` in `acp-dispatch.ts` re-routes it to the same `subagentUpdate` channel the webview consumes) and on the persist/replay `_x.ai/session/update` rail (→ `subagentLifecycle` → `subagentUpdate`, for `session/load`). `subagent_finished` fills `duration_ms` + the child's output — the duration Composer's tool-channel completion lacks (probe-confirmed on 0.2.101; earlier 0.2.93 measurements watched only the persist rail, which never fires live, hence the old "the CLI only logs these" note). Lifecycle tagging prefers an exact `subagent_id` match and only then the first untagged FIFO card (`tagSubagentChildSession`) — a started-ack can tag card A before `subagent_spawned`, so FIFO-first would steal B. A history replay settles never-completed rows at replay end. On grok **1.0.3+** the CLI multiplexes every session onto one stdout (`params.sessionId` is the demux key; captured 2026-08-14): child prose, thoughts, and tool calls arrive live on the child's sessionId after `subagent_spawned` (`child_session_id`). `parseAcpLine` keeps that id; `isForeignSessionUpdate` / `handleSessionUpdate` emit them as additive `childStream` host messages into the matching card (never the parent transcript). A child tool row closes the open prose segment (`closeChildProseSegment`) so the next chunk opens a new `.subagent-prose` below it — the same close-on-tool interleave as the parent transcript. Word-level child chunks batch through `requestAnimationFrame` per card (`scheduleChildStreamFlush` / `flushChildStream`), matching `appendAgent`/`flushAgent`. A child chunk whose session matches no card is dropped. Composer-era CLIs never send child streams — `test/fixtures/composer-subagent-session.jsonl` still pins that shape. grok persists each child as a **top-level sibling session** with `session_kind: "subagent"` — the history list hides those (`SessionListEntry.kind` from `buildEntry`, filtered in `postSessionsList`, which pages by index slots consumed — the `nextOffset` field on the `sessions` message — so hidden rows can't stall or duplicate load-more). Cold `session/load` still does not replay child transcripts into the parent; cards rebuild from the lifecycle rail. Real wire captures: `test/fixtures/composer-subagent-session.jsonl` (replayed by `test/subagent-replay.dom.test.ts`) and `test/fixtures/grok-subagent-mux.jsonl` (replayed by `test/subagent-mux.dom.test.ts`); deep-dive: `research/subagents.md`.
- **Logout (#13).** `grok.logout` command + gear-menu *Sign out* → `sidebar.logout()`. Codex observes `codex logout` success before reset; Grok retains its terminal flow. `resetProviderSessionsAfterLogout` synchronously detaches focused, background, active-remote, and detached-tab sessions before persisted connection-state writes or replacement starts can stall. Drafts with conversation ids enter META before disposal and leave it only after startup succeeds **and a focused/attached composer exists**; detached tabs retain META through desk-first sign-in and restore on their own same-provider, other-provider, or needs-provider reattachment. Notices are transient. Draft-bearing and `needsProvider` sessions survive parking, release, empty sweeps, and reaping. The focused replacement keeps its authorized cwd. Remote policy keeps account actions host-local and permits only retrying an already-connected provider session.
