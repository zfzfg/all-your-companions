{
  "name": "all-your-companions",
  "displayName": "All your Companions - in one Place!",
  "description": "Unified GUI for AI Coding Companions: Google Antigravity, Grok Build, OpenAI Codex, and Claude Code. Multi-session chat, voice control, rich media, and universal diff review. Fork of Grok Build for VS Code (v4.1.8) by Paweł Huryn.",
  "version": "0.1.0",
  "publisher": "zfzfg",
  "author": {
    "name": "Collin Lerche (zfzfg) | STERRA",
    "url": "https://sterra.online"
  },
  "license": "FSL-1.1-MIT",
  "homepage": "https://github.com/zfzfg/all-your-companions",
  "bugs": {
    "url": "https://github.com/zfzfg/all-your-companions/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zfzfg/all-your-companions"
  },
  "engines": {
    "vscode": "^1.106.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "keywords": [
    "antigravity",
    "gemini",
    "grok",
    "grok-build",
    "codex",
    "claude",
    "claude-code",
    "companions",
    "all-your-companions",
    "acp",
    "acp-client",
    "agent-client-protocol",
    "agent",
    "ai",
    "chat",
    "thinking",
    "tool-calls",
    "sidebar"
  ],
  "icon": "resources/AllYourCompanions-LOGO.png",
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "secondarySidebar": [
        {
          "id": "companionsSidebar",
          "title": "All your Companions",
          "icon": "resources/grok-icon.svg"
        }
      ],
      "activitybar": [
        {
          "id": "companionsPrimary",
          "title": "All your Companions",
          "icon": "resources/grok-icon.svg"
        },
        {
          "id": "companionsProjects",
          "title": "Projects",
          "icon": "resources/grok-icon.svg"
        }
      ],
      "panel": [
        {
          "id": "companionsPanel",
          "title": "All your Companions",
          "icon": "resources/grok-icon.svg"
        }
      ]
    },
    "views": {
      "companionsSidebar": [
        {
          "type": "webview",
          "id": "companions.chat",
          "name": "Companions"
        }
      ],
      "companionsProjects": [
        {
          "type": "webview",
          "id": "companions.projects",
          "name": "Projects"
        }
      ]
    },
    "commands": [
      {
        "command": "companions.open",
        "title": "All your Companions: Open"
      },
      {
        "command": "companions.moveView",
        "title": "All your Companions: Move Chat View"
      },
      {
        "command": "companions.newSession",
        "title": "All your Companions: New Session"
      },
      {
        "command": "companions.newWorktreeSession",
        "title": "All your Companions: New Worktree Session"
      },
      {
        "command": "companions.applyWorktree",
        "title": "All your Companions: Apply Worktree"
      },
      {
        "command": "companions.removeWorktree",
        "title": "All your Companions: Remove Worktree"
      },
      {
        "command": "companions.rewind",
        "title": "All your Companions: Rewind Conversation"
      },
      {
        "command": "companions.compact",
        "title": "All your Companions: Compact Conversation"
      },
      {
        "command": "companions.pickModel",
        "title": "All your Companions: Pick Model"
      },
      {
        "command": "companions.toggleMode",
        "title": "All your Companions: Toggle Plan / Agent Mode"
      },
      {
        "command": "companions.sendSelection",
        "title": "Add Selection to All your Companions"
      },
      {
        "command": "companions.sendFile",
        "title": "All your Companions: Send File"
      },
      {
        "command": "companions.insertAtMention",
        "title": "All your Companions: Insert @-Mention"
      },
      {
        "command": "companions.showLogs",
        "title": "All your Companions: Show Logs"
      },
      {
        "command": "companions.settings",
        "title": "All your Companions: Settings",
        "icon": "$(gear)"
      },
      {
        "command": "companions.expandAllToolDetails",
        "title": "All your Companions: Expand All Tool Details (This Session)"
      },
      {
        "command": "companions.collapseAllToolDetails",
        "title": "All your Companions: Collapse All Tool Details (This Session)"
      },
      {
        "command": "companions.findInSession",
        "title": "All your Companions: Find in Conversation"
      },
      {
        "command": "companions.logout",
        "title": "All your Companions: Log Out"
      },
      {
        "command": "companions._debugDummyPlan",
        "title": "All your Companions: Debug — Show dummy plan card (for testing Approve/Reject/Cancel)"
      },
      {
        "command": "companions.composerForward",
        "title": "All your Companions: Composer — Move Forward"
      },
      {
        "command": "companions.composerPreviousLine",
        "title": "All your Companions: Composer — Move to Previous Line"
      },
      {
        "command": "grok.open",
        "title": "All your Companions: Open"
      },
      {
        "command": "grok.moveView",
        "title": "All your Companions: Move Chat View"
      },
      {
        "command": "grok.newSession",
        "title": "All your Companions: New Session"
      },
      {
        "command": "grok.newWorktreeSession",
        "title": "All your Companions: New Worktree Session"
      },
      {
        "command": "grok.applyWorktree",
        "title": "All your Companions: Apply Worktree"
      },
      {
        "command": "grok.removeWorktree",
        "title": "All your Companions: Remove Worktree"
      },
      {
        "command": "grok.rewind",
        "title": "All your Companions: Rewind Conversation"
      },
      {
        "command": "grok.compact",
        "title": "All your Companions: Compact Conversation"
      },
      {
        "command": "grok.pickModel",
        "title": "All your Companions: Pick Model"
      },
      {
        "command": "grok.toggleMode",
        "title": "All your Companions: Toggle Plan / Agent Mode"
      },
      {
        "command": "grok.sendSelection",
        "title": "Add Selection to All your Companions"
      },
      {
        "command": "grok.sendFile",
        "title": "All your Companions: Send File"
      },
      {
        "command": "grok.insertAtMention",
        "title": "All your Companions: Insert @-Mention"
      },
      {
        "command": "grok.showLogs",
        "title": "All your Companions: Show Logs"
      },
      {
        "command": "grok.settings",
        "title": "All your Companions: Settings",
        "icon": "$(gear)"
      },
      {
        "command": "grok.expandAllToolDetails",
        "title": "All your Companions: Expand All Tool Details (This Session)"
      },
      {
        "command": "grok.collapseAllToolDetails",
        "title": "All your Companions: Collapse All Tool Details (This Session)"
      },
      {
        "command": "grok.findInSession",
        "title": "All your Companions: Find in Conversation"
      },
      {
        "command": "grok.logout",
        "title": "All your Companions: Log Out"
      },
      {
        "command": "grok._debugDummyPlan",
        "title": "All your Companions: Debug — Show dummy plan card (for testing Approve/Reject/Cancel)"
      },
      {
        "command": "grok.composerForward",
        "title": "All your Companions: Composer — Move Forward"
      },
      {
        "command": "grok.composerPreviousLine",
        "title": "All your Companions: Composer — Move to Previous Line"
      }
    ],
    "keybindings": [
      {
        "command": "companions.open",
        "key": "ctrl+;",
        "mac": "cmd+;"
      },
      {
        "command": "grok.open",
        "key": "ctrl+;",
        "mac": "cmd+;"
      },
      {
        "command": "companions.insertAtMention",
        "key": "alt+g",
        "when": "editorTextFocus"
      },
      {
        "command": "grok.insertAtMention",
        "key": "alt+g",
        "when": "editorTextFocus"
      },
      {
        "command": "companions.composerForward",
        "key": "ctrl+f",
        "when": "isMac && (companions.composerFocus || grok.composerFocus)"
      },
      {
        "command": "grok.composerForward",
        "key": "ctrl+f",
        "when": "isMac && grok.composerFocus"
      },
      {
        "command": "companions.composerPreviousLine",
        "key": "ctrl+p",
        "when": "isMac && (companions.composerFocus || grok.composerFocus)"
      },
      {
        "command": "grok.composerPreviousLine",
        "key": "ctrl+p",
        "when": "isMac && grok.composerFocus"
      },
      {
        "command": "companions.findInSession",
        "key": "ctrl+f",
        "mac": "cmd+f",
        "when": "focusedView == companions.chat || focusedView == grok.chat"
      },
      {
        "command": "grok.findInSession",
        "key": "ctrl+f",
        "mac": "cmd+f",
        "when": "focusedView == grok.chat"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "companions.settings",
          "when": "view == companions.chat || view == grok.chat",
          "group": "navigation"
        },
        {
          "command": "grok.settings",
          "when": "view == grok.chat",
          "group": "navigation"
        }
      ],
      "commandPalette": [
        {
          "command": "companions._debugDummyPlan",
          "when": "false"
        },
        {
          "command": "grok._debugDummyPlan",
          "when": "false"
        },
        {
          "command": "companions.composerForward",
          "when": "false"
        },
        {
          "command": "grok.composerForward",
          "when": "false"
        },
        {
          "command": "companions.composerPreviousLine",
          "when": "false"
        },
        {
          "command": "grok.composerPreviousLine",
          "when": "false"
        }
      ],
      "editor/context": [
        {
          "command": "companions.sendSelection",
          "when": "editorHasSelection",
          "group": "companions@1"
        },
        {
          "command": "grok.sendSelection",
          "when": "editorHasSelection",
          "group": "grok@1"
        }
      ],
      "explorer/context": [
        {
          "command": "companions.sendFile",
          "when": "resourceScheme == file",
          "group": "companions@1"
        },
        {
          "command": "grok.sendFile",
          "when": "resourceScheme == file",
          "group": "grok@1"
        }
      ],
      "editor/title/context": [
        {
          "command": "companions.sendFile",
          "when": "resourceScheme == file",
          "group": "companions@1"
        },
        {
          "command": "grok.sendFile",
          "when": "resourceScheme == file",
          "group": "grok@1"
        }
      ]
    },
    "configuration": {
      "title": "All your Companions",
      "properties": {
        "companions.cliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the companion CLI binary. Empty = auto-discover."
        },
        "companions.antigravityCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the Google Antigravity CLI (`agy` / `antigravity`). Empty = auto-discover on PATH."
        },
        "companions.geminiCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the Google Gemini CLI (`agy` / `gemini`). Empty = auto-discover on PATH."
        },
        "companions.grokCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the xAI Grok CLI (`grok`). Empty = auto-discover on PATH."
        },
        "companions.codexCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the OpenAI Codex binary. Empty = auto-discover on PATH."
        },
        "companions.claudeCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the Anthropic Claude Code CLI (`claude`). Empty = auto-discover on PATH."
        },
        "companions.defaultModel": {
          "type": "string",
          "default": "",
          "description": "Model ID to use on new sessions. Empty = companion default."
        },
        "companions.askTimeout": {
          "type": "string",
          "enum": [
            "off",
            "60s",
            "5m",
            "10m"
          ],
          "enumDescriptions": [
            "Wait indefinitely (default)",
            "Continue after 1 minute",
            "Continue after 5 minutes",
            "Continue after 10 minutes"
          ],
          "default": "off",
          "description": "Auto-continue an unanswered question card after this long. The selection you already marked is sent if the card is complete; otherwise the companion is told to continue without an answer. The timer runs in the editor, so closing the panel neither pauses nor extends it."
        },
        "companions.defaultMode": {
          "type": "string",
          "enum": [
            "",
            "agent",
            "yolo"
          ],
          "enumDescriptions": [
            "Agent mode (default)",
            "Agent mode",
            "Auto accept — auto-approve every permission request"
          ],
          "default": "",
          "description": "Mode for new sessions. Empty = Agent."
        },
        "companions.defaultEffort": {
          "type": "string",
          "enum": [
            "",
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultracode"
          ],
          "default": "",
          "description": "Reasoning effort passed to companion stdio."
        },
        "companions.defaultEffortByProvider": {
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "description": "Reasoning effort remembered per agent (grok, claude, codex, gemini). Updated automatically when you change the effort in a conversation. `companions.defaultEffort` is the fallback for agents with no entry here."
        },
        "companions.includeActiveFileByDefault": {
          "type": "boolean",
          "default": true,
          "description": "Auto-add the active editor file as a context chip."
        },
        "companions.mentionIndexLimit": {
          "type": "number",
          "default": 5000,
          "minimum": 100,
          "description": "Max workspace files indexed for composer @ file autocomplete."
        },
        "companions.showThinking": {
          "type": "boolean",
          "default": false,
          "description": "Show companion internal reasoning / thinking traces in chat."
        },
        "companions.expandCommandOutputs": {
          "type": "boolean",
          "default": false,
          "description": "Expand tool details and command IN/OUT blocks by default."
        },
        "companions.steerByDefault": {
          "type": "boolean",
          "default": false,
          "description": "Inject typed messages straight into running turns instead of queueing."
        },
        "companions.soundNotifications": {
          "type": "boolean",
          "default": false,
          "description": "Play an audible chime when a background companion finishes a turn."
        },
        "companions.chatFontScale": {
          "type": "number",
          "default": 100,
          "minimum": 50,
          "maximum": 300,
          "description": "Zoom for the chat panel as a percent."
        },
        "companions.voiceStreaming": {
          "type": "boolean",
          "default": true,
          "description": "Stream voice transcription live as you speak."
        },
        "companions.voiceSendPhrase": {
          "type": "string",
          "default": "companion send",
          "description": "Spoken phrase that auto-submits when it ends a transcription."
        },
        "companions.telemetry.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable anonymous, privacy-first usage telemetry."
        },
        "companions.antigravityRequestPermissions": {
          "type": "boolean",
          "default": false,
          "description": "Request interactive confirmation before Antigravity executes tool calls (experimental)."
        },
        "companions.sensitiveFilesWarn": {
          "type": "boolean",
          "default": true,
          "description": "Show a warning when starting a session in a workspace containing sensitive files (.env, keys)."
        },
        "companions.claudeAllowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Explicit whitelist of tools for Claude Code. Replaces the default tool preset completely (sets _meta.claudeCode.options.tools). Leave empty to use Claude Code's full default preset."
        },
        "companions.claudeCustomAgents": {
          "type": "string",
          "default": "",
          "description": "Optional custom agent configuration (JSON object) or agent name for Claude Code (sets _meta.claudeCode.options.agents or .agent)."
        },
        "grok.cliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the grok binary. Empty = auto-discover (~/.grok/bin/grok, then PATH)."
        },
        "grok.codexCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the OpenAI Codex binary. Empty = auto-discover on PATH, then in the newest OpenAI ChatGPT bundle installed by VS Code, Cursor, or remote VS Code."
        },
        "grok.claudeCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the Anthropic Claude Code CLI (`claude`). Empty = auto-discover on PATH, then well-known user-bin locations. Authentication stays in Anthropic's own CLI."
        },
        "grok.geminiCliPath": {
          "type": "string",
          "default": "",
          "description": "Path to the Google Antigravity or Gemini CLI (`agy` / `gemini`). Empty = auto-discover on PATH, then well-known user-bin locations. Authentication stays in Google's own CLI."
        },
        "grok.defaultModel": {
          "type": "string",
          "default": "",
          "description": "Model ID to use on new sessions. Empty = CLI default. If a non-empty value isn't available for the session's agent, it's replaced with the model the CLI falls back to (no prompt)."
        },
        "grok.askTimeout": {
          "type": "string",
          "enum": [
            "off",
            "60s",
            "5m",
            "10m"
          ],
          "enumDescriptions": [
            "Wait indefinitely (default)",
            "Continue after 1 minute",
            "Continue after 5 minutes",
            "Continue after 10 minutes"
          ],
          "default": "off",
          "description": "Auto-continue an unanswered question card after this long. The selection you already marked is sent if the card is complete; otherwise the companion is told to continue without an answer. The timer runs in the editor, so closing the panel neither pauses nor extends it."
        },
        "grok.defaultMode": {
          "type": "string",
          "enum": [
            "",
            "agent",
            "yolo"
          ],
          "enumDescriptions": [
            "Agent mode (CLI default)",
            "Agent mode",
            "Auto accept — auto-approve every permission request"
          ],
          "default": "",
          "description": "Mode for new sessions. Updated automatically to remember your last switch between Agent and Auto accept (Plan is never remembered). Empty = Agent."
        },
        "grok.includeActiveFileByDefault": {
          "type": "boolean",
          "default": true,
          "description": "Auto-add the active editor file as a context chip."
        },
        "grok.mentionIndexLimit": {
          "type": "number",
          "default": 5000,
          "minimum": 100,
          "markdownDescription": "Max workspace files indexed for composer **@** file autocomplete. Default 5000 keeps a cold index build bounded; raise it (no upper limit) if files are missing from the `@` list in large repos. Applies on the next `@` after the setting changes. Very large values cost more time on the first rebuild."
        },
        "grok.acp.promptIdleTimeoutMs": {
          "type": "number",
          "default": 1800000,
          "minimum": 0,
          "markdownDescription": "How long a live `session/prompt` may stay silent before the extension reports **ACP request timed out**. Any `session/update` or in-turn server request (permission, terminal, …) resets this idle clock. Default 1800000 (30 minutes). `0` disables the idle cap. Applies to newly started sessions (#117)."
        },
        "grok.acp.promptAbsoluteTimeoutMs": {
          "type": "number",
          "default": 86400000,
          "minimum": 0,
          "markdownDescription": "Hard wall-clock cap for one `session/prompt`, even while the turn is still streaming. Default 86400000 (24 hours). `0` disables the absolute cap. Applies to newly started sessions (#117)."
        },
        "grok.acp.requestTimeoutMs": {
          "type": "number",
          "default": 120000,
          "minimum": 1000,
          "markdownDescription": "Timeout for ACP methods other than `session/prompt` (`initialize`, `session/new`, `session/set_model`, …). Default 120000 (2 minutes). Applies to newly started sessions."
        },
        "grok.defaultEffort": {
          "type": "string",
          "enum": [
            "",
            "none",
            "minimal",
            "low",
            "medium",
            "high",
            "xhigh",
            "max",
            "ultracode"
          ],
          "enumDescriptions": [
            "CLI default (no flag passed to grok agent stdio)",
            "None",
            "Minimal",
            "Low — faster, lighter reasoning",
            "Medium",
            "High",
            "XHigh — deepest reasoning, slowest",
            "Max — maximum reasoning effort",
            "Ultracode — xHigh + workflows"
          ],
          "default": "",
          "description": "Reasoning effort passed as --reasoning-effort to grok agent stdio. Values match the grok CLI's accepted set (none, minimal, low, medium, high, xhigh). Applies live on recent CLIs (grok 0.2.101+); older CLIs, and resetting to the model default, restart the session."
        },
        "grok.defaultEffortByProvider": {
          "type": "object",
          "default": {},
          "additionalProperties": {
            "type": "string"
          },
          "description": "Reasoning effort remembered per agent (grok, claude, codex, gemini). Updated automatically when you change the effort in a conversation. `grok.defaultEffort` is the grok-only fallback for agents with no entry here."
        },
        "grok.useCtrlEnterToSend": {
          "type": "boolean",
          "default": false,
          "description": "Use Ctrl/Cmd+Enter to send (Enter inserts newline)."
        },
        "grok.terminalShell": {
          "type": "string",
          "enum": [
            "auto",
            "cmd"
          ],
          "enumDescriptions": [
            "PowerShell on Windows (pwsh, then Windows PowerShell 5.1); the shell $SHELL names on macOS and Linux — matches the standalone Grok CLI",
            "Force cmd.exe on Windows, /bin/sh on macOS and Linux"
          ],
          "default": "auto",
          "markdownDescription": "Which shell runs the agent's commands. `auto` (default) matches the standalone Grok CLI: PowerShell on Windows — `pwsh.exe` if installed, else Windows PowerShell 5.1 (#46) — and the shell `$SHELL` names on macOS and Linux (typically `/bin/zsh`) instead of `/bin/sh`, which on macOS is bash 3.2 and cannot run a modern sdkman init (#140). The agent's command is also unwrapped from the `bash -lc` envelope Grok sends, and run as an explicit argument, so your shell cannot hand it straight back to bash. A `$SHELL` that isn't POSIX-compatible (fish, nushell) falls back to `/bin/sh`, because the agent writes POSIX commands. `cmd` forces `cmd.exe` on Windows and `/bin/sh` on macOS/Linux, if matching your shell causes trouble — a profile that prints on startup, or is slow to load. On Windows, changing this switches the shell straight away but the agent is told which dialect to write when a conversation starts, so **start a new conversation** for the change to apply to both halves. **Tip: install PowerShell 7 (`pwsh`)** — the 5.1 fallback rejects `&&` chains and reports every failing command's exit code as 1."
        },
        "grok.showThinking": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Show Grok's reasoning (thinking) traces in chat. Off by default — a lightweight \"Thinking…\" indicator stands in while Grok reasons. Also toggleable live from the gear menu → **Config & debug → Show thinking traces** (applies to already-loaded sessions too)."
        },
        "grok.expandCommandOutputs": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Expand tool details by default — each shell command's IN/OUT block (full command text + captured output) and each edit's inline diff (useful for auditing Auto-accept sessions). With this setting on, groups containing command or edit details (such as \"Ran N commands\" or \"Edited N files\") open too; read/explore-only groups stay collapsed, and a lone command outside a group opens its details. Off: click a row or group header (›) to expand it — edit rows always show a `+N −M` change count, even when their diff is closed. Also toggleable live from the gear menu → **Config & debug → Expand tool details**."
        },
        "grok.steerByDefault": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Send straight into Grok's running turn instead of queueing it. Off (default): a message you send while Grok is working waits, and is sent as one prompt when the turn ends — you can still steer it on demand with the **Steer** button on the pending message. On: sending while Grok works **skips the queue** and injects the message into the turn immediately, so Grok changes course mid-answer. Steering never cancels the turn and never discards in-flight tool work — it is not a Stop. It carries plain text only: no attached files, no editor context, and no `/commands`. Needs a recent Grok Build CLI; where it's unsupported, messages fall back to the queue. Also toggleable live from the gear menu → **Config & debug → Steer by default**."
        },
        "grok.soundNotifications": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Play a short sound when Grok finishes a turn or hits an error — a rising chime on completion, a lower tone on error. Off by default. Plays **only when the Grok panel isn't focused** (you've switched to the editor or another window), so it notifies you when you've stepped away without beeping while you watch. Also toggleable live from the gear menu → **Config & debug → Sound notifications**."
        },
        "grok.processingSound": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Play a quiet repeating sound while Grok is still working. The first cue waits seven seconds, then repeats every eight seconds until the turn ends. Off by default and separate from the completion/error sound. Also toggleable live from the gear menu → **Config & debug → Still-processing sound**."
        },
        "grok.readRepliesAloud": {
          "type": "boolean",
          "default": false,
          "description": "Read completed Grok replies aloud in the VS Code chat. Code blocks are skipped."
        },
        "grok.summarizeRepliesAloud": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Read simplified summaries of spoken Grok replies. Each reply costs an extra xAI call and adds network delay; the full reply is used if summarization fails. Uses the same credential resolution as Voice input. AFK Pilot keeps its own per-device browser preference."
        },
        "grok.thumbsFeedback": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Show thumbs on a finished Grok turn so you can send a rating to SpaceXAI. **Off by default.** On, thumbs appear only when this Grok session actually supports feedback — never on Codex or Claude. Ratings are not stored. Toggle live from Settings → General → **Thumbs feedback to SpaceXAI**."
        },
        "grok.telemetry.enabled": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Send anonymous, privacy-first usage telemetry. A single `session_start` event per session carries an anonymous install id (a random GUID, no account/identity) plus a low-cardinality settings snapshot (including mode/model/effort, host kind, UI preferences, whether voice input is available, and which providers are connected; full list in [docs/privacy.md](https://github.com/PawelHuryn/grok-vscode/blob/main/docs/privacy.md)) — **never** any message content, code, file paths, or free-text settings. Also requires VS Code's global `telemetry.telemetryLevel` to be enabled and this to be the official build; turning either setting off stops all sending."
        },
        "grok.chatFontScale": {
          "type": "number",
          "default": 100,
          "minimum": 60,
          "maximum": 300,
          "markdownDescription": "Zoom for the Grok chat panel only, as a percent (100 = default). Scales the whole chat UI — text, icons, spacing — without affecting the rest of VS Code (unlike `Ctrl/Cmd+Shift+=`). Applies live, no reload needed. Supports User (global) and Workspace (local) scope."
        },
        "grok.voiceApiKey": {
          "type": "string",
          "default": "",
          "markdownDescription": "Optional override key for voice Speech-to-Text (`api.x.ai/v1/stt`). **Leave empty and it just works** — the extension reuses your `grok login` token automatically. Set this (or `GROK_VOICE_API_KEY` / `XAI_API_KEY` in your workspace `.env`) only to use a dedicated [console.x.ai](https://console.x.ai) developer key instead. Setup + costs: [docs/voice-setup.md](https://github.com/PawelHuryn/grok-vscode/blob/main/docs/voice-setup.md)."
        },
        "grok.ffmpegPath": {
          "type": "string",
          "default": "",
          "description": "Path to the ffmpeg binary used to record the microphone for voice control. Empty = use 'ffmpeg' from PATH. Install ffmpeg from https://ffmpeg.org if voice control reports it as missing."
        },
        "grok.voiceInputDevice": {
          "type": "string",
          "default": "",
          "description": "Microphone device for voice control. Empty = system default (on Windows, the first DirectShow audio device is auto-detected). Set this to a specific device name (Windows/dshow) or index (macOS/avfoundation) to override."
        },
        "grok.voiceSendPhrase": {
          "type": "string",
          "default": "grok send",
          "description": "Spoken phrase that auto-submits the message when it ends a voice transcription (e.g. say \"…fix the bug, grok send\"). The phrase is stripped before sending. Leave empty to disable hands-free sending."
        },
        "grok.voiceKeyterms": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Words or phrases to help streaming voice recognition spell code and project vocabulary correctly (for example, \"Get-ChildItem\", \"useEffect\", or an internal package name). Put the most important terms first. xAI accepts at most 100 terms of 50 characters each; the send phrase and \"Grok\" are kept first, then user terms fill the remaining slots. Supports User and Workspace scope."
        },
        "grok.voiceLanguage": {
          "type": "string",
          "default": "",
          "description": "Optional language code for streaming voice text formatting (for example, \"en\", \"fr\", \"de\", or \"ja\"). When set, xAI converts spoken numbers, currencies, and units to written forms. Leave empty to keep Inverse Text Normalization off and preserve spoken-form text. Supports User and Workspace scope."
        },
        "grok.voiceStreaming": {
          "type": "boolean",
          "default": true,
          "description": "Stream voice transcription live as you speak (words appear in the composer in real time, and \"grok send\" can submit without a second click). Disable to use one-shot batch mode (click to start, click to stop, then transcribe). Streaming costs $0.20/hr of audio vs $0.10/hr batch."
        },
        "grok.antigravityRequestPermissions": {
          "type": "boolean",
          "default": false,
          "description": "Request interactive confirmation before Antigravity executes tool calls (experimental)."
        },
        "grok.sensitiveFilesWarn": {
          "type": "boolean",
          "default": true,
          "description": "Show a warning when starting a session in a workspace containing sensitive files (.env, keys)."
        },
        "grok.claudeAllowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Explicit whitelist of tools for Claude Code. Replaces the default tool preset completely (sets _meta.claudeCode.options.tools). Leave empty to use Claude Code's full default preset."
        },
        "grok.claudeCustomAgents": {
          "type": "string",
          "default": "",
          "description": "Optional custom agent configuration (JSON object) or agent name for Claude Code (sets _meta.claudeCode.options.agents or .agent)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "tsc -p .",
    "compile": "tsc -p .",
    "watch": "tsc -p . -w",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:perf": "vitest run --config vitest.perf.config.ts",
    "compile:integration": "node -e \"require('fs').rmSync('out-integration',{recursive:true,force:true})\" && tsc -p integration/tsconfig.json",
    "test:integration": "npm run compile && npm run compile:integration && vscode-test",
    "test:live": "tsc -p . && node scripts/live-tests.cjs",
    "e2e:hc-colour": "node scripts/hc-colour-screens.mjs",
    "e2e:welcome-tip": "node scripts/ide-welcome-tip-screens.mjs",
    "marketplace:login": "node scripts/marketplace-session.mjs",
    "marketplace:publish": "node scripts/marketplace-publish.mjs",
    "e2e:lifecycle-host": "npm run compile && node scripts/lifecycle-host.mjs",
    "smoke:live": "npm run compile && node scripts/live-smoke.mjs",
    "telemetry:probe": "tsc -p . && node scripts/telemetry-probe.cjs",
    "check:vsix": "npm run compile && node scripts/check-vsix-requires.mjs",
    "prepackage": "node -e \"const fs=require('fs'); for (const f of fs.readdirSync('.')) if (f.endsWith('.vsix')) fs.unlinkSync(f); fs.rmSync('out',{recursive:true,force:true})\" && npm run check:vsix",
    "package": "vsce package --readme-path README.marketplace.md",
    "publish": "vsce publish --readme-path README.marketplace.md",
    "publish:ovsx": "node scripts/publish-ovsx.mjs"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.94.0",
    "@types/ws": "^8.18.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "3.9.2",
    "electron": "^33.2.1",
    "happy-dom": "^20.9.0",
    "playwright": "^1.49.1",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "0.69.0",
    "@agentclientprotocol/codex-acp": "1.1.14",
    "jpeg-js": "^0.4.4",
    "ws": "^8.21.0"
  }
}
